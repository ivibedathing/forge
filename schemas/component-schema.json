{
  "component": {
    "$defs": {
      "BodyKind": {
        "description": "How a rigid body participates in simulation.",
        "oneOf": [
          {
            "const": "dynamic",
            "description": "Fully simulated: forces, gravity, collisions.",
            "type": "string"
          },
          {
            "const": "kinematic",
            "description": "Moved by writes to its `Transform`; pushes dynamics, is not pushed.",
            "type": "string"
          },
          {
            "const": "fixed",
            "description": "Never moves. For platforms that also want a `RigidBody`; bare\n`Collider`s without a `RigidBody` are static geometry too.",
            "type": "string"
          }
        ]
      },
      "ColliderFit": {
        "description": "How a proxy's size is decided (M39). See [`ColliderPart::fit`].",
        "enum": [
          "bone"
        ],
        "type": "string"
      },
      "ColliderPart": {
        "additionalProperties": false,
        "description": "One proxy of a [`SkinnedCollider`]: a simple shape fixed in one joint's\nframe (M33).\n\nFlat and discriminated by `shape`, exactly as [`Collider`] is and for the\nsame reasons — the schema-driven walk and the editor's generated inspector\nboth read flat structs, and `jq` and an LLM both write them. `cuboid`,\n`sphere` and `capsule` only: `trimesh` and `convex_hull` describe a\nspecific mesh, and a proxy exists precisely because that mesh lives on the\nGPU where physics cannot reach it (`collider_part_shape_unsupported`).",
        "properties": {
          "fit": {
            "anyOf": [
              {
                "$ref": "#/$defs/ColliderFit"
              },
              {
                "type": "null"
              }
            ],
            "description": "Solve this part's length from the posed bone instead of holding the\nauthored one (M39). Absent — the default, and every pre-M39 part — is\nM33's rule exactly: only the placement follows the rig, never the size.\n\n`\"bone\"` takes a `capsule`'s `half_height`, or a `cuboid`'s Y\nhalf-extent, from the posed distance between this part's joint and that\njoint's first child, so a proxy set fitted to a rest pose keeps fitting\na skeleton the solver is moving. A joint with no child keeps the\nauthored value, which is what a hand or a head has."
          },
          "half_extents": {
            "description": "`cuboid` only. Each component `> 0`.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": [
              "array",
              "null"
            ]
          },
          "half_height": {
            "description": "`capsule` only: half the cylindrical section's height. `> 0`.",
            "format": "float",
            "type": [
              "number",
              "null"
            ]
          },
          "joint": {
            "description": "The joint this proxy rides. Its posed world transform, times `offset`\nand `rotation`, is where the shape is each step.",
            "type": "string"
          },
          "name": {
            "description": "What reports call this part. Absent, the joint's name — which is right\nfor the ordinary one-proxy-per-joint set and wrong the moment a limb\ntakes two, hence the field. Unique within the component.",
            "type": [
              "string",
              "null"
            ]
          },
          "offset": {
            "default": [
              0.0,
              0.0,
              0.0
            ],
            "description": "Metres from the joint's origin, **in the joint's own frame**, so a\nproxy is authored against the bone rather than against the world.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "radius": {
            "description": "`sphere` and `capsule`. `> 0`.",
            "format": "float",
            "type": [
              "number",
              "null"
            ]
          },
          "rotation": {
            "default": [
              0.0,
              0.0,
              0.0
            ],
            "description": "Euler degrees in the joint's frame. A capsule's axis is local **+Y**\n(rapier's, and `builtin:cylinder`'s), and this is what turns it onto a\nbone that runs some other way.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "sensor": {
            "default": false,
            "description": "Sensors detect overlaps but exert no forces — `Collider.sensor`,\nmeaning the same thing. Per part rather than per component, because a\nsword's blade and its guard want opposite answers.",
            "type": "boolean"
          },
          "shape": {
            "$ref": "#/$defs/ColliderShapeKind"
          }
        },
        "required": [
          "joint",
          "shape"
        ],
        "type": "object"
      },
      "ColliderShapeKind": {
        "description": "The collision shape kinds `Collider.shape` may name.",
        "enum": [
          "cuboid",
          "sphere",
          "capsule",
          "trimesh",
          "convex_hull"
        ],
        "type": "string"
      },
      "FractureMaterial": {
        "description": "What a `Breakable` is made of (M43).\n\n`glass` shatters into radial slivers that fly far and fast; `wood` splits\ninto splinters along the grain and tumbles about its long axis; `stone`\nbreaks into chunky blocks that barely scatter, so a granite block comes\napart and its pieces *drop*; `metal` tears into a few large plates that\nmove together and slide.\n\nFour, because four is what the fracture generator has distinct algorithms\nfor and what the runtime has distinct behaviour for. A fifth would need\nboth, and \"generic\" is spelled by leaving the field out.\n\nNOTE: leave these variants undocumented, for [`ParticleBlend`]'s reason —\na doc comment on an enum *variant* makes schemars emit oneOf/const instead\nof a flat `\"enum\": [...]`, which blinds the validation walk's\nclosed-vocabulary check. Measured here: `\"material\": \"wud\"` reported\nnothing at all until these four comments moved up into this one.",
        "enum": [
          "glass",
          "wood",
          "stone",
          "metal"
        ],
        "type": "string"
      },
      "Fragment": {
        "additionalProperties": false,
        "description": "One piece a `Breakable` entity shatters into (M14, shards since M43).\n\nA fragment is **either** a mesh reference or a shard, and exactly one of the\ntwo (`fragment_geometry`):\n\n- `mesh` follows `Mesh.asset` rules (builtin or relative glTF path), and\n  `half_extents` is its cuboid collider in fragment-local units — M14's\n  shape, where shards are boxes to the solver.\n- `points` is a convex point set (M43) that becomes a [`Shard`]: the\n  fragment's collider *is* its geometry, so `half_extents` alongside it is\n  an error rather than a number physics quietly ignores.\n\n`offset`/`rotation`/`scale` place the fragment relative to the parent\nentity, so the assembled fragments overlay the unbroken model. `scale`\nscales the collider, exactly as `Transform.scale` does.",
        "properties": {
          "density": {
            "default": 1.0,
            "description": "Fragment mass comes from `density` x collider volume. `> 0`.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "half_extents": {
            "description": "Cuboid collider half-extents, for a `mesh` fragment. The default is\n`[0.5, 0.5, 0.5]`, matching `builtin:cube`, so a fragment that is a\nscaled builtin cube needs no collider authoring.\n\nOptional rather than defaulted-in-place since M43: a shard's collider\nis its own hull, so the difference between \"absent\" and \"written at the\ndefault\" is the difference between a valid fragment and a claim about a\ncollider physics never builds.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": [
              "array",
              "null"
            ]
          },
          "mesh": {
            "description": "The piece's geometry as a mesh reference. Exclusive with `points`.",
            "type": [
              "string",
              "null"
            ]
          },
          "offset": {
            "default": [
              0.0,
              0.0,
              0.0
            ],
            "description": "Position relative to the parent entity's origin, in parent-local\nunits (the parent's `Transform.scale` applies).",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "points": {
            "description": "The piece's geometry as a convex point set in parent-local metres\n(M43) — what `engine fracture` writes. Exclusive with `mesh`.",
            "items": {
              "items": {
                "format": "float",
                "type": "number"
              },
              "maxItems": 3,
              "minItems": 3,
              "type": "array"
            },
            "maxItems": 32,
            "minItems": 4,
            "type": [
              "array",
              "null"
            ]
          },
          "rotation": {
            "default": [
              0.0,
              0.0,
              0.0
            ],
            "description": "Euler angles in **degrees**, `[x, y, z]`, XYZ order — the same\nconvention as `Transform.rotation`.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "scale": {
            "default": [
              1.0,
              1.0,
              1.0
            ],
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          }
        },
        "type": "object"
      },
      "GrowthStage": {
        "additionalProperties": false,
        "description": "One keyframe of a [`Meadow`]'s life cycle.\n\nAll seven fields are required. A half-specified keyframe is an error rather\nthan a fade to black — M21's palette wrote that rule down, and a meadow's\ntable is read the same way: linearly interpolated, and **wrapping** from the\nlast keyframe back round to the first, so the cycle closes without anyone\nhaving to author phase 1.0 as a copy of phase 0.0.",
        "properties": {
          "at": {
            "description": "Where in the cycle this keyframe sits, `[0, 1)`. Strictly increasing\ndown the table (`meadow_stages_invalid`).",
            "format": "float",
            "maximum": 0.999,
            "minimum": 0.0,
            "type": "number"
          },
          "color": {
            "description": "Linear RGB at the plant's base, each component `[0, 1]`.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "height": {
            "description": "Plant height as a fraction of [`Meadow::height`], `>= 0`. `0` is a plant\nthat is not there, which is what the seed keyframe says.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "lean": {
            "description": "Degrees the plant leans from vertical at its tip. The bend is a\ncantilever — the tip leans this far and the root not at all — so a large\nvalue at the end of the cycle is the plant collapsing rather than\ntipping over rigidly. `[0, 90]`.",
            "format": "float",
            "maximum": 90.0,
            "minimum": 0.0,
            "type": "number"
          },
          "sway": {
            "description": "How much of [`Meadow::wind`] reaches the plant at this stage, `>= 0`.\n\nThis is what separates green grass that flows from dry stalks that\nstand, and it is nearly free — the wind term is already in the vertex\nstage.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "tip_color": {
            "description": "Linear RGB at the plant's tip, each component `[0, 1]`.\n\nTwo colours rather than one because senescence runs tip-downward in real\ngrass: a stand going over turns straw at the top while its base is still\ngreen, and a single flat colour per stage looks painted on.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "width": {
            "description": "Blade width as a fraction of [`Meadow::blade_width`], `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          }
        },
        "required": [
          "at",
          "height",
          "width",
          "lean",
          "sway",
          "color",
          "tip_color"
        ],
        "type": "object"
      },
      "HudAlign": {
        "description": "Cross-axis alignment of a [`HudPanel`]'s children (M31). See [`HudLayout`]\nfor why the variants are undocumented.",
        "enum": [
          "start",
          "center",
          "end"
        ],
        "type": "string"
      },
      "HudAnchor": {
        "description": "Where a HUD element attaches on screen (M12).\n\n`offset` is measured **inward** from the anchor: from a right anchor,\n`offset[0]` runs leftward; from a bottom anchor, `offset[1]` runs upward;\nfrom `center` it is the usual +x-right / +y-down applied to the element's\ncenter. The anchored point is the element's matching corner (its center\nfor `center`), so `offset: [0, 0]` puts the element flush against its\ncorner at any resolution.",
        "enum": [
          "top_left",
          "top_right",
          "bottom_left",
          "bottom_right",
          "center"
        ],
        "type": "string"
      },
      "HudLayout": {
        "description": "How a [`HudPanel`] arranges its children (M31).\n\nNOTE (schemars): variants carry no doc comments on purpose. A doc comment\non a *variant* turns the generated schema from a flat `\"enum\": [...]` into\noneOf/const, which blinds the validation walk's closed-vocabulary check —\nthe same trap `ColliderShapeKind` documents.",
        "enum": [
          "free",
          "row",
          "column"
        ],
        "type": "string"
      },
      "HudTextAlign": {
        "description": "Horizontal alignment of text within its own box (M31). See [`HudLayout`]\nfor why the variants are undocumented.",
        "enum": [
          "left",
          "center",
          "right"
        ],
        "type": "string"
      },
      "JointPose": {
        "additionalProperties": false,
        "description": "One joint's local placement while a [`Ragdoll`] owns the skeleton (M39).\n\n**`rotation` is a quaternion, `w` last — the one rotation in this format\nthat is not XYZ Euler degrees.** `CLAUDE.md` names the reason under Traps:\nXYZ Euler clamps the middle angle to ±90°, so an orientation the solver\nintegrated past that comes back as the `(±180, θ, ±180)` twin, and a\nragdoll's joints go past it in the first second. M30 drew the same line for\nskeletal clips, and the distinction is *who wrote the numbers*: these are\nthe engine's, like a DCC tool's, not an agent's.",
        "properties": {
          "joint": {
            "description": "The joint this places. Every joint of the rig appears exactly once.",
            "type": "string"
          },
          "rotation": {
            "default": [
              0.0,
              0.0,
              0.0,
              1.0
            ],
            "description": "`[x, y, z, w]`, glTF's order and rapier's.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 4,
            "minItems": 4,
            "type": "array"
          },
          "scale": {
            "description": "Carried only when it is not 1: a ragdoll does not scale bones, so this\nis whatever the clip had at the moment of handoff.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": [
              "array",
              "null"
            ]
          },
          "translation": {
            "default": [
              0.0,
              0.0,
              0.0
            ],
            "description": "Metres, in the parent joint's frame.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          }
        },
        "required": [
          "joint"
        ],
        "type": "object"
      },
      "JunctionArm": {
        "additionalProperties": false,
        "description": "One road arriving at a [`Junction`].",
        "properties": {
          "end": {
            "$ref": "#/$defs/JunctionEnd",
            "default": "end",
            "description": "Which end of it arrives."
          },
          "road": {
            "default": "",
            "description": "Name of the entity carrying the `Road` that arrives here (invariant 4).",
            "type": "string"
          }
        },
        "type": "object"
      },
      "JunctionEnd": {
        "description": "Which end of a road arrives at a [`Junction`].",
        "oneOf": [
          {
            "const": "start",
            "description": "The road's first point.",
            "type": "string"
          },
          {
            "const": "end",
            "description": "The road's last point. A closed road has no free end, so an arm onto one\nis a validation error.",
            "type": "string"
          }
        ]
      },
      "ParticleBlend": {
        "description": "How a particle's color combines with what is already on screen.\n\nNOTE: leave these variants undocumented. A doc comment on an enum *variant*\nmakes schemars emit oneOf/const instead of a flat `\"enum\": [...]`, which\nblinds the validation walk's closed-vocabulary check — the same trap\n[`ColliderShapeKind`] carries a note about.",
        "enum": [
          "alpha",
          "additive"
        ],
        "type": "string"
      },
      "PlantedFoot": {
        "additionalProperties": false,
        "description": "One foot of a [`FootPlant`]: which joint it is, and how far up the chain\nthe solver may rotate to reach the ground.",
        "properties": {
          "ankle": {
            "description": "The ankle joint — the end of the chain, the thing put on the ground.",
            "type": "string"
          },
          "chain": {
            "default": 2,
            "description": "How many joints **above** the ankle rotate to reach the target. `2` is\nthe ordinary leg (knee and hip) and is what the two-bone solve means;\n`1` bends a single hinge and is what a stubby prop leg wants.",
            "format": "uint32",
            "maximum": 2,
            "minimum": 1,
            "type": "integer"
          },
          "sole": {
            "default": 0.0,
            "description": "Metres from the ankle joint down to the bottom of the foot, so the sole\nmeets the ground rather than the joint being buried in it.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          }
        },
        "required": [
          "ankle"
        ],
        "type": "object"
      },
      "RagdollJoint": {
        "additionalProperties": false,
        "description": "A per-joint override of [`Ragdoll`]'s default cone (M39).\n\nA ragdoll whose every joint is a 45° cone reads as a bag. A knee that only\nbends one way, and only backwards, is what makes it read as a body — and\nboth numbers being in the file is what makes it tunable without touching\nRust.",
        "properties": {
          "hinge": {
            "description": "Turn this joint into a hinge about this axis in the **child** part's\nlocal frame, instead of a cone. An elbow or a knee.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": [
              "array",
              "null"
            ]
          },
          "joint": {
            "description": "The joint to override. Some part of the `SkinnedCollider` must ride it,\nand it must not be the root — a root part has no joint to constrain.",
            "type": "string"
          },
          "limit": {
            "description": "Half-angle of the cone, in degrees. Ignored when `hinge` is set.",
            "format": "float",
            "maximum": 180.0,
            "minimum": 0.0,
            "type": [
              "number",
              "null"
            ]
          },
          "range": {
            "description": "The hinge's travel in degrees, `[min, max]`. `hinge` only.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 2,
            "minItems": 2,
            "type": [
              "array",
              "null"
            ]
          }
        },
        "required": [
          "joint"
        ],
        "type": "object"
      },
      "RoadMarkings": {
        "additionalProperties": false,
        "description": "What is painted on a road, and where.\n\nEvery marking is computed per pixel from the road's surface coordinates —\n`u`, metres from the centerline across the road, and `v`, metres along it —\nrather than built as geometry laid on the asphalt. That is what makes a line\nfollow every curve and grade for free, keeps a dash the same length in\nmetres through a hairpin as on a straight, and means paint can never\nz-fight: it is not a surface on a surface, it is the same pixel shaded\ndifferently.",
        "properties": {
          "center_dash": {
            "default": 3.0,
            "description": "Length of one painted dash in the centre line, in metres. `0` makes it\nsolid. `>= 0`.\n\nOn a **closed** road the dash period is snapped to a whole number of\nrepeats around the lap, so the pattern meets itself exactly at the seam\ninstead of leaving a short dash there. The ratio of dash to gap is what\nis preserved, not their absolute lengths.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "center_gap": {
            "default": 6.0,
            "description": "Unpainted gap between dashes, in metres. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "center_width": {
            "default": 0.0,
            "description": "Width of the centre line, in metres. `0` (the default) is no centre\nline, which is what a race circuit wants. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "color": {
            "default": [
              0.8799999952316284,
              0.8799999952316284,
              0.8799999952316284
            ],
            "description": "Linear RGB of the paint — the edge lines, the centre line, the start\nline, and the white half of a kerb. Each component `[0, 1]`.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "edge_inset": {
            "default": 0.10000000149011612,
            "description": "Gap between the asphalt's edge and the outside of the edge line, in\nmetres. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "edge_width": {
            "default": 0.14000000059604645,
            "description": "Width of the line down each edge of the asphalt, in metres. `0` is no\nedge line. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "kerb_color": {
            "default": [
              0.800000011920929,
              0.10000000149011612,
              0.07999999821186066
            ],
            "description": "Linear RGB of the red half of a kerb; the other half is `color`. Each\ncomponent `[0, 1]`.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "kerb_max_radius": {
            "default": 0.0,
            "description": "Corners with a radius at or under this get a kerb on the **inside** of\nthe turn, in metres. `0` (the default) is no kerbs. `>= 0`.\n\nWhich corners are tight enough, and which side of the road is the inside\nof the turn, are facts about the plan-view geometry that per-pixel code\ncannot know, so they are computed once and handed to the shader as spans\n— at most [`MAX_ROAD_KERBS`](crate::road::MAX_ROAD_KERBS) of them.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "kerb_stripe": {
            "default": 1.399999976158142,
            "description": "Length of one red or white kerb stripe, in metres. `> 0`.\n\nFitted per corner to a whole number of stripes, so a kerb begins and\nends on a stripe boundary rather than on a sliver.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "kerb_width": {
            "default": 0.8999999761581421,
            "description": "How far a kerb reaches out from the asphalt edge, in metres. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "start_line": {
            "default": false,
            "description": "Paint a line across the road.\n\nPainted rather than built, which matters more here than anywhere else: a\nstart line with any real height is a wall the car is parked against at\nstep 0.",
            "type": "boolean"
          },
          "start_line_at": {
            "default": 0.0,
            "description": "Where that line goes: metres along the centerline from the road's first\npoint. `>= 0`, and past the end of a closed road it wraps.\n\nA position rather than \"wherever the polygon starts\", because the two\nare different jobs: the polygon's first point is a *corner*, chosen by\nthe shape of the circuit, and a start line belongs partway down a\nstraight. Splitting a straight with an extra point to move the line\nwould be geometry surgery in service of paint — and on a short straight\nit fails outright, because the neighbouring corner's arc already covers\nthe place the line wanted to be. `engine road-centerline` is how a\ngenerator turns \"here, in world space\" into this number.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "start_line_width": {
            "default": 0.699999988079071,
            "description": "Width of that line along the road, in metres. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          }
        },
        "type": "object"
      },
      "RoadPoint": {
        "additionalProperties": false,
        "description": "One authored point on a road's centerline.\n\nThe road is a **polygon with corner radii**, not a spline: a closed polygon\nreturns to its own first vertex and its exterior angles sum to exactly one\nturn, so position and heading close without solving anything. Nothing here\ncarries a heading, deliberately — a heading is derived, and a stored one can\nbe edited into disagreeing with the points on either side of it.",
        "properties": {
          "bank": {
            "default": null,
            "description": "Roll of the cross-section here, in degrees, positive raising the\ndriver's **right** edge (M40).\n\nAbsent falls through to [`Road::auto_bank`], which is the way to author\na circuit: auto-banking knows which way the road turns and so raises the\noutside of every corner without the file carrying a sign that has to\nagree with the winding of a polygon being edited beside it. Reach for an\nexplicit value for the cases auto-banking cannot know — a banked\nstraight, a corner deliberately flat.",
            "format": "float",
            "type": [
              "number",
              "null"
            ]
          },
          "pin_height": {
            "default": false,
            "description": "Read `position.y` as an absolute height even though the road follows a\nterrain (M40) — a bridge deck, a level a junction has to meet.\n\nThe pin is a *local* correction to the followed profile, faded out over\n[`Road::follow_blend`] metres either side, so the road reaches this\nheight exactly and goes back to hugging the ground. Two pins closer\ntogether than that blend pull on each other and neither is reached\nexactly; validation warns (`road_pins_overlap`).\n\nWithout `follow_terrain` every height is already absolute and this does\nnothing.",
            "type": "boolean"
          },
          "position": {
            "default": [
              0.0,
              0.0,
              0.0
            ],
            "description": "Where the centerline passes, in the entity's local space. `y` is the\nroad *surface* height there; the profile between points is a monotone\ncubic through these, so the grade turns over smoothly and never\novershoots an authored height.\n\nWith [`Road::follow_terrain`] set, `y` means something different: it is\nthe **clearance above the sampled ground**, so `0` sits the road on the\nterrain. [`pin_height`](Self::pin_height) turns it back into an absolute\nheight for one point.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "radius": {
            "default": 0.0,
            "description": "Radius of the arc rounding this corner, in metres. `>= 0`.\n\n`0` is a sharp vertex — mitred, which is exactly right for a point that\nis not really a turn (a start line partway along a straight) and wrong\nfor one that is: past\n[`MAX_SHARP_TURN_DEGREES`](crate::road::MAX_SHARP_TURN_DEGREES) of turn\nthe mitre folds back through the road and validation refuses it.\n\nThe two arcs meeting on one edge have to fit on it, which is the other\nthing a polygon cannot guarantee (`road_corner_does_not_fit`).",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "width": {
            "default": null,
            "description": "Width of the asphalt here, in metres, overriding the road's `width`\n(M40). Absent — the default — is the road's own width, so a file that\nnever mentions this is the M23 road exactly. `> 0`.\n\nInterpolated along the centerline by the same monotone cubic the heights\nuse, so a road never bulges wider than the widest width authored on it.\n**The whole cross-section scales, shoulder included**: `u` — signed\nmetres across the road — has to mean one thing at every `v` for the\nshader to find the asphalt edge, so the widening is one factor on the\nsection rather than a metre value added to the asphalt alone. Paint\nscales with it: a section at 1.5× width wears a 1.5× wider edge line.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": [
              "number",
              "null"
            ]
          }
        },
        "type": "object"
      },
      "TerrainBasin": {
        "additionalProperties": false,
        "description": "One circular depression a [`Terrain`] cuts into its height field (M42): a\nflat floor, a smoothstepped wall, and how far down the floor sits.\n\nThe shape is `radius` metres of floor surrounded by `falloff` metres of wall,\nso the whole footprint is `radius + falloff` and the shoreline of any water\nput here lands somewhere on the wall. **Two numbers rather than one**: a\nsingle radius with a falloff to nothing gives a dish, and a dish deep enough\nto hold water has walls gentle enough that the waterline creeps far past\nwhere it was authored.\n\n**Overlapping basins take the deepest, not the sum.** Overlapping circles are\nhow anything that is not a circle gets authored — a lake is three of them, a\nchannel a line of them — and under a sum every overlap digs to twice the\ndepth, turning a lake into a ring of pits. The cost is a faint crease where\ntwo basins are equally deep, which is under the water in every use this was\nbuilt for.",
        "properties": {
          "center": {
            "default": [
              0.0,
              0.0
            ],
            "description": "Where the basin is centred, in **world** XZ metres — the same space\neverything else about a terrain is sampled in, and the same numbers the\n`Water` entity's `Transform.position` is written in.\n\nWorld rather than the patch's local space for M22's reason: heights are\na function of world position, which is what lets two patches sharing one\ndescription meet seamlessly. A basin in local space would be dragged\naround by its patch and would break that.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 2,
            "minItems": 2,
            "type": "array"
          },
          "depth": {
            "default": 0.0,
            "description": "Metres the floor drops below the surrounding field, `>= 0`.\n\nIn the same space as [`Terrain::height`] — metres of relief at\n`Transform.scale.y` 1, so the patch's vertical scale multiplies a basin\nexactly as it multiplies the hills. A second convention here would make\na patch at `scale.y: 2` disagree with itself about which of its numbers\nare metres.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "falloff": {
            "default": 4.0,
            "description": "Metres of wall between the floor's edge and the untouched field, `>= 0`.\n\nSmoothstepped, with the interpolant the value noise already uses, so the\nwall meets the surrounding ground with a zero derivative and the rim has\nno crease. 0 is a vertical wall, which reads as a quarry rather than a\npond and is occasionally what is wanted.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "radius": {
            "default": 0.0,
            "description": "Metres of flat floor at the centre, `>= 0`. 0 is legitimate and makes\nthe basin a smooth conical hollow of `falloff` metres.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          }
        },
        "type": "object"
      },
      "TerrainLayer": {
        "additionalProperties": false,
        "description": "One material a [`Terrain`] paints itself with, claiming a band of height and\na band of slope.\n\nA pixel's surface is the weighted average of every layer whose bands it falls\ninside; a layer that leaves both at their defaults covers everything and is\nthe base coat.\n\n**Slope does the heavy lifting.** Height alone gives horizontal stripes — a\ncontour map, and unmistakably so once the camera moves. What separates rock\nfrom grass in the world is that soil cannot cling to a steep face, so\n`slope_range` is the selector that reads as real and `height_range` adds\nbands of climate on top of it.",
        "properties": {
          "albedo": {
            "default": [
              0.12999999523162842,
              0.17000000178813934,
              0.09000000357627869
            ],
            "description": "Linear RGB, each component `[0, 1]` — `Material.albedo` for this band.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "height_blend": {
            "default": 0.5,
            "description": "Metres over which this layer fades out beyond each end of\n`height_range`. `>= 0`; 0 is a hard edge, which reads as a cut line and\nis almost never what ground does.\n\nAbsolute, not a fraction of the band — that was tried first and is a\ntrap. A fraction means a wide band gets a wide fade, so a layer written\nas \"above 1.9 m\" with a generous top end bleeds ten metres *below* where\nit was aimed and washes out everything under it. In metres, what the\nauthor writes is what the surface does.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "height_range": {
            "default": [
              -1000.0,
              1000.0
            ],
            "description": "World-space Y band this layer covers, in metres, `[min, max]`.\n\nWorld Y, not a fraction of [`Terrain::height`], so a layer's altitude\ndoes not shift when the relief is retuned — and so that two patches at\ndifferent elevations can share one description of what grows where.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 2,
            "minItems": 2,
            "type": "array"
          },
          "noise": {
            "default": 0.5,
            "description": "How much the detail noise jitters this layer's boundary, `[0, 1]`, as a\nfraction of its own fade widths.\n\nDrawn honestly, the boundary between two layers is an iso-line of a\nsmooth function — a clean sweeping curve, and the eye reads clean curves\nas artificial faster than it reads anything else here. Jittering the\nheight and slope the layer *thinks* it is at breaks the boundary into\ninterlocking fingers at two scales, for one multiply-add.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "roughness": {
            "default": 0.949999988079071,
            "description": "Perceptual roughness, `[0, 1]`, meaning what `Material.roughness` means.\nWet rock and packed sand are smoother than grass, and the difference\nshows wherever the sun is low.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "slope_blend": {
            "default": 8.0,
            "description": "Degrees over which this layer fades out beyond each end of\n`slope_range`. `>= 0`.\n\nSeparate from [`height_blend`](TerrainLayer::height_blend) because the\ntwo bands are in different units and one number cannot be honest about\nboth.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "slope_range": {
            "default": [
              0.0,
              90.0
            ],
            "description": "Surface-angle band this layer covers, in degrees from horizontal,\n`[min, max]`. 0 is flat ground, 90 a vertical face.",
            "items": {
              "format": "float",
              "maximum": 90.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 2,
            "minItems": 2,
            "type": "array"
          }
        },
        "type": "object"
      },
      "TreeLeaf": {
        "description": "What hangs on a tree's outermost branches.\n\nNOTE: leave these variants undocumented, like [`ParticleBlend`]'s — a doc\ncomment on a variant turns the schema into oneOf/const and blinds the\nvalidation walk's closed-vocabulary check.",
        "enum": [
          "blade",
          "cluster",
          "none"
        ],
        "type": "string"
      },
      "Wave": {
        "additionalProperties": false,
        "description": "One travelling wave in a [`Water`] surface's sum.\n\nGerstner rather than a sine: a Gerstner wave moves each point of the surface\n*toward* the crests as well as up, which sharpens crests and flattens\ntroughs. A sum of sines is a rubber sheet, and no amount of tuning it makes\nit read as water.",
        "properties": {
          "amplitude": {
            "default": 0.05999999865889549,
            "description": "Half the crest-to-trough height, in metres. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "direction": {
            "default": 0.0,
            "description": "Heading the wave travels, in degrees: a yaw about +Y applied to the\nengine's forward axis, so `0` travels toward **−Z** and `90` toward\n**−X**. The same convention as aiming a camera, a light or a particle\ncone.",
            "format": "float",
            "type": "number"
          },
          "speed": {
            "default": 1.2000000476837158,
            "description": "Crest travel speed in metres/second. `>= 0`; reverse a wave with\n`direction`, not a negative speed.\n\nReal deep-water waves are dispersive (`c = sqrt(g·λ/2π)`, so long waves\noutrun short ones), and authoring speed per wave rather than deriving it\nkeeps a scene free to be wrong about that on purpose — a lake's chop\nlooks better slightly slower than physics says.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "steepness": {
            "default": 0.4000000059604645,
            "description": "How far the surface is pulled toward the crests, `[0, 1]`: 0 is a plain\nsine, 1 is a cusp.\n\nThe **sum** over every wave must not exceed 1\n(`water_waves_self_intersect`). Past that the surface folds through\nitself and the crests curl into loops — a well-known Gerstner failure,\nand cheaper to refuse than to debug from a screenshot.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "wavelength": {
            "default": 4.0,
            "description": "Crest-to-crest distance in metres. `> 0`.\n\nThis is the field that sets the *scale* of the water: 40 m swells read\nas ocean, 2 m chop as a lake, 0.4 m as a puddle. It must also be\ncomfortably larger than one grid quad, or the surface cannot represent\nthe wave — see [`Water::segments`].",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          }
        },
        "type": "object"
      }
    },
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "description": "One component as it appears in a scene file.\n\nInternally tagged on `\"type\"`, so each component is a flat object\nrather than a single-key wrapper — the shape `jq` is pleasant\nagainst and the shape an LLM is least likely to get wrong.",
    "oneOf": [
      {
        "additionalProperties": false,
        "description": "Position, orientation, and scale.\n\nAll three fields are optional in JSON; omitting one gives the identity\nvalue, so a scene can say `{\"type\": \"Transform\", \"position\": [0, 3, 0]}`\nwithout restating a rotation and scale it does not care about.\n\n**One world unit is one metre**, and that is the convention every other\nnumber in the format is quoted against: gravity is `-9.81` because a body\nfalls 9.81 m/s², `Tree.height: 6.0` is a six-metre tree, and a `Wheel`\n0.35 m in radius belongs under a car 1.7 m wide. Time is seconds and mass\nis kilograms, so `Collider.density` is kg/m³ — note its default of `1.0`\nis *not* a plausible material, and a body meant to be pushed by forces\nwants a real one (the demo car's box chassis carries `350`, which is how\n4.3 m³ becomes 1.5 t).",
        "properties": {
          "position": {
            "default": [
              0.0,
              0.0,
              0.0
            ],
            "description": "World-space `[x, y, z]` **in metres**. +Y is up.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "rotation": {
            "default": [
              0.0,
              0.0,
              0.0
            ],
            "description": "Euler angles in **degrees**, `[x, y, z]`, applied in X→Y→Z intrinsic\norder (glam `EulerRot::XYZ`). Identity is `[0, 0, 0]`.\n\nDegrees rather than a quaternion is a settled decision (design doc §9):\nan agent told to \"rotate 45° about Y\" writes `[0.0, 45.0, 0.0]`\ndirectly, whereas an unlabeled `[x, y, z, w]` array invites silent\nordering bugs.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "scale": {
            "default": [
              1.0,
              1.0,
              1.0
            ],
            "description": "Multiplier on the entity's own geometry, per axis. Identity is\n`[1, 1, 1]`.\n\n**Every `builtin:` primitive is one metre across at scale 1**, so on\nthose this field reads directly as a size in metres: a\n`builtin:cube` at `[1.7, 0.7, 3.6]` is a car-sized box, and a\n`builtin:sphere` at `[0.9, 0.9, 0.9]` is 0.9 m across — *not* 1.8.\nThe recipes size the same way (`Terrain`, `Water` and `Meadow` take\ntheir footprint from `scale` in XZ), and it also multiplies\n`Collider` dimensions, which is why a cuboid collider matching a\nbuiltin cube is authored as `half_extents: [0.5, 0.5, 0.5]` in\n*local* units rather than in metres.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "type": {
            "const": "Transform",
            "type": "string"
          }
        },
        "required": [
          "type"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "Renderable geometry.\n\n`asset` is either a `builtin:` primitive (`builtin:cube`, `builtin:cylinder`,\n`builtin:plane`, `builtin:sphere`, `builtin:triangle`) or a `.gltf`/`.glb`\nfile's relative path, resolved\nagainst the directory of the scene file that references it (invariant 3).\n`engine validate` checks the reference resolves; a file that exists but\nfails to parse is reported by validation's asset pass and again at render\ntime.",
        "properties": {
          "asset": {
            "type": "string"
          },
          "type": {
            "const": "Mesh",
            "type": "string"
          }
        },
        "required": [
          "type",
          "asset"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "A viewpoint. `engine screenshot --camera <name>` selects one by entity name.",
        "properties": {
          "active": {
            "default": false,
            "description": "Marks the camera used when none is named explicitly.\n\nExactly one camera in a scene may set this. Zero or several is a\nvalidation error rather than a warning-plus-guess: a deterministic\nfailure is cheaper for an agent than a nondeterministic success.",
            "type": "boolean"
          },
          "far": {
            "default": 1000.0,
            "description": "Far clip distance. Strictly positive, and must exceed `near`\n(checked cross-field by `engine validate`).",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "fov": {
            "default": 60.0,
            "description": "Vertical field of view, in degrees. Strictly between 0 and 180.",
            "exclusiveMaximum": 180.0,
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "near": {
            "default": 0.10000000149011612,
            "description": "Near clip distance. Strictly positive.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "type": {
            "const": "Camera",
            "type": "string"
          }
        },
        "required": [
          "type"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "Surface appearance, in the metallic/roughness parameterization every\nmainstream engine and glTF file uses.\n\nAll color fields are **linear** RGB in `[0, 1]` — physical reflectance, not\nsRGB-encoded screen values. The engine never silently decodes an authored\ncolor; the PNG pixel is the lit, sRGB-encoded result, so `albedo: [0.5,\n0.5, 0.5]` under full light reads back ≈188, not 128.\n\nSince M26 a material may instead be a **file**: `{\"type\": \"Material\",\n\"asset\": \"materials/asphalt.json\"}` names a JSON document holding these same\nfields minus the `\"type\"`. `asset` is exclusive with every other field —\nsetting both is `material_asset_with_fields` — because serde cannot tell an\nabsent field from one written at its default, so a partial override would\nresolve to something the file does not say. A variant is a second file.",
        "properties": {
          "albedo": {
            "default": [
              0.800000011920929,
              0.800000011920929,
              0.800000011920929
            ],
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "albedo_map": {
            "description": "An sRGB colour texture, **multiplied** by `albedo` (M26).\n\nA tint over the map, not a replacement — which means the default\n`[0.8, 0.8, 0.8]` darkens an imported texture by 20% unless the file\nsays `\"albedo\": [1, 1, 1]` beside the map. `engine import` writes that\nexplicitly; a hand-authored material has to.",
            "type": [
              "string",
              "null"
            ]
          },
          "alpha": {
            "default": 1.0,
            "description": "Uniform opacity: `1` = opaque, `0` = invisible. Range `[0, 1]`.\n\nA flat blend with no view dependence — the \"ghost this object\" knob.\nAnything below 1 moves the entity out of the opaque pass and into the\nsorted blended one, where it tests depth but does not write it.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "alpha_cutoff": {
            "default": 0.0,
            "description": "Above 0, a pixel whose `albedo_map` alpha falls below this is\ndiscarded — and so is its shadow, through a second caster pipeline with\na fragment stage. This is what an alpha-cut leaf needs. Range `[0, 1]`;\n`0` (the default) cuts nothing, so the depth-only caster pass every\ncurrent scene uses is untouched.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "asset": {
            "description": "A `materials/*.json` file holding this material, relative to the scene\nfile (invariant 3). Exclusive with every other field.",
            "type": [
              "string",
              "null"
            ]
          },
          "attenuation": {
            "default": [
              1.0,
              1.0,
              1.0
            ],
            "description": "What survives that path, per linear-RGB channel: transmitted colour is\nscaled by `exp(-(1 - attenuation) * thickness)`. `[1, 1, 1]` absorbs\nnothing.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "emissive": {
            "default": [
              0.0,
              0.0,
              0.0
            ],
            "description": "Added after lighting, unaffected by any light — \"make this visible\nregardless of lighting\" is a debugging move worth having. Range\n`[0, 1]` per component.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "emissive_map": {
            "description": "An sRGB colour texture multiplied by `emissive`.",
            "type": [
              "string",
              "null"
            ]
          },
          "ior": {
            "default": 1.0,
            "description": "Index of refraction, `1.0` (the default) being no bending at all.\nRead only by a transmissive surface, where it refracts the view vector\nagainst the shading normal and offsets the scene-colour sample. Range\n`[1, 3]` — glass is 1.5, water 1.33, diamond 2.4.",
            "format": "float",
            "maximum": 3.0,
            "minimum": 1.0,
            "type": "number"
          },
          "metallic": {
            "default": 0.0,
            "description": "`0` = dielectric, `1` = metal. Metals have no diffuse; their specular\nis tinted by `albedo`. Range `[0, 1]`.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "normal_map": {
            "description": "A tangent-space normal map, linear data. The tangent frame is derived\nper pixel from screen-space derivatives rather than stored per vertex,\nso this works unmodified on `Water`, `Terrain`, `Road`, `Tree` and\n`Cloud` geometry, none of which carries tangents.",
            "type": [
              "string",
              "null"
            ]
          },
          "normal_strength": {
            "default": 1.0,
            "description": "Scales the normal map's tangent-space XY. The first thing anyone does\nwith a normal map is discover it is too strong, and the alternative is\nre-authoring the texture.",
            "format": "float",
            "maximum": 8.0,
            "minimum": 0.0,
            "type": "number"
          },
          "orm_map": {
            "description": "Occlusion in R, roughness in G, metallic in B — glTF's packing, so an\nimport is a file copy rather than a channel re-pack. Linear data, never\ncolour. R multiplies the ambient and sky terms only, never the direct\nsun: that is what makes it *ambient* occlusion rather than a second\nshadow. G and B multiply `roughness` and `metallic`.",
            "type": [
              "string",
              "null"
            ]
          },
          "roughness": {
            "default": 0.8999999761581421,
            "description": "Perceptual roughness: `0` = mirror-tight highlight, `1` = matte.\nRange `[0, 1]`.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "thickness": {
            "default": 0.0,
            "description": "How far light travels inside the surface, in metres. Both the scale of\nthe refraction offset and the Beer–Lambert path length, so a thick block\nof ice is finally greener than a thin one. `0` is the pre-M26 behaviour.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "transmission": {
            "default": 0.0,
            "description": "How much light passes *through* the surface instead of scattering off\nit: `0` = opaque, `1` = clear glass. Range `[0, 1]`.\n\nUnlike [`Material::alpha`] this is view-dependent and keeps the\nspecular lobe, which is the whole difference between a transparent\nobject and a faded one: a water surface seen edge-on reflects the sky\nand hides its bottom, and seen from overhead it does neither. The\napproximation is a Fresnel lerp back toward opaque at grazing angles,\nwith the diffuse term scaled by `1 - transmission` (light that went\nthrough did not come back). There is no refraction and no\nscene-color sampling, so what is behind the surface is not bent or\ntinted by its thickness.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "type": {
            "const": "Material",
            "type": "string"
          },
          "uv_offset": {
            "default": [
              0.0,
              0.0
            ],
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 2,
            "minItems": 2,
            "type": "array"
          },
          "uv_scale": {
            "default": [
              1.0,
              1.0
            ],
            "description": "Tiling for every map on this material: UV × `uv_scale` + `uv_offset`.\nSampling repeats on both axes, so `[20, 20]` is twenty tiles.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 2,
            "minItems": 2,
            "type": "array"
          }
        },
        "required": [
          "type"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "A sun: parallel light with no falloff.\n\nThe light shines down the entity's local **−Z**, taken from its\n`Transform` — the same convention the camera uses, so aiming a light is\naiming a camera. With no `Transform` the light travels toward −Z\n(horizontally); a noon sun is `\"rotation\": [-90, 0, 0]`.\n\nAt most one per scene (`multiple_directional_lights`). A scene with **no**\nlight components at all gets a documented fallback rig (sun + ambient); a\nscene with any light component gets exactly what it wrote — absent means\noff.",
        "properties": {
          "color": {
            "default": [
              1.0,
              1.0,
              1.0
            ],
            "description": "Linear RGB chromaticity, each component in `[0, 1]`. Magnitude lives\nin `intensity`.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "intensity": {
            "default": 1.0,
            "description": "Unitless multiplier, `>= 0`, unbounded above: intensity 2 is twice as\nbright, and a white light at 1.0 on a white surface head-on reads\nwhite.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "type": {
            "const": "DirectionalLight",
            "type": "string"
          }
        },
        "required": [
          "type"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "A flat, non-directional fill: `albedo * color * intensity`, added to the\nlit result. Exists because a sun-only scene renders back faces pure black,\nand a black region in a screenshot tells an agent nothing.\n\nAt most one per scene (`multiple_ambient_lights`).",
        "properties": {
          "color": {
            "default": [
              1.0,
              1.0,
              1.0
            ],
            "description": "Linear RGB, each component in `[0, 1]`.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "intensity": {
            "default": 0.05000000074505806,
            "description": "`>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "type": {
            "const": "AmbientLight",
            "type": "string"
          }
        },
        "required": [
          "type"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "A local light that shines in every direction from its entity's position,\nfalling off with distance (M17).\n\nUnlike the sun, there may be **many** per scene — up to\n[`MAX_POINT_LIGHTS`], beyond which the scene is invalid\n(`too_many_point_lights`) rather than silently missing the extras. It has no\norientation, so its `Transform.rotation` and `scale` are ignored; only\n`position` is read. Point lights do not cast shadows (the engine has one\nshadow map, and it belongs to the sun) and are not attenuated by fog.\n\nThis is the component that lets a fire light the ground around it: its\n`intensity` and `color` are in the curated script API\n(`world.light_intensity` / `set_light_intensity` / `light_color` /\n`set_light_color`), so the same flicker signal that drives a flame's\nemission rate can drive the light it casts.\n\nPresence counts as \"the scene lit itself\": a scene whose only light is a\n`PointLight` gets no fallback sun, same as with any other light component.",
        "properties": {
          "color": {
            "default": [
              1.0,
              1.0,
              1.0
            ],
            "description": "Linear RGB, each component in `[0, 1]`.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "intensity": {
            "default": 1.0,
            "description": "Brightness at one unit of distance. `>= 0`.\n\nFalloff is inverse-square, so this is the value the surface of a sphere\none metre away receives — which makes `intensity` comparable to\n`DirectionalLight.intensity` at exactly that distance and four times\ndimmer at two metres. A campfire is a few units; a candle is a fraction.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "range": {
            "default": 10.0,
            "description": "Distance in world units at which the light reaches exactly zero. `> 0`.\n\nInverse-square falloff never truly reaches zero, so a range is what\nkeeps a light local: the physical curve is multiplied by a window that\nsmoothly closes at `range`. Without it, every light in a scene would\ncontribute a little to every surface, and a lantern in one room would\nlift the black level of the next.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "type": {
            "const": "PointLight",
            "type": "string"
          }
        },
        "required": [
          "type"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "A simulated rigid body (M8). Requires a `Transform`; a **dynamic** body\nalso requires a `Collider` (`missing_collider` — it would fall forever\nthrough everything).\n\nSimulation state is derived, never authoritative: this component is the\ninitial conditions, and `engine simulate --bake` writes the evolved values\nback as ordinary scene text.",
        "properties": {
          "angular_damping": {
            "default": 0.0,
            "description": "`>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "angular_velocity": {
            "default": [
              0.0,
              0.0,
              0.0
            ],
            "description": "**Degrees per second**, `[x, y, z]` — the same units and axis order as\n`Transform.rotation`, for the same reason: the agent that writes\n`\"rotation\": [0, 45, 0]` writes `[0, 90, 0]` for a half-turn per\nsecond. Converted to rad/s only at the physics-backend boundary.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "body": {
            "$ref": "#/$defs/BodyKind"
          },
          "can_sleep": {
            "default": true,
            "description": "Allow the solver to put this body to sleep once it settles.",
            "type": "boolean"
          },
          "ccd": {
            "default": false,
            "description": "Continuous collision detection, for small fast bodies that would\notherwise tunnel.",
            "type": "boolean"
          },
          "gravity_scale": {
            "default": 1.0,
            "description": "Multiplier on scene gravity. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "linear_damping": {
            "default": 0.0,
            "description": "`>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "linear_velocity": {
            "default": [
              0.0,
              0.0,
              0.0
            ],
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "locked_rotations": {
            "default": [
              false,
              false,
              false
            ],
            "description": "Lock rotation around the `[x, y, z]` world axes. A vehicle locks\n`[true, false, true]`: yaw stays free for steering while contacts can\nno longer pitch or roll it over.",
            "items": {
              "type": "boolean"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "type": {
            "const": "RigidBody",
            "type": "string"
          }
        },
        "required": [
          "type",
          "body"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "Collision geometry (M8). Requires a `Transform`. With no `RigidBody` on\nthe entity, this is static collision geometry — the common case for\nground planes and walls.\n\nOne flat object discriminated by `shape` (the shape `jq` and an LLM\nhandle best): `cuboid` uses `half_extents`, `sphere` uses `radius`,\n`capsule` (Y-axis) uses `half_height` + `radius`; `trimesh` and\n`convex_hull` take their geometry from `asset`, or from the entity's own\n`Mesh` when `asset` is absent — the collider matches what the screenshot\nshows, by construction. Validation enforces which fields each shape\nrequires and forbids — the file format is the contract, the flat Rust\nstruct is how it stays walkable by the schema-driven validator and the\neditor's generated inspector.\n\n`Transform.scale` scales the shape when the physics world is built — a\ncube scaled 2x collides 2x big, which is what the screenshot shows.\nNonuniform scale on a round shape has no physics representation and is a\nvalidation error, never a silent approximation (mesh shapes scale\nper-vertex, so nonuniform is fine there).\n\nLayers: `layers` names the collision layers this collider belongs to,\n`collides_with` restricts which layers it interacts with. Both absent\nmeans \"collide with everything\" — exactly the pre-layer behavior. Two\ncolliders interact only if each one's `collides_with` (or absence)\nadmits a layer the other belongs to. Layer names are scene-local\nstrings; a scene may use at most 32 distinct names.",
        "properties": {
          "asset": {
            "description": "`trimesh` and `convex_hull` only: the mesh whose geometry to collide\nas (`builtin:` or a `.gltf`/`.glb` path relative to the scene file).\nAbsent, the entity's own `Mesh.asset` is used — the common case; a\nmesh shape with neither is `collider_missing_mesh`.",
            "type": [
              "string",
              "null"
            ]
          },
          "collides_with": {
            "description": "Only interact with colliders belonging to these layers. Absent =\ninteract with everything. Empty is an error — omit the field\ninstead.",
            "items": {
              "type": "string"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "density": {
            "default": 1.0,
            "description": "Mass comes from `density` x shape volume. `> 0`.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "friction": {
            "default": 0.5,
            "description": "`>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "half_extents": {
            "description": "`cuboid` only. Each component `> 0`.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": [
              "array",
              "null"
            ]
          },
          "half_height": {
            "description": "`capsule` only: half the cylindrical section's height. `> 0`.",
            "format": "float",
            "type": [
              "number",
              "null"
            ]
          },
          "layers": {
            "description": "Collision layers this collider is a member of. Absent = member of\nevery layer. Empty is an error — omit the field instead.",
            "items": {
              "type": "string"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "offset": {
            "default": [
              0.0,
              0.0,
              0.0
            ],
            "description": "Local offset of the shape from the entity's transform origin.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "radius": {
            "description": "`sphere` and `capsule`. `> 0`.",
            "format": "float",
            "type": [
              "number",
              "null"
            ]
          },
          "restitution": {
            "default": 0.0,
            "description": "Bounciness, `[0, 1]`. When two colliders touch, the **larger** of\nthe two restitutions applies (max-combine), so a bouncy ball bounces\non a plain floor exactly as its own value says.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "sensor": {
            "default": false,
            "description": "Sensors detect overlaps (trace events) but exert no forces.",
            "type": "boolean"
          },
          "shape": {
            "$ref": "#/$defs/ColliderShapeKind"
          },
          "type": {
            "const": "Collider",
            "type": "string"
          }
        },
        "required": [
          "type",
          "shape"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "Breaks into pre-authored fragments (M14) — on a hard enough collision,\ninside an explosion, or when a script calls `world.break_entity`.\n\nOn break the entity is replaced, after that step's physics, by one\ndynamic-body entity per fragment (`Parent.frag0`, `Parent.frag1`, …),\neach inheriting the parent's `Material` and motion. Fragments are\nordinary entities afterwards: they render, trace, and bake like anything\nelse.",
        "properties": {
          "dust": {
            "description": "Throw the material's burst of dust, sawdust, glitter or sparks at the\nbreak (M44). Default **on**, and **does nothing without a `material`**\n— there is no generic dust, only stone's and wood's and glass's and\nmetal's.\n\nThe burst is a `ParticleEmitter` entity named `Parent.dust`, spawned at\nthe impact point and sized to the object, which despawns itself once\nits last particle dies. It is an ordinary entity while it lives: it\ntraces, it bakes, and a script can find it by name.",
            "type": "boolean"
          },
          "fragments": {
            "description": "What the entity becomes. At least one.",
            "items": {
              "$ref": "#/$defs/Fragment"
            },
            "minItems": 1,
            "type": "array"
          },
          "impulse_threshold": {
            "description": "Contact impulse, in kg·m/s (≈ mass x closing speed), at or above\nwhich a collision breaks this entity. **Absent means collisions\nnever break it** — only scripts and explosions do. Impulse rather\nthan force so the number survives a `timestep_hz` change. `> 0`.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": [
              "number",
              "null"
            ]
          },
          "material": {
            "anyOf": [
              {
                "$ref": "#/$defs/FractureMaterial"
              },
              {
                "type": "null"
              }
            ],
            "description": "What this is made of (M43), which decides how the pieces behave once\nthey are pieces: how hard they scatter away from the impact, how much\nthey tumble, and what their surfaces are like.\n\n**Absent is M14 unchanged** — fragments take the parent's motion and\nnothing else, on `friction: 0.5, restitution: 0.0`. A material is also\nwhat `engine fracture` generates shard geometry for, but the two halves\nare independent: a wooden crate that breaks into authored boxes still\nscatters like wood."
          },
          "type": {
            "const": "Breakable",
            "type": "string"
          }
        },
        "required": [
          "type",
          "fragments"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "Plays an animation clip against scene time (M9), or against ground\ncovered (M32).\n\n`clip` is a relative path to a property clip (`*.anim.json`), or a\n`path#ClipName` glTF fragment naming a skeletal clip in the entity's own\nmesh file (M30). A player in the file is playing — there is no play/pause\nruntime state.\n\nThe clock is scene time by default, which keeps the pose a pure function\nof (files, time). Setting `stride` swaps that clock for **distance\ntravelled**, and `phase` is where the clip has got to: still a field in\nthe file, so nothing moves into hidden state.",
        "properties": {
          "clip": {
            "type": "string"
          },
          "looping": {
            "default": true,
            "description": "Wrap by clip duration; when false, clamp to the final pose.",
            "type": "boolean"
          },
          "phase": {
            "default": 0.0,
            "description": "How far this player has got, in **cycles** of its clip, when `stride`\ndrives it (M32). Ignored otherwise.\n\nCycles rather than seconds so the locomotion system needs no clip\nduration to advance it: one step covering `d` metres adds `d / stride`,\nand nothing has to open the clip file to know that. The engine writes\nit back every fixed step and the change-based bake splices it, so where\na character is in its stride survives a bake the same way where it is\nstanding does. A looping player's phase is reduced into `[0, 1)` as it\nis stored.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "speed": {
            "default": 1.0,
            "description": "Time multiplier; local time = `t * speed + start_offset`, or\n`phase * speed + start_offset` when `stride` is set.",
            "format": "float",
            "type": "number"
          },
          "start_offset": {
            "default": 0.0,
            "format": "float",
            "type": "number"
          },
          "stride": {
            "default": 0.0,
            "description": "Metres of ground one **cycle** of this clip covers (M32).\n\n`0` — the default — is the M9 behaviour: scene time drives the clip.\nAbove zero the clip is driven by the entity's horizontal displacement\ninstead, advancing `distance / stride` cycles per fixed step, which is\nwhat stops a walk cycle sliding when the character's speed changes.\n`engine list-joints` measures the right value off the clip itself.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "type": {
            "const": "AnimationPlayer",
            "type": "string"
          }
        },
        "required": [
          "type",
          "clip"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "Gameplay logic as data (M10): a Rhai script run once per fixed step.\n\n`source` is a relative `.rhai` path defining `fn step(world, step)`.\nScripts mutate the world through a small registered API and never invent\nstate of their own — baked output after a scripted run is an ordinary\nscene file.",
        "properties": {
          "source": {
            "type": "string"
          },
          "type": {
            "const": "Script",
            "type": "string"
          }
        },
        "required": [
          "type",
          "source"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "One raycast-suspension wheel of a vehicle (M12).\n\nSits on its own entity — the wheel's visual — and names its chassis in\n`vehicle` (an entity with a **dynamic** `RigidBody` + `Collider`). The\nengine groups every wheel naming the same chassis into one raycast\nvehicle: each wheel casts a ray down its suspension, a spring/damper\npushes the chassis, and a tire friction model drives and grips at the\ncontact point. The wheel entity's own `Transform` is **written by\nphysics** every step — suspension compression, steering yaw, and axle\nspin — so screenshots show wheels that steer, roll, and bounce. Author\nits rest pose; simulation owns it afterwards. A wheel entity must not\ncarry its own `RigidBody` or `Collider` (`wheel_with_physics`) — the\nchassis owns all collision.\n\nControl fields (`engine_force`, `brake`, `steering`) are runtime inputs\nthe same way `RigidBody.linear_velocity` is: scripts write them via\n`world.set_engine_force(...)` and friends, and physics reads them each\nstep.",
        "properties": {
          "brake": {
            "default": 0.0,
            "description": "Braking strength, `>= 0`. A runtime input scripts write each step\n(`world.set_brake`).",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "engine_force": {
            "default": 0.0,
            "description": "Drive force along the wheel's rolling direction, in newtons.\nPositive is the chassis's forward (−Z); negative reverses. A runtime\ninput scripts write each step (`world.set_engine_force`).",
            "format": "float",
            "type": "number"
          },
          "friction_slip": {
            "default": 10.5,
            "description": "Tire traction: how much forward/braking impulse the contact patch\ntransmits before it slips, as a multiple of the suspension load.\nLarger = grippier; too large flips the vehicle under hard braking.\n`>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "max_suspension_force": {
            "default": 30000.0,
            "description": "Hard cap on the suspension force, in newtons (not mass-scaled).\n`>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "offset": {
            "default": [
              0.0,
              0.0,
              0.0
            ],
            "description": "Suspension attachment point in the chassis's local frame, in meters\n(rotated with the chassis, **not** multiplied by its\n`Transform.scale`). The suspension ray starts here and points down\nthe chassis's local −Y.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "radius": {
            "default": 0.30000001192092896,
            "description": "Wheel radius in meters. `> 0`.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "side_friction_stiffness": {
            "default": 1.0,
            "description": "Multiplier on the tire's sideways grip. `1.0` = full lateral\nfriction; lower values let the vehicle drift. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "steering": {
            "default": 0.0,
            "description": "Steering angle in **degrees** about the chassis's up axis; positive\nsteers left. A runtime input scripts write each step\n(`world.set_steering`).",
            "format": "float",
            "type": "number"
          },
          "suspension_compression": {
            "default": 2.5,
            "description": "Damping while the spring compresses, per kilogram of chassis mass.\n`sqrt(stiffness)` is critical damping; ~0.5× that is a comfortable\ncar. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "suspension_damping": {
            "default": 3.5,
            "description": "Damping while the spring extends (rebound), per kilogram of chassis\nmass. Usually a little higher than `suspension_compression`. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "suspension_rest_length": {
            "default": 0.30000001192092896,
            "description": "Suspension spring rest length in meters: how far below `offset` the\nwheel center hangs when the spring is neither compressed nor\nstretched. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "suspension_stiffness": {
            "default": 24.0,
            "description": "Spring stiffness **per kilogram of chassis mass** (the backend\nmultiplies by mass, so the same value suspends a light or heavy\nchassis identically). Higher = stiffer. `> 0`. Static sag per wheel\nis roughly `9.81 / (4 * stiffness)` meters — keep\n`suspension_travel` above that.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "suspension_travel": {
            "default": 0.25,
            "description": "Maximum travel from rest length, in meters, both directions. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "type": {
            "const": "Wheel",
            "type": "string"
          },
          "vehicle": {
            "description": "Name of the chassis entity this wheel belongs to. Must be a\n*different* entity with a dynamic `RigidBody` and a `Collider`.",
            "type": "string"
          }
        },
        "required": [
          "type",
          "vehicle"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "A screen-space text label (M12): lines of the built-in 8×8 pixel font,\ndrawn over the 3D scene after lighting, independent of any camera.\n\nNeeds no `Transform` — placement is `anchor` + `offset` in framebuffer\npixels, which is what the agent sees in the PNG. Text is always opaque\nand never anti-aliased, so a HUD glyph is bit-exact in baselines. Glyphs\noutside the font's coverage render as a filled box: visibly wrong in the\nscreenshot, never a panic.\n\nM31 adds `parent`, `visible` and `stretch` (shared by every element in the\nfamily), plus `align`, `wrap` and `line_gap`. Every one defaults to the M12\nbehaviour: no parent means a child of the viewport placed by exactly the\nM12 anchor arithmetic, and `wrap: 0` means the single unwrapped line it has\nalways been.",
        "properties": {
          "align": {
            "$ref": "#/$defs/HudTextAlign",
            "default": "left",
            "description": "Alignment of each line within the text's own box, which differs from\nthe box only when the text is stretched or wrapped."
          },
          "anchor": {
            "$ref": "#/$defs/HudAnchor",
            "default": "top_left"
          },
          "color": {
            "default": [
              1.0,
              1.0,
              1.0
            ],
            "description": "Linear RGB in `[0, 1]`, like every color in the engine; encoded to\nsRGB when the overlay is rasterized.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "line_gap": {
            "default": 0.0,
            "description": "Extra pixels between lines, on top of the glyph cell.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "offset": {
            "default": [
              0.0,
              0.0
            ],
            "description": "Pixels inward from `anchor` (see [`HudAnchor`]). Inside a `row` or\n`column` parent this is a nudge on top of the computed position rather\nthan the position itself.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 2,
            "minItems": 2,
            "type": "array"
          },
          "parent": {
            "description": "The name of an entity carrying a [`HudPanel`] to place this inside.\nAbsent (the default) means a child of the viewport.",
            "type": [
              "string",
              "null"
            ]
          },
          "size": {
            "default": 16.0,
            "description": "Glyph height in pixels, `>= 4`. The 8×8 font renders at integer\nscale `max(1, round(size / 8))`, so `16` means exactly 2× glyphs.",
            "format": "float",
            "minimum": 4.0,
            "type": "number"
          },
          "stretch": {
            "default": [
              false,
              false
            ],
            "description": "Fill the parent's content box on `[x, y]`, ignoring this element's own\nsize on that axis. Two booleans rather than a `\"fill\"` string in a\nnumeric field, which would break the schema-driven walk.",
            "items": {
              "type": "boolean"
            },
            "maxItems": 2,
            "minItems": 2,
            "type": "array"
          },
          "text": {
            "description": "The label. `\\n` breaks a line explicitly; `wrap` breaks it\nautomatically. Scripts may rewrite it via `world.set_hud_text` — an\nempty string is a legal rest value for a script-driven readout.",
            "type": "string"
          },
          "type": {
            "const": "HudText",
            "type": "string"
          },
          "visible": {
            "default": true,
            "description": "Drawn and hit-testable when true (the default). Hiding a panel hides\nits whole subtree — one boolean is how a menu opens and closes.",
            "type": "boolean"
          },
          "wrap": {
            "default": 0.0,
            "description": "Wrap width in pixels; `0` (the default) is no wrapping. Breaks on\nspaces — a word longer than `wrap` overflows rather than splitting,\nsince a mid-word break in a fixed-width font reads as corruption.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          }
        },
        "required": [
          "type",
          "text"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "A screen-space solid rectangle (M12): the primitive behind health bars,\nspeed bars, and backdrops. Drawn with the panels and images, before all\n`HudText`, file order within the class.\n\nM31 adds only the shared `parent`/`visible`/`stretch`; a rect stays the\nflat script-driven bar it has always been.",
        "properties": {
          "anchor": {
            "$ref": "#/$defs/HudAnchor",
            "default": "top_left"
          },
          "color": {
            "default": [
              1.0,
              1.0,
              1.0
            ],
            "description": "Linear RGB in `[0, 1]`.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "offset": {
            "default": [
              0.0,
              0.0
            ],
            "description": "Pixels inward from `anchor` (see [`HudAnchor`]). Inside a `row` or\n`column` parent this is a nudge on top of the computed position.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 2,
            "minItems": 2,
            "type": "array"
          },
          "opacity": {
            "default": 1.0,
            "description": "`[0, 1]`; `1` (the default) replaces the pixel exactly, fractions\nalpha-blend on the GPU (deterministic per adapter, like every\nbaseline).",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "parent": {
            "description": "The name of an entity carrying a [`HudPanel`] to place this inside.\nAbsent (the default) means a child of the viewport.",
            "type": [
              "string",
              "null"
            ]
          },
          "size": {
            "description": "`[width, height]` in pixels, each `>= 0` — zero is legal so a\nscript-driven bar can be empty. Scripts resize via\n`world.set_hud_rect_size` or `world.set_hud_size`. Ignored on an axis\nwhere `stretch` is true.",
            "items": {
              "format": "float",
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 2,
            "minItems": 2,
            "type": "array"
          },
          "stretch": {
            "default": [
              false,
              false
            ],
            "description": "Fill the parent's content box on `[x, y]`, ignoring `size` on that\naxis — the full-screen dim backdrop, and the bar spanning a column.",
            "items": {
              "type": "boolean"
            },
            "maxItems": 2,
            "minItems": 2,
            "type": "array"
          },
          "type": {
            "const": "HudRect",
            "type": "string"
          },
          "visible": {
            "default": true,
            "description": "Drawn and hit-testable when true (the default).",
            "type": "boolean"
          }
        },
        "required": [
          "type",
          "size"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "A screen-space container that lays its children out (M31).\n\nThis is the component that removes hand-computed pixel offsets. Children\nname it in their `parent`; `layout` decides whether they are stacked in a\n`row`, a `column`, or placed `free` by their own anchors relative to this\npanel's content box.\n\n**Absent `width`/`height` means hug contents** — the panel is exactly its\nchildren's extent plus `padding`. That is the default because it is the\ncase that makes a dialog authorable: the box follows the text instead of\nthe text being fitted to a box someone solved by hand.\n\n`opacity` defaults to **0**, so a bare `HudPanel` is an invisible layout\ngroup; set it and the same component is the dialog's backdrop. One\ncomponent rather than a container plus a rect whose size would have to be\nkept in agreement with it.",
        "properties": {
          "align": {
            "$ref": "#/$defs/HudAlign",
            "default": "start",
            "description": "Cross-axis alignment of children in a `row`/`column`."
          },
          "anchor": {
            "$ref": "#/$defs/HudAnchor",
            "default": "top_left"
          },
          "color": {
            "default": [
              1.0,
              1.0,
              1.0
            ],
            "description": "Background colour, linear RGB in `[0, 1]`. Only visible at\n`opacity > 0`.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "gap": {
            "default": 0.0,
            "description": "Pixels between children along the main axis of a `row`/`column`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "height": {
            "description": "Fixed height in pixels. Absent hugs the children.",
            "format": "float",
            "minimum": 0.0,
            "type": [
              "number",
              "null"
            ]
          },
          "layout": {
            "$ref": "#/$defs/HudLayout",
            "default": "free"
          },
          "offset": {
            "default": [
              0.0,
              0.0
            ],
            "description": "Pixels inward from `anchor` (see [`HudAnchor`]).",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 2,
            "minItems": 2,
            "type": "array"
          },
          "opacity": {
            "default": 0.0,
            "description": "`[0, 1]`, defaulting to **0** — an invisible layout group.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "padding": {
            "default": 0.0,
            "description": "Uniform inset in pixels between this panel's edge and its content box.\nPer-side padding is the obvious next field and costs nothing to add\nlater; M12's \"no z field until something needs it\" applies.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "parent": {
            "description": "The name of another [`HudPanel`] entity to nest inside.",
            "type": [
              "string",
              "null"
            ]
          },
          "stretch": {
            "default": [
              false,
              false
            ],
            "description": "Fill the parent's content box on `[x, y]`, ignoring `width`/`height`\nand hug sizing on that axis.",
            "items": {
              "type": "boolean"
            },
            "maxItems": 2,
            "minItems": 2,
            "type": "array"
          },
          "type": {
            "const": "HudPanel",
            "type": "string"
          },
          "visible": {
            "default": true,
            "description": "Drawn and hit-testable when true (the default). A hidden panel hides\nits whole subtree.",
            "type": "boolean"
          },
          "width": {
            "description": "Fixed width in pixels. Absent hugs the children.",
            "format": "float",
            "minimum": 0.0,
            "type": [
              "number",
              "null"
            ]
          }
        },
        "required": [
          "type"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "A screen-space textured rectangle (M31): icons, logos, framed panels.\n\n`texture` is a PNG relative to the scene file, loaded through the same\n`TextureSource` and `(asset, space)` cache the material system uses, in\nsRGB — so `texture_too_large` fires from `validate`, before a device\nexists. Only the base level is read: the overlay draws at most one\ndestination pixel per texel band and never minifies below it, so a mip\nlevel selection would have no correct answer at this scale.\n\nSampling is **nearest-neighbour**, written out in `engine-core` like every\nother generator here — a render sits under a baseline, so the filter is a\nformat contract, and nearest is exactly reproducible where a bilinear\nfilter is a float-rounding question.",
        "properties": {
          "anchor": {
            "$ref": "#/$defs/HudAnchor",
            "default": "top_left"
          },
          "offset": {
            "default": [
              0.0,
              0.0
            ],
            "description": "Pixels inward from `anchor` (see [`HudAnchor`]).",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 2,
            "minItems": 2,
            "type": "array"
          },
          "opacity": {
            "default": 1.0,
            "description": "`[0, 1]`, multiplied onto the texture's own alpha.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "parent": {
            "description": "The name of an entity carrying a [`HudPanel`] to place this inside.",
            "type": [
              "string",
              "null"
            ]
          },
          "size": {
            "description": "`[width, height]` in destination pixels. Ignored on an axis where\n`stretch` is true.",
            "items": {
              "format": "float",
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 2,
            "minItems": 2,
            "type": "array"
          },
          "slice": {
            "default": [
              0.0,
              0.0,
              0.0,
              0.0
            ],
            "description": "Nine-slice insets in **source** pixels, `[left, top, right, bottom]`.\nThe default `[0, 0, 0, 0]` is a plain stretch. Corners are copied\n1:1, edges tile along their axis and the centre tiles both ways —\ntiling rather than stretching, because tiling at nearest is exact\nwhere stretching at nearest is a moiré pattern.",
            "items": {
              "format": "float",
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 4,
            "minItems": 4,
            "type": "array"
          },
          "stretch": {
            "default": [
              false,
              false
            ],
            "description": "Fill the parent's content box on `[x, y]`, ignoring `size`.",
            "items": {
              "type": "boolean"
            },
            "maxItems": 2,
            "minItems": 2,
            "type": "array"
          },
          "texture": {
            "description": "A `.png` path relative to the scene file (invariant 3).",
            "type": "string"
          },
          "tint": {
            "default": [
              1.0,
              1.0,
              1.0
            ],
            "description": "Multiplies the decoded texel in linear space, so one grey frame\ntexture serves a red panel and a blue one — the material system's\nauthoring rule for `albedo_map`, here for the same reason.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "type": {
            "const": "HudImage",
            "type": "string"
          },
          "visible": {
            "default": true,
            "description": "Drawn and hit-testable when true (the default).",
            "type": "boolean"
          }
        },
        "required": [
          "type",
          "texture",
          "size"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "Makes the HUD element on its own entity clickable (M31).\n\nCarries no geometry: the hit box is that element's laid-out rectangle. An\nentity with a `HudInteract` and no `HudPanel`/`HudRect`/`HudImage`/\n`HudText` is `hud_interact_without_element`.\n\nA separate component rather than an `interactive: true` flag on each of\nfour components, because the flag would be four fields that must stay in\nagreement, and because the tints belong next to it.\n\nThe tints multiply the element's own colour (clamped to `[0, 1]` after\nmultiplying) and default to `[1, 1, 1]` — no change — so adding a\n`HudInteract` moves no pixel until a cursor arrives. They exist so the\nordinary case, a button that lights up under the pointer, needs no script\nat all; anything richer is a script writing colours.",
        "properties": {
          "disabled": {
            "default": false,
            "description": "Excluded from hit-testing when true, so it never hovers, presses or\nclicks — and never blocks what is under it either.",
            "type": "boolean"
          },
          "hover_tint": {
            "default": [
              1.0,
              1.0,
              1.0
            ],
            "description": "Colour multiplier while the cursor is over this element. Unbounded\nabove — a hover tint brightens.",
            "items": {
              "format": "float",
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "press_tint": {
            "default": [
              1.0,
              1.0,
              1.0
            ],
            "description": "Colour multiplier while a button is held down on this element.",
            "items": {
              "format": "float",
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "type": {
            "const": "HudInteract",
            "type": "string"
          }
        },
        "required": [
          "type"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "A deterministic particle emitter (M13): smoke, sparks, dust — and, with the\nM17 fields, fire.\n\nParticles spray from the entity's position into a cone around its local\n**−Z** — the same aiming convention the camera and lights use, so a rising\nsmoke plume is `\"rotation\": [90, 0, 0]`. Each particle is born with\n`start_*` values and reaches its `end_*` values as it dies; the billboard\nrenders as a soft unlit disc, alpha-blended over the scene.\n\nParticles are simulation state on the fixed step clock, advanced by\n`--steps` exactly like physics (`--time` poses animations, it does not\nadvance particles). The state is derived and disposable — never baked,\nnever traced — and the `seed` makes it reproducible: same file, same\nsteps, same particles, so screenshots of smoke diff-render bit-exactly.\n\n# The fire fields (M17)\n\nA cone of identical sprites reads as a sparkler, not a flame. Five fields\nfix that, and **every one of them defaults to the M13 behaviour** — a\npre-M17 emitter is byte-identical, down to which random numbers it draws:\n`radius` spreads emission over a fire bed, the three `*_jitter` fields\nbreak the lockstep of a population born identical, `turbulence` makes\nflames lick, `stretch` turns round puffs into motion-aligned tongues and\nember streaks, and `blend: \"additive\"` makes overlapping flame get\n*brighter* instead of merely more opaque, which is the single biggest\ndifference between fire and orange smoke.\n\n**Random draws are skipped, not defaulted, when a field is off.** Every\njitter field consumes the emitter's RNG only when it is non-zero, in the\ndocumented order (direction → disc → speed → size → lifetime →\nturbulence). That is what keeps every baseline blessed before M17 exact,\nand it is the same discipline as not consuming the RNG on a capped spawn.",
        "properties": {
          "acceleration": {
            "default": [
              0.0,
              0.0,
              0.0
            ],
            "description": "World-space acceleration applied to every live particle, units/s².\nParticles ignore physics; this is where buoyancy (`[0, 1, 0]` for\nsmoke) or gravity (`[0, -9.81, 0]` for sparks) comes from.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "blend": {
            "$ref": "#/$defs/ParticleBlend",
            "default": "alpha",
            "description": "How the billboard combines with the scene behind it. `\"alpha\"` (default)\noccludes what it covers; `\"additive\"` adds light to it, so overlapping\nsprites brighten toward white and a thin one is nearly invisible.\n\nFire, sparks, and magic are additive; smoke, dust, and spray are not.\nAdditive particles draw after every alpha-blended one, so a flame glows\n*through* the smoke above it rather than being occluded by it — the\napproximation is deliberate and it is what a real fire looks like,\nsince that smoke is genuinely lit from below."
          },
          "despawn_when_done": {
            "default": false,
            "description": "Despawn this emitter's entity once its `duration` is up **and** its\nlast particle has died (M44). Default `false`.\n\nThis is what keeps a scene that breaks twenty crates from accumulating\ntwenty spent emitters. Requires `duration` — without one the emitter\nnever finishes and the flag could never fire, which is\n`emitter_never_finishes` rather than a field that quietly does nothing.\n\nThe despawn is an ordinary one: it traces as `despawned` like any\nother, and the entity is simply gone from a bake taken afterwards.",
            "type": "boolean"
          },
          "drag": {
            "default": 0.0,
            "description": "Velocity damping per second. `>= 0`; 0 means none.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "duration": {
            "description": "Seconds of **emission** after this emitter's first step (M44). `> 0`.\n\n**Absent is M13: it emits forever.** Present, it is a burst — the puff\nof dust a break throws, the spark shower off an impact — and when the\ntime is up emission stops exactly as `rate: 0` does: no new particles,\nand the live ones finish their own `lifetime`.\n\nIt is measured from when the *system* first sees the emitter, not from\nscene time, so an emitter that arrives mid-run (a break's dust) burns\nits burst from the moment it exists rather than being born already\nspent. That start is derived state, like particle positions: it is not\nbaked, so a scene baked mid-puff reloads and puffs again — the same\nthing the tour's fire already does, for the same reason.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": [
              "number",
              "null"
            ]
          },
          "end_alpha": {
            "default": 0.0,
            "description": "Opacity at death, `[0, 1]`. The default 0 fades particles out.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "end_color": {
            "default": [
              0.699999988079071,
              0.699999988079071,
              0.699999988079071
            ],
            "description": "Linear RGB at death, each component in `[0, 1]`.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "end_size": {
            "default": 0.20000000298023224,
            "description": "Billboard half-size at death. `> 0`; larger than `start_size` grows\n(smoke), smaller shrinks (sparks).",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "lifetime": {
            "default": 2.0,
            "description": "Seconds each particle lives. `> 0`.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "lifetime_jitter": {
            "default": 0.0,
            "description": "Fraction by which each particle's lifespan varies from `lifetime`,\n`[0, 1)`. The most valuable of the three for fire: identical lifespans\nput the whole population's fade at one height, which draws a flat top\non the flame, and varying them is what makes it ragged.\n\nA particle's lifespan is fixed **at birth**, so animating `lifetime`\nmid-run retimes new particles and leaves live ones alone.",
            "format": "float",
            "maximum": 0.99,
            "minimum": 0.0,
            "type": "number"
          },
          "max_particles": {
            "default": 1024,
            "description": "Cap on live particles; spawns beyond it are dropped (deterministically).\n`[1, 65536]`.",
            "format": "uint32",
            "maximum": 65536,
            "minimum": 1,
            "type": "integer"
          },
          "radius": {
            "default": 0.0,
            "description": "Radius of the disc particles are born on, in world units, centred on the\nentity and lying in the plane **perpendicular to the aim** (local XY).\n`>= 0`; 0 (default) emits from the single point.\n\nA campfire is a bed of coals, not a nozzle: emitting a flame from one\npoint gives a cone with a visible apex, and no amount of `spread` hides\nit. Like `Wheel.offset`, this is rotated by the entity's rotation but\n**not** scaled by its `Transform.scale`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "rate": {
            "default": 10.0,
            "description": "Particles spawned per second. `>= 0`; 0 stops emission (existing\nparticles live out their lifetime).",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "seed": {
            "default": 0,
            "description": "Seed for the emitter's random spray directions. Same seed, same steps →\nidentical particles; give two otherwise-identical emitters different\nseeds so they don't emit in lockstep.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "size_jitter": {
            "default": 0.0,
            "description": "Fraction by which each particle's size varies from `start_size` /\n`end_size`, `[0, 1]`. One multiplier scales both, so a particle keeps\nits authored growth curve and only its scale changes.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "speed": {
            "default": 1.0,
            "description": "Initial speed in units/second along the sampled cone direction. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "speed_jitter": {
            "default": 0.0,
            "description": "Fraction by which each particle's launch speed varies from `speed`,\n`[0, 1]`: 0.3 means every particle draws uniformly from ±30%.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "spread": {
            "default": 15.0,
            "description": "Cone half-angle in degrees around local −Z. `[0, 180]`: 0 is a beam,\n90 a hemisphere, 180 a full sphere.",
            "format": "float",
            "maximum": 180.0,
            "minimum": 0.0,
            "type": "number"
          },
          "start_alpha": {
            "default": 1.0,
            "description": "Opacity at birth, `[0, 1]`.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "start_color": {
            "default": [
              0.699999988079071,
              0.699999988079071,
              0.699999988079071
            ],
            "description": "Linear RGB at birth, each component in `[0, 1]`. Unlit.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "start_size": {
            "default": 0.20000000298023224,
            "description": "Billboard half-size in world units at birth. `> 0`.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "stretch": {
            "default": 0.0,
            "description": "Seconds of travel to stretch the billboard along its own velocity.\n`>= 0`; 0 (default) keeps sprites round.\n\nThe sprite grows along its direction of motion by the distance the\nparticle covers in this many seconds, so the same value stretches a\nfast ember into a streak and leaves slow smoke nearly circular — which\nis what a camera does. Flame tongues want a little (~0.05), sparks want\na lot (~0.2).",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "turbulence": {
            "default": 0.0,
            "description": "Strength of a swirling world-space acceleration field, units/s².\n`>= 0`; 0 (default) means none.\n\nThis is what separates a flame from a jet of orange dots: hot gas is\nunstable and curls. The field is smooth value noise sampled along each\nparticle's own path (see `turbulence_scale`), so a particle wanders\ncoherently rather than vibrating — and it is generated by an integer\nhash specified in this repo, so it is exactly as reproducible as the\nspray directions.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "turbulence_scale": {
            "default": 1.0,
            "description": "Size in world units of one cell of the turbulence field. `> 0`.\n\nSmall values curl the flame tightly (candle), large ones sway the whole\nplume (bonfire in wind). Roughly: a particle's direction changes over\nthis distance travelled.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "type": {
            "const": "ParticleEmitter",
            "type": "string"
          }
        },
        "required": [
          "type"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "A procedurally generated tree (M19): trunk, recursive branches, and leaves,\ngrown from a `seed` into geometry the renderer draws like any other mesh.\n\nThe tree is built in entity-local space with its **base at the origin,\ngrowing along +Y**, so `\"position\": [x, 0, z]` plants it on flat ground and\n`Transform.scale` sizes the whole thing. It replaces the entity's `Mesh`\nrather than accompanying one (`tree_with_mesh`): the component *is* the\ngeometry.\n\n# Two draws, two materials\n\nA tree needs bark and foliage, and one `Material` cannot be both. The\nentity's own `Material` is the **bark**; the leaves get [`Tree::leaf_color`]\nand [`Tree::leaf_roughness`], and are always opaque and non-metallic. That\nlast part is a constraint, not an oversight: every leaf of one tree is a\nsingle mesh, and the blended pass sorts whole draws, so translucent leaves\ncould not be sorted against each other and would visibly z-fight.\n\n# Randomness\n\nEverything jittered is drawn from a private xorshift seeded by `seed`,\nconsumed in a fixed order (a branch draws its own wander, then recurses into\nits children in index order, then scatters its leaves), so two trees that\ndiffer only in `seed` are different trees and the *same* tree is the same\nmesh forever — which is what lets a forest sit under a pinned baseline.\n`jitter` is the master dial for how much variation there is at all; `0`\ngrows a rigid diagram of a tree.\n\n# Cost\n\nVertices scale as `(branches · whorl)^levels · segments · sides`, and a\ntree that would blow past the budget is a validation error\n(`tree_too_complex`) rather than a hang. `levels: 2` with the default\nbranching is ~1k vertices; `levels: 3` is ~6k. Generation happens once per\ndistinct parameter set and is cached, so a forest of nine trees is nine\nmeshes no matter how many frames render.",
        "properties": {
          "branch_angle": {
            "default": 48.0,
            "description": "Angle in degrees between a child branch and its parent at the\nattachment. Small angles sweep up (poplar), large ones reach out\n(oak) or hang down (spruce, with negative `tropism`). `[0, 180]`.",
            "format": "float",
            "maximum": 180.0,
            "minimum": 0.0,
            "type": "number"
          },
          "branch_start": {
            "default": 0.3499999940395355,
            "description": "Fraction of a branch's length that carries no children — the bare\ntrunk under the crown. `[0, 1)`.",
            "format": "float",
            "maximum": 0.99,
            "minimum": 0.0,
            "type": "number"
          },
          "branch_twist": {
            "default": 137.5,
            "description": "Degrees of rotation around the parent between successive attachment\npoints. The default is the golden angle, which is what real phyllotaxis\nconverges on and what stops branches from stacking into visible rows.",
            "format": "float",
            "type": "number"
          },
          "branches": {
            "default": 5,
            "description": "Attachment points spaced along each parent branch. `[0, 16]`.",
            "format": "uint32",
            "maximum": 16,
            "minimum": 0,
            "type": "integer"
          },
          "crook": {
            "default": 8.0,
            "description": "Degrees of random wander per meter of branch. This is the gnarl: `0`\ngrows perfectly straight poles, `8` is a healthy tree, `25` is an old\nolive. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "flare": {
            "default": 0.4000000059604645,
            "description": "Extra radius at the very foot of the trunk, as a fraction: `0.4` makes\nthe base 40% wider than the trunk above it. Root flare is most of what\nsays \"grown\" rather than \"placed\", and it is at eye level. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "height": {
            "default": 6.0,
            "description": "Trunk length in meters, before `Transform.scale`. `> 0`.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "jitter": {
            "default": 0.25,
            "description": "How much every jittered quantity — branch length, radius, angle, leaf\nplacement — varies, as a fraction. `[0, 1)`; `0` is a diagram.",
            "format": "float",
            "maximum": 0.99,
            "minimum": 0.0,
            "type": "number"
          },
          "leaf": {
            "$ref": "#/$defs/TreeLeaf",
            "default": "blade",
            "description": "What hangs on the outermost branches: `\"blade\"` (a folded leaf, the\ndefault), `\"cluster\"` (a foliage blob — cheaper per unit of cover, and\nwhat conifer sprays and distant trees want), or `\"none\"` for a bare\nwinter or dead tree."
          },
          "leaf_color": {
            "default": [
              0.09000000357627869,
              0.25999999046325684,
              0.07999999821186066
            ],
            "description": "Foliage albedo, linear RGB in `[0, 1]` — the leaves' half of the tree's\nappearance, the entity's `Material` being the bark's.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "leaf_roughness": {
            "default": 0.75,
            "description": "Foliage roughness, `[0, 1]`. Leaves are waxy, not matte: a little\nspecular is what separates them from felt.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "leaf_size": {
            "default": 0.30000001192092896,
            "description": "Length of one leaf, or diameter of one cluster, in meters. `> 0`.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "leaves_per_branch": {
            "default": 6,
            "description": "Leaves scattered along each outermost branch. `[0, 64]`; `0` is\nequivalent to `\"leaf\": \"none\"`.",
            "format": "uint32",
            "maximum": 64,
            "minimum": 0,
            "type": "integer"
          },
          "length_falloff": {
            "default": 0.3499999940395355,
            "description": "How much shorter children get toward the parent's tip, `[0, 1]`: `0`\nmakes every child the same length (a round crown), `0.8` makes the top\nones stubs (the cone of a conifer).",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "length_ratio": {
            "default": 0.6200000047683716,
            "description": "Child length as a fraction of its parent's. `(0, 2]`.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "maximum": 2.0,
            "type": "number"
          },
          "levels": {
            "default": 2,
            "description": "How many generations of branches hang off the trunk. `0` is a bare\npole; `2` reads as a tree; `3` is a good hero tree; `4` is expensive.\n`[0, 4]`.",
            "format": "uint32",
            "maximum": 4,
            "minimum": 0,
            "type": "integer"
          },
          "radius_ratio": {
            "default": 0.6000000238418579,
            "description": "Child radius as a fraction of the parent's radius where it attaches.\n`(0, 1]`.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "maximum": 1.0,
            "type": "number"
          },
          "seed": {
            "default": 0,
            "description": "Seeds every random draw. Two trees with the same parameters and\ndifferent seeds are different trees; the same seed always regrows the\nsame tree.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "segments": {
            "default": 5,
            "description": "Lengthwise segments per branch — how finely a branch can curve. `[1, 16]`.",
            "format": "uint32",
            "maximum": 16,
            "minimum": 1,
            "type": "integer"
          },
          "sides": {
            "default": 6,
            "description": "Radial sides of a branch tube. `[3, 16]`; `5`–`6` is plenty, since bark\nsilhouettes read from the branching, not the cross-section.",
            "format": "uint32",
            "maximum": 16,
            "minimum": 3,
            "type": "integer"
          },
          "taper": {
            "default": 0.11999999731779099,
            "description": "Radius at a branch's tip as a fraction of its base. `[0, 1]`; small\nvalues give the sharp taper of a young shoot.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "tropism": {
            "default": 8.0,
            "description": "Degrees per meter that a branch curves toward the sky as it grows.\nPositive lifts tips toward the light (most trees), negative lets\ngravity droop them (spruce, willow). This is what makes branches curve\nrather than point.\n\nIt does not apply to the trunk, whose line is [`Tree::crook`] alone: a\nnegative tropism on the trunk is unstable — a degree of crook tips it\noff vertical and the bend then compounds until the tree grows sideways.",
            "format": "float",
            "type": "number"
          },
          "trunk_radius": {
            "default": 0.1599999964237213,
            "description": "Trunk radius at the ground, in meters, before `Transform.scale`. `> 0`.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "type": {
            "const": "Tree",
            "type": "string"
          },
          "whorl": {
            "default": 1,
            "description": "Branches emitted at each attachment point **on the trunk**, spread\nevenly around it. `1` (default) gives the alternate, spiralling\narrangement of most broadleaf trees; `4`–`5` gives the whorls of a\nconifer, where a ring of limbs leaves the trunk at one height.\n\nDeeper levels are always alternate, which is both what a real conifer's\nlimbs carry and what keeps the geometry finite — a whorl compounding at\nevery level multiplies the tree by itself. `[1, 8]`.",
            "format": "uint32",
            "maximum": 8,
            "minimum": 1,
            "type": "integer"
          }
        },
        "required": [
          "type"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "A body of water: an ocean, a lake, a pond, a canal.\n\nThe entity owns its own surface geometry — a tessellated unit grid sized by\n`Transform.scale`, exactly like a scaled `builtin:plane` — so a `Water`\nentity carries **no** `Mesh` and no `Material` (`water_with_mesh`). One\nsurface is one entity: sixteen tiles pretending to be a pond is what this\ncomponent exists to delete, and their seams are visible in any screenshot.\n\nWaves are evaluated in **world space** in the vertex stage, which has two\nconsequences worth knowing: scaling a surface never stretches its waves, and\ntwo adjacent water entities at the same height share one continuous surface\nfor free.\n\nShading is water-specific rather than a `Material`: sky reflection with a\nFresnel-weighted view term, absorption of what is behind the surface with\ndepth (`shallow_color` → `deep_color`), and foam where the water meets\ngeometry or folds at a crest. What it does *not* do is refract — the bed of\na pond is not displaced by the ripples above it.",
        "properties": {
          "crest_foam": {
            "default": 0.0,
            "description": "Foam on the crests, `[0, 1]`; 0 (the default) is off.\n\nDriven by the Gerstner Jacobian — where the surface pinches toward\nfolding, which is exactly where a real wave breaks — so it appears only\non steep waves and needs no second noise field to place it.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "deep_color": {
            "default": [
              0.009999999776482582,
              0.05000000074505806,
              0.07999999821186066
            ],
            "description": "Linear RGB of water far deeper than `depth_fade`. Each component\n`[0, 1]`.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "density": {
            "default": 1000.0,
            "description": "Density of the fluid in kg/m³, `> 0`. Fresh water is 1000 (the default),\nsea water about 1025.\n\nThe **only** field here that nothing renders. It is what a [`Buoyancy`]\nbody weighs the water it displaces against, and it lives on the lake\nrather than on the boat because it is a property of the fluid: two hulls\nin one pond disagreeing about how dense the water is would not be a knob,\nit would be a bug. The authoring knob for \"this floats higher\" already\nexists and is [`Collider::density`], in the same unit.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "depth_fade": {
            "default": 2.5,
            "description": "Metres of water the view has to pass through to reach `deep_color` and\nfull `opacity`. `> 0`.\n\nBeer-Lambert absorption against the depth of whatever is behind the\nsurface, so the same water is clear at the edge of a pond and opaque in\nthe middle — which is most of how a surface reads as *deep* rather than\nas a coloured pane. A clear alpine lake is 6 or more; a silty pond is\nunder 1.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "detail": {
            "default": 0.5,
            "description": "Strength of the per-pixel ripple normals, `[0, 1]`.\n\nSmall-scale roughness the grid is far too coarse to carry as geometry,\nperturbing the normal and nothing else. Per line of code this is the\nbiggest single difference between \"blue glass\" and \"water\", because it\nis what breaks the sun and the sky into glitter between the vertices.\nNothing physical may depend on it — no buoyancy, no collision.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "detail_scale": {
            "default": 0.6000000238418579,
            "description": "Size of one ripple cell in metres. `> 0`. Around 0.5 reads as wind\ntexture on a lake; 3 or more as a swell the grid did not resolve.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "foam_color": {
            "default": [
              0.8600000143051147,
              0.8999999761581421,
              0.9200000166893005
            ],
            "description": "Linear RGB of both foam kinds, each component `[0, 1]`. Foam is\nscattered light, so it is opaque where it appears.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "ior": {
            "default": 1.0,
            "description": "Index of refraction, `1.0` (the default) being no bending at all — the\npre-M27 surface, which absorbs and tints what is behind it but never\nmoves it. Range `[1, 3]`; water is 1.33.\n\nUnlike [`Material::ior`] this needs no companion `thickness`: the shader\nalready measures how far the view ray travels through the body to reach\nthe bed, so the bend scales with the water's own depth and a pond bends\nits bed most where it is deepest. It also cannot change how *deep* the\nwater looks — absorption stays [`Water::depth_fade`]'s job — so it can\nbe turned on in a tuned scene without re-tuning it.",
            "format": "float",
            "maximum": 3.0,
            "minimum": 1.0,
            "type": "number"
          },
          "opacity": {
            "default": 0.9399999976158142,
            "description": "How opaque deep water becomes, `[0, 1]`. 1 hides its bed completely\nhowever clear the shallows are.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "roughness": {
            "default": 0.05999999865889549,
            "description": "Surface roughness, `[0, 1]`, meaning what `Material.roughness` means.\n\nWater is smooth, but not 0: a mirror-tight sun highlight is a single\nblown-out pixel that aliases as the camera moves, and the reflected sky\ncarries most of the look anyway.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "segments": {
            "default": 64,
            "description": "Quads per axis across the surface. `[1, 512]`.\n\nThis is the resolution the *waves* are drawn at, and it is the one field\nworth thinking about: a wave needs roughly eight quads per wavelength to\nlook like a wave rather than a fold, so a 14 m pond carrying 2 m chop\nwants ~64, and a 200 m ocean carrying 3 m chop cannot be drawn by any\ngrid this component will generate. Detail *normals* are per pixel and\ncost nothing, which is why the glitter survives a coarse grid even\nthough the silhouette does not.",
            "format": "uint32",
            "maximum": 512,
            "minimum": 1,
            "type": "integer"
          },
          "shallow_color": {
            "default": [
              0.09000000357627869,
              0.20000000298023224,
              0.20999999344348907
            ],
            "description": "Linear RGB of water one `depth_fade` deep or less — the colour at the\nshoreline. Each component `[0, 1]`.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "shore_foam": {
            "default": 0.0,
            "description": "Width in metres of the foam line where the surface meets geometry.\n`>= 0`; 0 (the default) is off.\n\nThis is the shoreline, and it is also the waterline on anything standing\nin the water: it comes from the depth behind the surface, so a boat, an\nice block and the bank all get one without being marked up.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "type": {
            "const": "Water",
            "type": "string"
          },
          "waves": {
            "default": [],
            "description": "The waves summed to shape the surface, at most\n[`MAX_WAVES`](crate::water::MAX_WAVES) of them. Empty (the default)\nleaves the surface flat: a mirror, which is what a sheltered pond at\ndawn actually looks like.",
            "items": {
              "$ref": "#/$defs/Wave"
            },
            "maxItems": 8,
            "type": "array"
          }
        },
        "required": [
          "type"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "A cloud: a cumulus, a raft of stratocumulus, a storm anvil, a torn wisp.\n\nA recipe rather than a mesh reference, like [`Tree`] — the engine grows it\ninto a cluster of lobes, each of which grows smaller lobes on itself, seeded\nso two clouds with the same parameters and different seeds are different\nclouds. The entity owns that geometry, sized by `Transform.scale` like a\nwater surface, so a `Cloud` entity carries **no** `Mesh` and **no**\n`Material` (`cloud_with_mesh`). A cloud is not a GGX surface: what a\n`Material` describes, `color` / `shade_color` / `density` / `feather`\ndescribe instead.\n\nNon-uniform scale is the normal case, not an edge case — `scale: [24, 12,\n24]` is what makes a cumulus wider than it is tall, and it oblates the lobes\nwith it.\n\nShading is three cheap stand-ins for multiple scattering, none of which is\nvolumetric: wrapped diffuse between `shade_color` and `color`, a forward-\nscattering silver lining when the camera looks toward the sun, and an alpha\nthat fades toward each lobe's own silhouette. Clouds do not cast shadows\n(the engine has one shadow cascade and it is fitted to the camera, not to a\ncloud at altitude) and are not lit by `PointLight`s.",
        "properties": {
          "children": {
            "default": 3,
            "description": "Lobes grown on each lobe of the previous generation. `[0, 8]`.",
            "format": "uint32",
            "maximum": 8,
            "minimum": 0,
            "type": "integer"
          },
          "color": {
            "default": [
              1.0,
              0.9800000190734863,
              0.949999988079071
            ],
            "description": "Linear RGB of the sunlit side. Each component `[0, 1]`.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "density": {
            "default": 0.8999999761581421,
            "description": "How opaque the cloud is where it is thickest, `[0, 1]`. Lobes do not\nwrite depth, so overlapping ones accumulate — which is a cheap stand-in\nfor optical depth, and why a wisp wants a much lower value than a storm.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "detail": {
            "default": 2,
            "description": "Icosphere subdivisions per lobe: 12, 42, 162 or 642 vertices. `[0, 3]`.\nThis is the quality dial, and `2` is plenty for anything at cloud\ndistance.",
            "format": "uint32",
            "maximum": 3,
            "minimum": 0,
            "type": "integer"
          },
          "drift": {
            "default": [
              0.0,
              0.0,
              0.0
            ],
            "description": "World-space metres per second the cloud travels, evaluated against the\nscene clock (`--time`, else `steps / timestep_hz`) — so a drifting sky\nis as reproducible as a wave, and a script is not needed to move it.\n\nThe cloud's *shape* never changes with time. Regenerating lobes per\nframe would mint a new mesh every frame and defeat the renderer's upload\ncache; in this engine, generated geometry is made once.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "drift_wrap": {
            "default": 0.0,
            "description": "Metres after which a drifting cloud recycles to where it started. `>= 0`;\n`0` (the default) lets it drift away for good.\n\nWrapping *teleports* the cloud, so it wants to be wider than the view or\nfar enough out that fog has already eaten it before it jumps.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "feather": {
            "default": 3.0,
            "description": "How crisp the cloud's edges are, `[0, 8]`. Alpha follows\n`1 - (1 - facing)^feather` as the surface turns away from the camera, so\n**higher is crisper** and low values are wispy: 1 fades the whole\nsurface proportionally, 3 keeps the body opaque and thins only the last\nfew degrees before the silhouette.\n\nIt is doing two jobs. A real cloud's silhouette is where it thins out,\nnot where its geometry stops — and the same fade is what hides the\nboundaries *between* two interpenetrating lobes, since each of them\nvanishes exactly where its surface turns away.",
            "format": "float",
            "maximum": 8.0,
            "minimum": 0.0,
            "type": "number"
          },
          "flatten": {
            "default": 0.0,
            "description": "How much the cloud sits on a flat base, `[0, 1]`. `0` is a puffball with\nlobes scattered through its whole box; `1` seats every lobe on the base\nplane and folds what hangs below onto it.\n\nA cumulus has a flat bottom because condensation begins at one altitude,\nwhich is why every fair-weather cloud in a field shares a base — it is\nthe cheapest of this component's cues and one of the most legible.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "jitter": {
            "default": 0.30000001192092896,
            "description": "How much every jittered quantity — lobe radius, placement, child size —\nvaries, as a fraction. `[0, 1)`; `0` is a diagram.",
            "format": "float",
            "maximum": 0.99,
            "minimum": 0.0,
            "type": "number"
          },
          "levels": {
            "default": 2,
            "description": "Generations of smaller lobes piled on the base ones. `[0, 3]`: `0` is a\ncluster of plain spheres, `2` reads as cauliflower, `3` is expensive.",
            "format": "uint32",
            "maximum": 3,
            "minimum": 0,
            "type": "integer"
          },
          "lobe_ratio": {
            "default": 0.550000011920929,
            "description": "Child lobe radius as a fraction of its parent's, `(0, 1]`. This is the\ndial that makes the silhouette detailed at more than one scale — at 1\nevery lobe is the same size and the cloud reads as popcorn.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "maximum": 1.0,
            "type": "number"
          },
          "lobe_size": {
            "default": 0.41999998688697815,
            "description": "Diameter of a base lobe as a fraction of the cloud's own size, `(0, 1]`.\nLarge values give a few fat billows, small ones a curdled texture.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "maximum": 1.0,
            "type": "number"
          },
          "lobes": {
            "default": 6,
            "description": "Lobes in the base cluster, spread over the footprint on a golden-angle\nspiral. `[1, 32]`; a handful reads as one cumulus, a dozen or more as a\nraft.",
            "format": "uint32",
            "maximum": 32,
            "minimum": 1,
            "type": "integer"
          },
          "rise": {
            "default": 0.3499999940395355,
            "description": "How strongly child lobes are biased toward the sky, `[0, 1]`. A cumulus\nis a convection cell: its detail is on top, where the air is still\nrising, and its underside is smooth. At `0` children scatter in every\ndirection and the cloud reads as a sea urchin.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "seed": {
            "default": 0,
            "description": "Seeds every random draw. Two clouds with the same parameters and\ndifferent seeds are different clouds; the same seed always regrows the\nsame cloud.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "shade_color": {
            "default": [
              0.41999998688697815,
              0.46000000834465027,
              0.5799999833106995
            ],
            "description": "Linear RGB of the self-shadowed side, each component `[0, 1]`.\n\nBlue-grey rather than grey by default, and that is the point: the\nunderside of a cloud is lit by the sky above it, not by the sun it is\nhiding from. Darkening this toward slate is most of what makes a storm.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "type": {
            "const": "Cloud",
            "type": "string"
          },
          "wobble": {
            "default": 0.11999999731779099,
            "description": "Smooth radial distortion of each lobe, as a fraction of its radius.\n`[0, 1)`; a little is what stops a lobe from reading as a ball.",
            "format": "float",
            "maximum": 0.99,
            "minimum": 0.0,
            "type": "number"
          }
        },
        "required": [
          "type"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "A patch of ground: displaced terrain with a procedurally shaded surface\n(M22).\n\nThe entity carries **no** [`Mesh`] and **no** [`Material`] — `Terrain` owns\nboth, like [`Water`] — and having either is `terrain_with_mesh`. Geometry is\na tessellated unit grid sized by `Transform.scale`, displaced by an fBm\nheight field; `Transform.scale.y` multiplies that displacement, so [`height`]\nis what you get at scale 1.\n\nHeights are sampled in **world** XZ, so two patches with the same fields meet\nseamlessly and moving one moves it *through* the field rather than dragging\nits hills along.\n\nUnlike water's waves, the height field is evaluated on the **CPU**: terrain\ndoes not animate, so the surface is generated once and cached, and there is\nexactly one implementation for the renderer, the collider (a `trimesh`\n`Collider` with no asset uses this surface) and\n`world.terrain_height(name, x, z)` to share. Appearance is the opposite —\nper-pixel, in the shader, mirrored by nothing, which is what licenses detail\nfar finer than the grid.\n\n[`height`]: Terrain::height",
        "properties": {
          "basins": {
            "default": [],
            "description": "Circular depressions cut into the height field, in **world** XZ (M42).\n\nEmpty (the default) is M22's landscape exactly — the noise is the whole\nanswer. This is the only way to say \"the ground dips *here*\": everything\nelse about a patch describes a texture of relief rather than a place, and\na body of water needs a place.\n\nCut inside [`height_at`](crate::terrain::height_at), so the drawn\nsurface, its normals, the `trimesh` collider standing on it,\n`world.terrain_height`, a `Road` that follows this patch and a `Meadow`\ngrowing on it all follow from the one implementation.",
            "items": {
              "$ref": "#/$defs/TerrainBasin"
            },
            "type": "array"
          },
          "bump": {
            "default": 0.30000001192092896,
            "description": "Per-pixel normal perturbation from the detail noise, `[0, 1]`.\n\nBumpiness with no displacement behind it — nothing physical may depend on\nit, which is what allows detail far finer than the grid or the collider.\nIt fades with view distance, because sub-pixel normal variation aliases\ninto sparkle that reads as broken rather than as low quality (the lesson\nwater's detail ripples already paid for).",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "color_variation": {
            "default": 0.25,
            "description": "How strongly the detail noise modulates the blended albedo, `[0, 1]`.\n\nThe cure for the one-flat-colour look: even a single-layer terrain stops\nbeing a sheet of paint. Past ~0.5 the ground reads as camouflage.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "feature_scale": {
            "default": 40.0,
            "description": "Metres across one cell of the largest noise octave. `> 0`.\n\nThe size of the big rolling features, and the field that decides whether\na patch reads as dunes, as pasture or as foothills. Well under\n`segments`-worth of quads and the grid cannot resolve it; far larger than\nthe patch and the ground reads as a tilted plane.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "height": {
            "default": 2.0,
            "description": "Metres of displacement at full amplitude, `>= 0`.\n\nThe field is normalised to `[-1, 1]` before scaling, so this is the peak\nabove (and below) the entity's own Y, and **adding octaves adds detail\nrather than altitude**. 0 is a flat patch, which is a legitimate thing to\nask for and is still shaded by the layer system.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "layers": {
            "default": [],
            "description": "The materials the surface is painted with, at most\n[`MAX_TERRAIN_LAYERS`](crate::terrain::MAX_TERRAIN_LAYERS), blended by\nheight and slope.\n\nEmpty (the default) paints the whole surface with\n[`TerrainLayer`]'s own defaults, so a bare `{\"type\": \"Terrain\"}` is a\nplausible grassy patch rather than an error or a blank.",
            "items": {
              "$ref": "#/$defs/TerrainLayer"
            },
            "maxItems": 4,
            "type": "array"
          },
          "octaves": {
            "default": 4,
            "description": "How many octaves of noise are summed. `[1, 8]`.\n\nEach octave halves the feature size and scales its amplitude by\n[`persistence`](Terrain::persistence). Past about 5 the added detail is\nfiner than the grid can carry and only costs generation time.",
            "format": "uint32",
            "maximum": 8,
            "minimum": 1,
            "type": "integer"
          },
          "persistence": {
            "default": 0.5,
            "description": "Amplitude multiplier per octave, `[0, 1]`.\n\nLow values give smooth swells; near 1 gives a rough, noisy surface with\nno clear large-scale shape. 0.5 is the usual landscape.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "seed": {
            "default": 0,
            "description": "Chooses the landscape. Any change reshapes every hill.\n\nThe noise hash is written out in this crate rather than pulled from a\ndependency, so a given seed means the same terrain across upgrades — a\nterrain render sits under a `diff-render` baseline, which makes this a\nformat contract.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "segments": {
            "default": 128,
            "description": "Quads per axis across the patch. `[1, 512]`.\n\nThe resolution the *relief* is drawn and collided at. What matters is\nmetres per quad against [`feature_scale`](Terrain::feature_scale): a\n200 m patch at 192 has one vertex per metre, which resolves a 40 m hill\ncomfortably and a 3 m hummock not at all. Surface *detail* is per pixel\nand does not care.",
            "format": "uint32",
            "maximum": 512,
            "minimum": 1,
            "type": "integer"
          },
          "texture_scale": {
            "default": 4.0,
            "description": "Metres across one cell of the surface-detail noise. `> 0`.\n\nThe scale of the mottling within a layer and of the fingers along the\nboundary between two — the *texture*, as opposed to the relief. Around a\nfew metres reads as ground cover seen from standing height.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "type": {
            "const": "Terrain",
            "type": "string"
          },
          "warp": {
            "default": 0.0,
            "description": "Domain warp: how far the field is dragged sideways before it is summed,\nas a fraction of [`feature_scale`](Terrain::feature_scale). `[0, 2]`;\n0 (the default) is off.\n\nTwo lines of arithmetic, and the largest single difference between \"fBm\"\nand \"landscape\". Unwarped fBm is isotropic blobs; warping shears them\ninto ridges and valleys that read as though water once ran over them.\nPast ~1 the surface starts to look smeared.",
            "format": "float",
            "maximum": 2.0,
            "minimum": 0.0,
            "type": "number"
          }
        },
        "required": [
          "type"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "A road: a circuit, a street, a mountain pass.\n\nThe entity owns its surface geometry — one continuous ribbon generated from\nthe centerline — so a `Road` entity carries **no** `Mesh` and no `Material`\n(`road_with_mesh`), the same rule [`Water`] follows and for the same reason.\n\nAsphalt, shoulders and the embankment skirt are all the **same** triangles.\nThat is not a saving, it is the point: road and shoulder as two surfaces at\nslightly different heights build a ledge along the asphalt edge, and a wheel\nthat drops off it wedges against the step and stops the car dead. There is\nno seam between segments either, because consecutive cross-sections share\ntheir vertices.\n\nPhysics reads the same mesh: a `Collider` with `\"shape\": \"trimesh\"` on a\nroad entity needs no `asset` and no `Mesh`, because the road is the\ngeometry. Friction and collision layers stay on the `Collider`, where every\nother surface in the engine keeps them.",
        "properties": {
          "auto_bank": {
            "default": 0.0,
            "description": "Bank every corner by this many degrees at a radius of\n[`auto_bank_radius`](Self::auto_bank_radius), scaling as `1 / radius`\nfor anything wider and capped here for anything tighter (M40). `0` —\nthe default — leaves the road flat. `>= 0`.\n\n**The engine picks the sign**, raising the outside of each turn, and\nthat is the whole reason this field exists next to\n[`RoadPoint::bank`]. Which way a corner banks is a fact about the\nwinding of the polygon it belongs to; deriving it per corner by hand is\nhow a circuit ends up with one corner that throws the car off.\n\nIt is a shape knob, not a physical one. Banking a corner for a *speed*\nis `atan(v² / gR)` and wants a velocity a road cannot know — the answer\nfor a kart and an F1 car differ by 20°.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "auto_bank_radius": {
            "default": 20.0,
            "description": "The corner radius [`auto_bank`](Self::auto_bank) is quoted at, in\nmetres. `> 0`.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "bank_color": {
            "default": [
              0.20000000298023224,
              0.17000000178813934,
              0.12999999523162842
            ],
            "description": "Linear RGB of the embankment below the shoulder. Each component\n`[0, 1]`.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "closed": {
            "default": false,
            "description": "Join the last point back to the first. A closed road is a circuit: the\npolygon's exterior angles sum to one turn, so it shuts without a solver.",
            "type": "boolean"
          },
          "color": {
            "default": [
              0.09000000357627869,
              0.09000000357627869,
              0.10000000149011612
            ],
            "description": "Linear RGB of the asphalt. Each component `[0, 1]`.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "follow_blend": {
            "default": 30.0,
            "description": "How far either side of a [`pinned`](RoadPoint::pin_height) point its\nheight correction fades out, in metres. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "follow_smoothing": {
            "default": 12.0,
            "description": "How far along the road the sampled ground is averaged, in metres.\n`>= 0`, and `0` reproduces the terrain exactly, bumps and all.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "follow_terrain": {
            "default": null,
            "description": "Name of a `Terrain` entity this road rides on (M40). Absent — the\ndefault — is a road whose heights are absolute, which is every road\nbefore M40.\n\nWith it set, each point's `y` becomes a **clearance above the ground**\nand the terrain is sampled at every centerline sample rather than only\nat the authored points, then smoothed over\n[`follow_smoothing`](Self::follow_smoothing) metres — terrain is noise,\nand a road that reproduces it is undrivable.\n\nThe road does **not** carve the terrain: a height field stays the one\nsource of truth for where the ground is. Where the smoothed road passes\nbelow the real ground the ground pokes through it, and the answers are\nmore clearance, a [`pinned`](RoadPoint::pin_height) point, or more\nsmoothing.",
            "type": [
              "string",
              "null"
            ]
          },
          "grain": {
            "default": 0.0,
            "description": "How strongly the asphalt is grained, `[0, 1]`. `0` — the default — is\nthe flat colour M23 shipped (M40).\n\nA value-noise field in the road's own `(u, v)`, so it follows every\ncurve and grade the way the markings do, perturbing albedo and\nroughness. Deliberately not a normal perturbation: grain that tilts the\nshading normal sparkles under a moving camera at exactly the frequencies\na deterministic renderer should not be producing.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "grain_scale": {
            "default": 0.3499999940395355,
            "description": "Size of one grain cell, in metres. `> 0`.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "markings": {
            "$ref": "#/$defs/RoadMarkings",
            "default": {
              "center_dash": 3.0,
              "center_gap": 6.0,
              "center_width": 0.0,
              "color": [
                0.8799999952316284,
                0.8799999952316284,
                0.8799999952316284
              ],
              "edge_inset": 0.10000000149011612,
              "edge_width": 0.14000000059604645,
              "kerb_color": [
                0.800000011920929,
                0.10000000149011612,
                0.07999999821186066
              ],
              "kerb_max_radius": 0.0,
              "kerb_stripe": 1.399999976158142,
              "kerb_width": 0.8999999761581421,
              "start_line": false,
              "start_line_at": 0.0,
              "start_line_width": 0.699999988079071
            },
            "description": "What is painted on it."
          },
          "points": {
            "default": [
              {
                "bank": null,
                "pin_height": false,
                "position": [
                  0.0,
                  0.0,
                  0.0
                ],
                "radius": 0.0,
                "width": null
              },
              {
                "bank": null,
                "pin_height": false,
                "position": [
                  0.0,
                  0.0,
                  -20.0
                ],
                "radius": 0.0,
                "width": null
              }
            ],
            "description": "The centerline, corner by corner, in the order they are driven. At\nleast two points; a closed road needs at least three.",
            "items": {
              "$ref": "#/$defs/RoadPoint"
            },
            "maxItems": 256,
            "type": "array"
          },
          "roughness": {
            "default": 0.9200000166893005,
            "description": "Surface roughness, `[0, 1]`, meaning what `Material.roughness` means.\nAsphalt is nearly matte; wet asphalt is not.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "segment_angle": {
            "default": 5.0,
            "description": "Most degrees of arc one segment may cover through a corner. `>= 0.5`.\n\nThis is the resolution knob that matters: a corner cut every 5° is\nsmooth to drive and to look at, and the cost is linear in the road's\nlength rather than quadratic like a grid's.",
            "format": "float",
            "minimum": 0.5,
            "type": "number"
          },
          "segment_length": {
            "default": 2.0,
            "description": "Longest a straight segment may be before the road is cut again, in\nmetres. `>= 0.25`.",
            "format": "float",
            "minimum": 0.25,
            "type": "number"
          },
          "shoulder": {
            "default": 1.5,
            "description": "Drivable shoulder each side of the asphalt, in metres. `>= 0`.\n\nPart of the same surface, not a second one — see the note above about\nledges.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "shoulder_color": {
            "default": [
              0.17000000178813934,
              0.20000000298023224,
              0.14000000059604645
            ],
            "description": "Linear RGB of the shoulder each side of the asphalt. Each component\n`[0, 1]`.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "skirt": {
            "default": 0.6000000238418579,
            "description": "How far the embankment drops below the road's outer edge, in metres.\n`>= 0`.\n\nThis is what stops an elevated road from floating. Set it deeper than\nthe road ever climbs and it simply disappears under the ground plane\nwherever the road is low.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "type": {
            "const": "Road",
            "type": "string"
          },
          "width": {
            "default": 7.0,
            "description": "Width of the asphalt, edge to edge, in metres. `> 0`.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          }
        },
        "required": [
          "type"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "Where roads meet: the patch of asphalt a ribbon cannot be (M40).\n\nA [`Road`] is swept along a curve, which is the wrong primitive for a\ncrossroads — two ribbons crossing leave a hole. A junction is instead the\narea **bounded by the mouths of the roads that reach it**: each arm names a\nroad and which end of it arrives, and the patch stretches to whatever those\nmouths turn out to be.\n\nThe mouths are read off each road's finished surface — the same\n[`RoadSurface`](crate::road::RoadSurface) the renderer draws and physics\nbuilds its trimesh from — so a junction cannot disagree with the road it\njoins about where that road ended, how wide it was there, what height it\nreached, or how far it was banked. That is the `engine road-centerline` rule\napplied inside the engine: nothing re-derives a curve someone else built.\n\n**The junction does not trim its roads.** It would be the tidier result and\nit inverts the ownership every recipe here follows: a road's geometry would\nbecome a function of which junctions happen to name it, and `engine inspect`\non the road would stop predicting the road. The author ends each road at the\njunction's mouth, and because the patch stretches, \"roughly there\" is enough\n— an arm stopping 2 m short simply makes the patch 2 m longer on that side.\n\nLike every other recipe, the entity owns its geometry and so carries **no**\n`Mesh` and no `Material` (`junction_with_mesh`), and a `Collider` with\n`\"shape\": \"trimesh\"` on it takes the patch.",
        "properties": {
          "arms": {
            "default": [],
            "description": "The roads that meet here, in any order — the patch is built in\nrotational order about the mouths whatever order they are listed in. At\nleast two.",
            "items": {
              "$ref": "#/$defs/JunctionArm"
            },
            "maxItems": 16,
            "type": "array"
          },
          "bank_color": {
            "default": [
              0.20000000298023224,
              0.17000000178813934,
              0.12999999523162842
            ],
            "description": "Linear RGB of the embankment. Each component `[0, 1]`.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "color": {
            "default": [
              0.09000000357627869,
              0.09000000357627869,
              0.10000000149011612
            ],
            "description": "Linear RGB of the asphalt. Each component `[0, 1]`.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "corner_segments": {
            "default": 6,
            "description": "How finely each of those corners is cut. `>= 1`.",
            "format": "uint32",
            "maximum": 64,
            "minimum": 1,
            "type": "integer"
          },
          "flare": {
            "default": 1.0,
            "description": "How far the corner between two arms reaches out toward where their edges\nwould have crossed, `[0, 1]`.\n\n`1` — the default — is the full flare, a quadratic Bézier through that\nintersection, which is the shape a real corner has. `0` is the straight\nchord from one mouth's corner to the next.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "grain": {
            "default": 0.0,
            "description": "How strongly the asphalt is grained, `[0, 1]` — [`Road::grain`] on a\npatch, so a junction and the roads reaching it can wear the same\nsurface.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "grain_scale": {
            "default": 0.3499999940395355,
            "description": "Size of one grain cell, in metres. `> 0`.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "roughness": {
            "default": 0.9200000166893005,
            "description": "Surface roughness, `[0, 1]`.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "shoulder": {
            "default": 1.5,
            "description": "Shoulder around the patch, in metres — the same surface, for the same\nreason a road's is. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "shoulder_color": {
            "default": [
              0.17000000178813934,
              0.20000000298023224,
              0.14000000059604645
            ],
            "description": "Linear RGB of the shoulder. Each component `[0, 1]`.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "skirt": {
            "default": 0.6000000238418579,
            "description": "How far the embankment drops below the shoulder's outer edge, in metres.\n`>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "type": {
            "const": "Junction",
            "type": "string"
          }
        },
        "required": [
          "type"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "Ground cover that grows, seeds and dies on a loop: grass, weeds, wildflowers\nand the dry stand they turn into.\n\nA recipe rather than a mesh reference, like [`Tree`] and [`Cloud`] — the\nengine grows one plant and scatters copies of it over the footprint\n`Transform.scale` gives, so a `Meadow` entity carries **no** `Mesh` and\n**no** `Material` (`meadow_with_mesh`).\n\n**It is the first recipe in this engine whose subject changes shape over\ntime.** A sprout is not a small blade of grass. The resolution is that the\ngeometry is static and the *life cycle lives in the vertex stage*: the plant\nis built once carrying every organ any stage will need, and each organ scales\nto nothing outside the phase window it belongs to. See\n`designs/meadow-design.md`.\n\nThe cycle runs on the scene clock, so it can be sped up:\n[`cycle_length`](Meadow::cycle_length) `: 3.0` runs a whole generation in\nthree seconds. **`0` — the default — freezes the field** at\n[`phase`](Meadow::phase), the way `daylight.day_length: 0` freezes the day:\nmost scenes want a dial, not motion, and a frozen field is reproducible with\nno `--time` at all.\n\nEvery generation **reseeds** rather than regrowing: a plant's position within\nits own cell, its height and its lean all shift a little each time round, so\nthe dead stalk and the sprout that replaces it are not collinear. That costs\none integer hash in the shader and no state anywhere.\n\nA meadow is scenery: no `Collider`, no shadow cast (a 2048² map cannot\nresolve a blade of grass, and what it would record is noise that crawls),\nand no `PointLight` contribution.",
        "properties": {
          "blade_width": {
            "default": 0.007000000216066837,
            "description": "Width of a blade at its base, in metres, `> 0`.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "blades": {
            "default": 5,
            "description": "Blades in one plant. `[1, 12]`; `3`–`5` reads as a tuft.",
            "format": "uint32",
            "maximum": 12,
            "minimum": 1,
            "type": "integer"
          },
          "cycle_length": {
            "default": 0.0,
            "description": "Seconds one full life cycle takes, `>= 0`.\n\n**`0` freezes the field** at [`phase`](Meadow::phase) — the default, and\n`daylight.day_length: 0`'s reasoning exactly.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "density": {
            "default": 24.0,
            "description": "Plants per square metre of footprint, `>= 0`.\n\nThe footprint is `Transform.scale` in XZ, so the plant count is\n`density × area`, rounded up to a square grid. `0` is an empty field,\nwhich is a legitimate thing to animate toward.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "flower_color": {
            "default": [
              0.4000000059604645,
              0.3700000047683716,
              0.17000000178813934
            ],
            "description": "Linear RGB of the flower head, each component `[0, 1]`. What stops the\nweed stage from being nothing but taller grass.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "head_size": {
            "default": 0.017999999225139618,
            "description": "Size of the flower and seed heads, in metres, `>= 0`. `0` grows a plant\nthat never flowers.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "height": {
            "default": 0.44999998807907104,
            "description": "Height of a fully grown plant in metres, `> 0`. `Transform.scale.y`\nmultiplies it, the way it multiplies a [`Terrain`]'s relief.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "max_slope": {
            "default": 38.0,
            "description": "Steepest ground grass will grow on, in degrees, `[0, 90]`. Plants on\nsteeper ground are dropped; `90` keeps everything.",
            "format": "float",
            "maximum": 90.0,
            "minimum": 0.0,
            "type": "number"
          },
          "phase": {
            "default": 0.41999998688697815,
            "description": "Where the cycle starts, `[0, 1)` — and where a frozen field sits. The\ndefault is mature green, so `{\"type\": \"Meadow\"}` alone puts a working\nfield of grass in a scene.",
            "format": "float",
            "maximum": 0.999,
            "minimum": 0.0,
            "type": "number"
          },
          "seed": {
            "default": 0,
            "description": "Seeds placement and the template's blades. Two meadows with the same\nparameters and different seeds are different fields.\n\nThe generator's xorshift and the shader's reseed hash are both written\nout in this repo, so a given seed means the same field across dependency\nupgrades — a meadow render sits under a `diff-render` baseline, which\nmakes both a format contract.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "segments": {
            "default": 4,
            "description": "Lengthwise segments per blade — how finely a blade can curve as it leans\nand bends in the wind. `[1, 8]`.\n\nTogether with [`blades`](Meadow::blades) this sets the per-plant\ntriangle count, and the product with the plant count is what\n`meadow_too_complex` bounds.",
            "format": "uint32",
            "maximum": 8,
            "minimum": 1,
            "type": "integer"
          },
          "size_jitter": {
            "default": 0.3499999940395355,
            "description": "How much plant height varies between plants, as a fraction, `[0, 1)`.\n`0` is a lawn.",
            "format": "float",
            "maximum": 0.99,
            "minimum": 0.0,
            "type": "number"
          },
          "splay": {
            "default": 54.0,
            "description": "Degrees the outermost blades splay from vertical, `[0, 90]`. Blade 0\nstays near upright, which is what gives a tuft a centre rather than a\nhole.",
            "format": "float",
            "maximum": 90.0,
            "minimum": 0.0,
            "type": "number"
          },
          "stages": {
            "default": [
              {
                "at": 0.0,
                "color": [
                  0.12999999523162842,
                  0.10000000149011612,
                  0.05000000074505806
                ],
                "height": 0.0,
                "lean": 0.0,
                "sway": 0.0,
                "tip_color": [
                  0.1599999964237213,
                  0.12999999523162842,
                  0.07000000029802322
                ],
                "width": 0.5
              },
              {
                "at": 0.09000000357627869,
                "color": [
                  0.1599999964237213,
                  0.3400000035762787,
                  0.07000000029802322
                ],
                "height": 0.1599999964237213,
                "lean": 10.0,
                "sway": 0.5,
                "tip_color": [
                  0.3100000023841858,
                  0.550000011920929,
                  0.12999999523162842
                ],
                "width": 0.699999988079071
              },
              {
                "at": 0.3100000023841858,
                "color": [
                  0.05999999865889549,
                  0.1899999976158142,
                  0.03999999910593033
                ],
                "height": 0.8600000143051147,
                "lean": 17.0,
                "sway": 1.0,
                "tip_color": [
                  0.17000000178813934,
                  0.4000000059604645,
                  0.09000000357627869
                ],
                "width": 1.0
              },
              {
                "at": 0.5299999713897705,
                "color": [
                  0.09000000357627869,
                  0.20000000298023224,
                  0.05000000074505806
                ],
                "height": 1.0,
                "lean": 21.0,
                "sway": 0.8999999761581421,
                "tip_color": [
                  0.30000001192092896,
                  0.36000001430511475,
                  0.10000000149011612
                ],
                "width": 1.0499999523162842
              },
              {
                "at": 0.7599999904632568,
                "color": [
                  0.2199999988079071,
                  0.18000000715255737,
                  0.07000000029802322
                ],
                "height": 0.949999988079071,
                "lean": 27.0,
                "sway": 0.4000000059604645,
                "tip_color": [
                  0.6200000047683716,
                  0.5099999904632568,
                  0.1899999976158142
                ],
                "width": 0.9200000166893005
              },
              {
                "at": 0.9100000262260437,
                "color": [
                  0.14000000059604645,
                  0.10999999940395355,
                  0.05999999865889549
                ],
                "height": 0.6200000047683716,
                "lean": 71.0,
                "sway": 0.25,
                "tip_color": [
                  0.25999999046325684,
                  0.20999999344348907,
                  0.10999999940395355
                ],
                "width": 0.800000011920929
              }
            ],
            "description": "The life cycle, as keyframes over `phase`. At least two, at most\n[`MAX_GROWTH_STAGES`](crate::meadow::MAX_GROWTH_STAGES), strictly\nincreasing in `at`.\n\nThe default is the full seed → sprout → grass → weeds → dry → collapse\ncycle, so a meadow is worth looking at before anything is authored.",
            "items": {
              "$ref": "#/$defs/GrowthStage"
            },
            "type": "array"
          },
          "stagger": {
            "default": 0.25,
            "description": "How far plants desync from each other, `[0, 1]`.\n\n`0` marches the whole field in lockstep; `1` spreads offsets across the\nwhole cycle, so every stage is present at every moment and the field\nnever appears to change. A real meadow browns together with variation,\nwhich is why the default is near the low end.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "terrain": {
            "description": "The [`Terrain`] entity this meadow stands on, by name.\n\nEach plant's altitude is sampled from that patch through the same\nfunction `world.terrain_height` and `engine terrain-height` call, so\nthere is one implementation of \"where is the ground\" and nothing to keep\nin agreement. Absent, the field is flat at the entity's own Y.",
            "type": [
              "string",
              "null"
            ]
          },
          "type": {
            "const": "Meadow",
            "type": "string"
          },
          "wind": {
            "default": 9.0,
            "description": "How far the wind bends a plant at full sway, in degrees, `>= 0`. `0` is\nstill air.",
            "format": "float",
            "maximum": 90.0,
            "minimum": 0.0,
            "type": "number"
          },
          "wind_direction": {
            "default": 0.0,
            "description": "Which way the wind blows, in degrees — `0` toward −Z, the engine's\nforward convention, shared with `Water`'s wave directions.",
            "format": "float",
            "type": "number"
          },
          "wind_speed": {
            "default": 3.5,
            "description": "How fast gusts travel across the field, in metres per second, `>= 0`.\n\nGusts are a travelling wave, not a per-plant shimmer: sampling the noise\nagainst a moving coordinate is what makes wind cross a meadow visibly.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          }
        },
        "required": [
          "type"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "Puts a skinned character's feet on the terrain under them (M32).\n\nA post-pass over the posed skeleton: each named ankle is moved to the\nground beneath where the clip put it, the joints above it rotate to follow\n(two-bone IK), the hips drop when a leg cannot reach, and the sole tilts to\nthe slope. It runs wherever the pose does, so `engine list-joints --time`\nreports the planted rig and the render draws it — one answer, not two.\n\nThe ground is a `Terrain` entity named in `ground`, and that is a purity\ndecision rather than a convenience: planting against the *physics* world\nwould make the pose a function of the simulation, and the pose being a pure\nfunction of (files, time) is what lets `list-joints` answer at all. The cost\nis that a character cannot stand on a crate — see\n`designs/locomotion-design.md` §5.",
        "properties": {
          "align": {
            "default": 30.0,
            "description": "Degrees the sole may tilt to meet the ground's normal. `0` leaves the\nfoot's animated orientation alone, which is right for a character that\nonly ever walks on the flat.",
            "format": "float",
            "maximum": 90.0,
            "minimum": 0.0,
            "type": "number"
          },
          "feet": {
            "description": "The feet to plant, at most [`MAX_PLANTED_FEET`].",
            "items": {
              "$ref": "#/$defs/PlantedFoot"
            },
            "type": "array"
          },
          "ground": {
            "description": "The entity carrying the `Terrain` these feet stand on.",
            "type": "string"
          },
          "hips": {
            "description": "The joint lowered when a leg cannot reach its target — the pelvis, in\na humanoid. Absent, the deficit is simply clamped: one foot plants and\nthe other stretches, which is bounded but reads as wrong.",
            "type": [
              "string",
              "null"
            ]
          },
          "max_drop": {
            "default": 0.5,
            "description": "How far below the animated ankle a target may be, in metres. This is\nwhat keeps a foot in mid-swing from being dragged to the floor:\nplanting is a *correction*, and a correction with no ceiling is a\ndifferent animation.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "max_lift": {
            "default": 0.5,
            "description": "And how far above it.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "type": {
            "const": "FootPlant",
            "type": "string"
          }
        },
        "required": [
          "type",
          "feet",
          "ground"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "Collision proxies that follow a skinned character's pose (M33).\n\nM30 said a skinned mesh is visual and physics sees only the entity's own\n`Collider`; this is the one item of that reversed. Each part is re-posed\nevery fixed step from the same joint globals the render and\n`engine list-joints` use, so a hitbox cannot disagree with the picture\nabout where a head is.\n\n**The pose drives the proxies and nothing reads them back** — they are\nkinematic, so they are hit, they push dynamic bodies, and they report\ncontacts, but they never move a joint. That is what keeps M30's claim that\nthe pose is a pure function of (files, time) true, and it is why a proxy\nholds a character up exactly as much as a moving wall holds up the hand\npushing it: not at all. What a character stands on is still its own\n`Collider`.\n\nLayers, friction and restitution sit here rather than on each part: \"bullets\nhit hitboxes\" is a statement about the character, and per-part copies would\nbe four more strings to keep in agreement per part.",
        "properties": {
          "collides_with": {
            "description": "Only interact with colliders belonging to these layers. Absent =\ninteract with everything.",
            "items": {
              "type": "string"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "friction": {
            "default": 0.5,
            "description": "`>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "layers": {
            "description": "Collision layers every part belongs to. Absent = every layer, exactly\nas on `Collider`. Empty is an error — omit the field instead.",
            "items": {
              "type": "string"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "parts": {
            "description": "The proxies, at most [`MAX_COLLIDER_PARTS`].",
            "items": {
              "$ref": "#/$defs/ColliderPart"
            },
            "type": "array"
          },
          "restitution": {
            "default": 0.0,
            "description": "Bounciness, `[0, 1]`, max-combined like `Collider.restitution`.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "type": {
            "const": "SkinnedCollider",
            "type": "string"
          }
        },
        "required": [
          "type",
          "parts"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "Physics driving a skinned character's skeleton (M39).\n\nM33 said the pose drives the proxies and nothing reads them back, and called\nthat its whole design. **This reverses that sentence for one entity, once,\npermanently.** When `active` turns true the entity's `SkinnedCollider`\nproxies stop being kinematic followers and become dynamic bodies wired\ntogether with rapier joints; from that step on the skeleton is a report of\nwhere they ended up.\n\n**The pose stays in the file, which is why invariant 2 survives the\nreversal.** `pose` is written back after every step, exactly as M32 writes\n`AnimationPlayer.phase` back, and `locomotion::posed_globals_at` reads it\nbefore it looks at a clip — so the render, `engine list-joints`,\n`engine list-colliders` and `world.joint_position` all see the ragdolled\nskeleton through the seam they already shared. M32's rule is what settled\nit: a ragdoll halfway to the floor, baked and reloaded, has to land in the\nsame heap, and a pose living in the physics world would reload standing up.\n\nThe bodies **are** the proxies, so the hitbox that was shot is the body that\nfalls, and the collider set does not change on handoff — which matters\nbecause that set is an input to rapier's broad phase.\n\nSee `designs/ragdoll-design.md`.",
        "properties": {
          "active": {
            "default": false,
            "description": "Whether physics owns the skeleton. A scene may ship this true and its\ncharacter is a corpse from step 0; `world.ragdoll(name)` sets it, and\nnothing clears it — the handoff is one-way (design §3).",
            "type": "boolean"
          },
          "angular_damping": {
            "default": 0.6000000238418579,
            "description": "The same, for spin — and the one that stops a corpse pinwheeling.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "density": {
            "default": 985.0,
            "description": "kg/m³, `Collider.density`'s unit. Each part's mass is its shape's\nvolume times this. Defaults to a shade under water, which is roughly\nwhat a person is.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "joints": {
            "description": "Joints that want something other than the default cone.",
            "items": {
              "$ref": "#/$defs/RagdollJoint"
            },
            "type": "array"
          },
          "limit": {
            "default": 45.0,
            "description": "Half-angle in degrees of the cone every joint gets unless `joints`\noverrides it.",
            "format": "float",
            "maximum": 180.0,
            "minimum": 0.0,
            "type": "number"
          },
          "linear_damping": {
            "default": 0.05000000074505806,
            "description": "Velocity damping on every part. Deliberately above a physically honest\nvalue: a real body tumbles for longer than a game wants to watch, and\nthis is the dial that fixes it.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "pose": {
            "description": "The skeleton, once physics owns it: one entry per joint of the rig,\nwritten by the engine after every step. Absent until the handoff.\n\nIt is in the file rather than in the physics world because that is what\nmakes a baked ragdoll reload into the same heap — see the type's docs.",
            "items": {
              "$ref": "#/$defs/JointPose"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "type": {
            "const": "Ragdoll",
            "type": "string"
          }
        },
        "required": [
          "type"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "Makes a dynamic body float on a named [`Water`] surface (M41).\n\nArchimedes, sampled: the body's collider is divided into columns, each column\nis asked how deep it sits under the wave above it, and each pushes up with\nthe weight of the water it displaces. Because the pushes land at their own\ncolumns rather than at the centre of mass, a hull that rolls has more of\nitself submerged on the low side and rights itself — the pitch and roll come\nout of the same sum as the lift, with nothing modelling them separately.\n\n**Absent, nothing floats**, which is the pre-M41 engine exactly. The\ncomponent needs a `RigidBody` that is dynamic and a `Collider` to have a\nshape at all, and validation says so rather than letting a scene author a\ncomponent that silently does nothing.",
        "properties": {
          "angular_drag": {
            "default": 2.0,
            "description": "Angular damping in 1/s, scaled by submersion exactly as [`drag`] is.\n`>= 0`.\n\nUsually wants to be the larger of the two: water stops a hull from\nspinning far more effectively than it stops it from drifting, and a boat\nthat rolls for twenty seconds after a wave reads as weightless.\n\n[`drag`]: Buoyancy::drag",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "drag": {
            "default": 1.0,
            "description": "Linear damping in 1/s applied **in proportion to how submerged the body\nis**, `>= 0`.\n\nAdded on top of [`RigidBody::linear_damping`], not replacing it: that\nfield is the body's drag in air, and this is the water's. Water drag is\nnot a property of the boat, which is exactly why it cannot be authored on\nthe `RigidBody` — a hull thrown clear of the pond has to stop being\ndamped the moment it leaves, and a half-submerged one is dragged half as\nhard.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "samples": {
            "default": 2,
            "description": "Columns per axis across the body's footprint, `[1, 4]`. Default 2, so a\nhull is sampled at four points.\n\nThe fidelity knob, and it decides whether the body can **turn**: at 1\nthere is a single upward push through the middle and a raft cannot right\nitself or ride a slope, because a force through the centre of mass makes\nno torque. At 2 each quarter of the hull feels its own wave, which is\nwhat makes a boat pitch into a swell instead of hovering over it. Past\nthat the returns fall off quickly — 3 and 4 are for a long hull spanning\nseveral wavelengths.",
            "format": "uint32",
            "maximum": 4,
            "minimum": 1,
            "type": "integer"
          },
          "type": {
            "const": "Buoyancy",
            "type": "string"
          },
          "water": {
            "default": "",
            "description": "The [`Water`] entity this body floats on, by name. Required.\n\nNamed rather than found by overlap, for [`Meadow::terrain`]'s reason: one\nimplementation of \"where is the surface\", pointed at explicitly. A scene\nwith two ponds has to say which one, and a scene with one still says it,\nso the file records what the physics did.",
            "type": "string"
          }
        },
        "required": [
          "type"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "A convex piece of a broken thing (M43), as a point set that **owns its\ngeometry** — so an entity with one carries no `Mesh`, the rule `Water`,\n`Terrain`, `Cloud`, `Road`, `Junction` and `Meadow` already follow.\n\nIt is `Tree`'s exception on materials: the entity's own `Material` is the\nshard's surface, because a shard of a painted crate is painted.\n\nThe drawn solid is the convex hull of `points`, flat-shaded, and a\n`convex_hull` `Collider` on the same entity collides with that same hull —\nthe file carries points rather than faces precisely so the two cannot be\ndifferent shapes.\n\nOrdinary authored data, not a runtime-only artifact: rubble on the ground is\na pile of these. `engine fracture` is what usually writes them.",
        "properties": {
          "points": {
            "description": "The convex point set, in entity-local metres, scaled by\n`Transform.scale` like every other length. At least 4, at most 32, and\nthey must bound a volume (`shard_degenerate`).",
            "items": {
              "items": {
                "format": "float",
                "type": "number"
              },
              "maxItems": 3,
              "minItems": 3,
              "type": "array"
            },
            "maxItems": 32,
            "minItems": 4,
            "type": "array"
          },
          "type": {
            "const": "Shard",
            "type": "string"
          }
        },
        "required": [
          "type",
          "points"
        ],
        "type": "object"
      },
      {
        "additionalProperties": false,
        "description": "Defines the serialized component union alongside everything that must stay\nin step with it.\n\nThe name list feeds `did_you_mean` suggestions and the spawn arm feeds\nA region of space holding a grid of baked light probes (M35).\n\nThe fill light every other surface gets is a lie: `sky_ambient` hands the\nwhole sky hemisphere to the inside of a forest, the underside of a truck and\na bare patch of open ground alike, because nothing in the engine knows that\ngeometry stands between a surface and the sky. A `LightProbeVolume` replaces\nthat constant with a function of *where you are standing and which way you\nare facing* — which is what puts contact darkening under an object and lets\ncolour travel off a coloured wall.\n\nLike [`Water`], [`Terrain`], [`Road`], [`Cloud`] and [`Meadow`], the entity\ncarries a `Transform` and this component and **no `Mesh` and no `Material`**\n(`light_probe_volume_with_mesh`). Unlike those, it grows no geometry at all:\nthe `Transform` is read purely as *bounds* — a unit box scaled and\npositioned, non-uniform scale being the normal case.\n\n```json\n{ \"type\": \"LightProbeVolume\", \"spacing\": 4.0, \"bake\": \"gi/showcase.gi.json\" }\n```\n\nSeveral volumes may overlap, which is how an interior gets finer spacing than\nthe landscape around it; the smallest `spacing` wins, name-sorted where two\ntie. A point outside every volume falls back to `sky_ambient`, which is\nexactly the pre-M35 path — so [`blend`](LightProbeVolume::blend) exists to\nmake that boundary a fade rather than a step.\n\nThe bake is a file rather than a load-time computation because the loop is\nthe product: a BVH build plus a few hundred thousand rays is seconds, and it\nwould otherwise be paid by every `screenshot`, every `diff-render` and every\neditor reload.",
        "properties": {
          "bake": {
            "default": "",
            "description": "Path to the baked transfer file, relative to the **scene file**\n(invariant 3), conventionally `gi/<name>.gi.json`.\n\nWritten by `engine bake-gi`. A path that is not there is\n`gi_bake_missing`; one taken from a different version of the scene is\n`gi_bake_stale`. Neither is a warning, because a bake that disagrees\nwith its geometry renders light that is quietly wrong — the one failure\nmode this system is built to make impossible.",
            "type": "string"
          },
          "blend": {
            "default": 1.0,
            "description": "Metres of fade at the volume's edge, `>= 0`.\n\nOver this distance the volume's irradiance crosses to whatever lies\noutside it — another volume, or the `sky_ambient` fallback. Without it\nthe boundary is a visible step, since the two models disagree by exactly\nthe occlusion the volume measured.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "bounces": {
            "default": 1,
            "description": "Light bounces the bake gathers, `[1, 2]`.\n\nBounce one holds nearly all of the visible difference; bounce two costs\nanother pass over the volume and mostly lifts the black out of deep\nocclusion.",
            "format": "uint32",
            "maximum": 2,
            "minimum": 1,
            "type": "integer"
          },
          "intensity": {
            "default": 1.0,
            "description": "Scales the whole effect, `>= 0`.\n\nExists so an authoring pass can dial GI back without re-baking, and so\n`0.0` is a one-field A/B against the pre-M35 look. Animatable, which is\nhow a scene fades GI in.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "spacing": {
            "default": 4.0,
            "description": "Metres between probes, `> 0`.\n\nA spacing, not a resolution: resizing a volume keeps its GI detail\ninstead of stretching it, and two volumes at the same spacing agree\nwhere they meet. Probe counts are derived from this and the `Transform`\nscale, reported by `bake-gi`, and bounded by `too_many_gi_probes`.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "type": {
            "const": "LightProbeVolume",
            "type": "string"
          }
        },
        "required": [
          "type"
        ],
        "type": "object"
      }
    ],
    "title": "ComponentData"
  },
  "components": [
    "Transform",
    "Mesh",
    "Camera",
    "Material",
    "DirectionalLight",
    "AmbientLight",
    "PointLight",
    "RigidBody",
    "Collider",
    "Breakable",
    "AnimationPlayer",
    "Script",
    "Wheel",
    "HudText",
    "HudRect",
    "HudPanel",
    "HudImage",
    "HudInteract",
    "ParticleEmitter",
    "Tree",
    "Water",
    "Cloud",
    "Terrain",
    "Road",
    "Junction",
    "Meadow",
    "FootPlant",
    "SkinnedCollider",
    "Ragdoll",
    "Buoyancy",
    "Shard",
    "LightProbeVolume"
  ],
  "scene": {
    "$defs": {
      "BodyKind": {
        "description": "How a rigid body participates in simulation.",
        "oneOf": [
          {
            "const": "dynamic",
            "description": "Fully simulated: forces, gravity, collisions.",
            "type": "string"
          },
          {
            "const": "kinematic",
            "description": "Moved by writes to its `Transform`; pushes dynamics, is not pushed.",
            "type": "string"
          },
          {
            "const": "fixed",
            "description": "Never moves. For platforms that also want a `RigidBody`; bare\n`Collider`s without a `RigidBody` are static geometry too.",
            "type": "string"
          }
        ]
      },
      "ColliderFit": {
        "description": "How a proxy's size is decided (M39). See [`ColliderPart::fit`].",
        "enum": [
          "bone"
        ],
        "type": "string"
      },
      "ColliderPart": {
        "additionalProperties": false,
        "description": "One proxy of a [`SkinnedCollider`]: a simple shape fixed in one joint's\nframe (M33).\n\nFlat and discriminated by `shape`, exactly as [`Collider`] is and for the\nsame reasons — the schema-driven walk and the editor's generated inspector\nboth read flat structs, and `jq` and an LLM both write them. `cuboid`,\n`sphere` and `capsule` only: `trimesh` and `convex_hull` describe a\nspecific mesh, and a proxy exists precisely because that mesh lives on the\nGPU where physics cannot reach it (`collider_part_shape_unsupported`).",
        "properties": {
          "fit": {
            "anyOf": [
              {
                "$ref": "#/$defs/ColliderFit"
              },
              {
                "type": "null"
              }
            ],
            "description": "Solve this part's length from the posed bone instead of holding the\nauthored one (M39). Absent — the default, and every pre-M39 part — is\nM33's rule exactly: only the placement follows the rig, never the size.\n\n`\"bone\"` takes a `capsule`'s `half_height`, or a `cuboid`'s Y\nhalf-extent, from the posed distance between this part's joint and that\njoint's first child, so a proxy set fitted to a rest pose keeps fitting\na skeleton the solver is moving. A joint with no child keeps the\nauthored value, which is what a hand or a head has."
          },
          "half_extents": {
            "description": "`cuboid` only. Each component `> 0`.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": [
              "array",
              "null"
            ]
          },
          "half_height": {
            "description": "`capsule` only: half the cylindrical section's height. `> 0`.",
            "format": "float",
            "type": [
              "number",
              "null"
            ]
          },
          "joint": {
            "description": "The joint this proxy rides. Its posed world transform, times `offset`\nand `rotation`, is where the shape is each step.",
            "type": "string"
          },
          "name": {
            "description": "What reports call this part. Absent, the joint's name — which is right\nfor the ordinary one-proxy-per-joint set and wrong the moment a limb\ntakes two, hence the field. Unique within the component.",
            "type": [
              "string",
              "null"
            ]
          },
          "offset": {
            "default": [
              0.0,
              0.0,
              0.0
            ],
            "description": "Metres from the joint's origin, **in the joint's own frame**, so a\nproxy is authored against the bone rather than against the world.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "radius": {
            "description": "`sphere` and `capsule`. `> 0`.",
            "format": "float",
            "type": [
              "number",
              "null"
            ]
          },
          "rotation": {
            "default": [
              0.0,
              0.0,
              0.0
            ],
            "description": "Euler degrees in the joint's frame. A capsule's axis is local **+Y**\n(rapier's, and `builtin:cylinder`'s), and this is what turns it onto a\nbone that runs some other way.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "sensor": {
            "default": false,
            "description": "Sensors detect overlaps but exert no forces — `Collider.sensor`,\nmeaning the same thing. Per part rather than per component, because a\nsword's blade and its guard want opposite answers.",
            "type": "boolean"
          },
          "shape": {
            "$ref": "#/$defs/ColliderShapeKind"
          }
        },
        "required": [
          "joint",
          "shape"
        ],
        "type": "object"
      },
      "ColliderShapeKind": {
        "description": "The collision shape kinds `Collider.shape` may name.",
        "enum": [
          "cuboid",
          "sphere",
          "capsule",
          "trimesh",
          "convex_hull"
        ],
        "type": "string"
      },
      "ComponentData": {
        "description": "One component as it appears in a scene file.\n\nInternally tagged on `\"type\"`, so each component is a flat object\nrather than a single-key wrapper — the shape `jq` is pleasant\nagainst and the shape an LLM is least likely to get wrong.",
        "oneOf": [
          {
            "additionalProperties": false,
            "description": "Position, orientation, and scale.\n\nAll three fields are optional in JSON; omitting one gives the identity\nvalue, so a scene can say `{\"type\": \"Transform\", \"position\": [0, 3, 0]}`\nwithout restating a rotation and scale it does not care about.\n\n**One world unit is one metre**, and that is the convention every other\nnumber in the format is quoted against: gravity is `-9.81` because a body\nfalls 9.81 m/s², `Tree.height: 6.0` is a six-metre tree, and a `Wheel`\n0.35 m in radius belongs under a car 1.7 m wide. Time is seconds and mass\nis kilograms, so `Collider.density` is kg/m³ — note its default of `1.0`\nis *not* a plausible material, and a body meant to be pushed by forces\nwants a real one (the demo car's box chassis carries `350`, which is how\n4.3 m³ becomes 1.5 t).",
            "properties": {
              "position": {
                "default": [
                  0.0,
                  0.0,
                  0.0
                ],
                "description": "World-space `[x, y, z]` **in metres**. +Y is up.",
                "items": {
                  "format": "float",
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "rotation": {
                "default": [
                  0.0,
                  0.0,
                  0.0
                ],
                "description": "Euler angles in **degrees**, `[x, y, z]`, applied in X→Y→Z intrinsic\norder (glam `EulerRot::XYZ`). Identity is `[0, 0, 0]`.\n\nDegrees rather than a quaternion is a settled decision (design doc §9):\nan agent told to \"rotate 45° about Y\" writes `[0.0, 45.0, 0.0]`\ndirectly, whereas an unlabeled `[x, y, z, w]` array invites silent\nordering bugs.",
                "items": {
                  "format": "float",
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "scale": {
                "default": [
                  1.0,
                  1.0,
                  1.0
                ],
                "description": "Multiplier on the entity's own geometry, per axis. Identity is\n`[1, 1, 1]`.\n\n**Every `builtin:` primitive is one metre across at scale 1**, so on\nthose this field reads directly as a size in metres: a\n`builtin:cube` at `[1.7, 0.7, 3.6]` is a car-sized box, and a\n`builtin:sphere` at `[0.9, 0.9, 0.9]` is 0.9 m across — *not* 1.8.\nThe recipes size the same way (`Terrain`, `Water` and `Meadow` take\ntheir footprint from `scale` in XZ), and it also multiplies\n`Collider` dimensions, which is why a cuboid collider matching a\nbuiltin cube is authored as `half_extents: [0.5, 0.5, 0.5]` in\n*local* units rather than in metres.",
                "items": {
                  "format": "float",
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "type": {
                "const": "Transform",
                "type": "string"
              }
            },
            "required": [
              "type"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "Renderable geometry.\n\n`asset` is either a `builtin:` primitive (`builtin:cube`, `builtin:cylinder`,\n`builtin:plane`, `builtin:sphere`, `builtin:triangle`) or a `.gltf`/`.glb`\nfile's relative path, resolved\nagainst the directory of the scene file that references it (invariant 3).\n`engine validate` checks the reference resolves; a file that exists but\nfails to parse is reported by validation's asset pass and again at render\ntime.",
            "properties": {
              "asset": {
                "type": "string"
              },
              "type": {
                "const": "Mesh",
                "type": "string"
              }
            },
            "required": [
              "type",
              "asset"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "A viewpoint. `engine screenshot --camera <name>` selects one by entity name.",
            "properties": {
              "active": {
                "default": false,
                "description": "Marks the camera used when none is named explicitly.\n\nExactly one camera in a scene may set this. Zero or several is a\nvalidation error rather than a warning-plus-guess: a deterministic\nfailure is cheaper for an agent than a nondeterministic success.",
                "type": "boolean"
              },
              "far": {
                "default": 1000.0,
                "description": "Far clip distance. Strictly positive, and must exceed `near`\n(checked cross-field by `engine validate`).",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "fov": {
                "default": 60.0,
                "description": "Vertical field of view, in degrees. Strictly between 0 and 180.",
                "exclusiveMaximum": 180.0,
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "near": {
                "default": 0.10000000149011612,
                "description": "Near clip distance. Strictly positive.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "type": {
                "const": "Camera",
                "type": "string"
              }
            },
            "required": [
              "type"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "Surface appearance, in the metallic/roughness parameterization every\nmainstream engine and glTF file uses.\n\nAll color fields are **linear** RGB in `[0, 1]` — physical reflectance, not\nsRGB-encoded screen values. The engine never silently decodes an authored\ncolor; the PNG pixel is the lit, sRGB-encoded result, so `albedo: [0.5,\n0.5, 0.5]` under full light reads back ≈188, not 128.\n\nSince M26 a material may instead be a **file**: `{\"type\": \"Material\",\n\"asset\": \"materials/asphalt.json\"}` names a JSON document holding these same\nfields minus the `\"type\"`. `asset` is exclusive with every other field —\nsetting both is `material_asset_with_fields` — because serde cannot tell an\nabsent field from one written at its default, so a partial override would\nresolve to something the file does not say. A variant is a second file.",
            "properties": {
              "albedo": {
                "default": [
                  0.800000011920929,
                  0.800000011920929,
                  0.800000011920929
                ],
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "albedo_map": {
                "description": "An sRGB colour texture, **multiplied** by `albedo` (M26).\n\nA tint over the map, not a replacement — which means the default\n`[0.8, 0.8, 0.8]` darkens an imported texture by 20% unless the file\nsays `\"albedo\": [1, 1, 1]` beside the map. `engine import` writes that\nexplicitly; a hand-authored material has to.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "alpha": {
                "default": 1.0,
                "description": "Uniform opacity: `1` = opaque, `0` = invisible. Range `[0, 1]`.\n\nA flat blend with no view dependence — the \"ghost this object\" knob.\nAnything below 1 moves the entity out of the opaque pass and into the\nsorted blended one, where it tests depth but does not write it.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "alpha_cutoff": {
                "default": 0.0,
                "description": "Above 0, a pixel whose `albedo_map` alpha falls below this is\ndiscarded — and so is its shadow, through a second caster pipeline with\na fragment stage. This is what an alpha-cut leaf needs. Range `[0, 1]`;\n`0` (the default) cuts nothing, so the depth-only caster pass every\ncurrent scene uses is untouched.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "asset": {
                "description": "A `materials/*.json` file holding this material, relative to the scene\nfile (invariant 3). Exclusive with every other field.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "attenuation": {
                "default": [
                  1.0,
                  1.0,
                  1.0
                ],
                "description": "What survives that path, per linear-RGB channel: transmitted colour is\nscaled by `exp(-(1 - attenuation) * thickness)`. `[1, 1, 1]` absorbs\nnothing.",
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "emissive": {
                "default": [
                  0.0,
                  0.0,
                  0.0
                ],
                "description": "Added after lighting, unaffected by any light — \"make this visible\nregardless of lighting\" is a debugging move worth having. Range\n`[0, 1]` per component.",
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "emissive_map": {
                "description": "An sRGB colour texture multiplied by `emissive`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "ior": {
                "default": 1.0,
                "description": "Index of refraction, `1.0` (the default) being no bending at all.\nRead only by a transmissive surface, where it refracts the view vector\nagainst the shading normal and offsets the scene-colour sample. Range\n`[1, 3]` — glass is 1.5, water 1.33, diamond 2.4.",
                "format": "float",
                "maximum": 3.0,
                "minimum": 1.0,
                "type": "number"
              },
              "metallic": {
                "default": 0.0,
                "description": "`0` = dielectric, `1` = metal. Metals have no diffuse; their specular\nis tinted by `albedo`. Range `[0, 1]`.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "normal_map": {
                "description": "A tangent-space normal map, linear data. The tangent frame is derived\nper pixel from screen-space derivatives rather than stored per vertex,\nso this works unmodified on `Water`, `Terrain`, `Road`, `Tree` and\n`Cloud` geometry, none of which carries tangents.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "normal_strength": {
                "default": 1.0,
                "description": "Scales the normal map's tangent-space XY. The first thing anyone does\nwith a normal map is discover it is too strong, and the alternative is\nre-authoring the texture.",
                "format": "float",
                "maximum": 8.0,
                "minimum": 0.0,
                "type": "number"
              },
              "orm_map": {
                "description": "Occlusion in R, roughness in G, metallic in B — glTF's packing, so an\nimport is a file copy rather than a channel re-pack. Linear data, never\ncolour. R multiplies the ambient and sky terms only, never the direct\nsun: that is what makes it *ambient* occlusion rather than a second\nshadow. G and B multiply `roughness` and `metallic`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "roughness": {
                "default": 0.8999999761581421,
                "description": "Perceptual roughness: `0` = mirror-tight highlight, `1` = matte.\nRange `[0, 1]`.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "thickness": {
                "default": 0.0,
                "description": "How far light travels inside the surface, in metres. Both the scale of\nthe refraction offset and the Beer–Lambert path length, so a thick block\nof ice is finally greener than a thin one. `0` is the pre-M26 behaviour.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "transmission": {
                "default": 0.0,
                "description": "How much light passes *through* the surface instead of scattering off\nit: `0` = opaque, `1` = clear glass. Range `[0, 1]`.\n\nUnlike [`Material::alpha`] this is view-dependent and keeps the\nspecular lobe, which is the whole difference between a transparent\nobject and a faded one: a water surface seen edge-on reflects the sky\nand hides its bottom, and seen from overhead it does neither. The\napproximation is a Fresnel lerp back toward opaque at grazing angles,\nwith the diffuse term scaled by `1 - transmission` (light that went\nthrough did not come back). There is no refraction and no\nscene-color sampling, so what is behind the surface is not bent or\ntinted by its thickness.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "type": {
                "const": "Material",
                "type": "string"
              },
              "uv_offset": {
                "default": [
                  0.0,
                  0.0
                ],
                "items": {
                  "format": "float",
                  "type": "number"
                },
                "maxItems": 2,
                "minItems": 2,
                "type": "array"
              },
              "uv_scale": {
                "default": [
                  1.0,
                  1.0
                ],
                "description": "Tiling for every map on this material: UV × `uv_scale` + `uv_offset`.\nSampling repeats on both axes, so `[20, 20]` is twenty tiles.",
                "items": {
                  "format": "float",
                  "type": "number"
                },
                "maxItems": 2,
                "minItems": 2,
                "type": "array"
              }
            },
            "required": [
              "type"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "A sun: parallel light with no falloff.\n\nThe light shines down the entity's local **−Z**, taken from its\n`Transform` — the same convention the camera uses, so aiming a light is\naiming a camera. With no `Transform` the light travels toward −Z\n(horizontally); a noon sun is `\"rotation\": [-90, 0, 0]`.\n\nAt most one per scene (`multiple_directional_lights`). A scene with **no**\nlight components at all gets a documented fallback rig (sun + ambient); a\nscene with any light component gets exactly what it wrote — absent means\noff.",
            "properties": {
              "color": {
                "default": [
                  1.0,
                  1.0,
                  1.0
                ],
                "description": "Linear RGB chromaticity, each component in `[0, 1]`. Magnitude lives\nin `intensity`.",
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "intensity": {
                "default": 1.0,
                "description": "Unitless multiplier, `>= 0`, unbounded above: intensity 2 is twice as\nbright, and a white light at 1.0 on a white surface head-on reads\nwhite.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "type": {
                "const": "DirectionalLight",
                "type": "string"
              }
            },
            "required": [
              "type"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "A flat, non-directional fill: `albedo * color * intensity`, added to the\nlit result. Exists because a sun-only scene renders back faces pure black,\nand a black region in a screenshot tells an agent nothing.\n\nAt most one per scene (`multiple_ambient_lights`).",
            "properties": {
              "color": {
                "default": [
                  1.0,
                  1.0,
                  1.0
                ],
                "description": "Linear RGB, each component in `[0, 1]`.",
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "intensity": {
                "default": 0.05000000074505806,
                "description": "`>= 0`.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "type": {
                "const": "AmbientLight",
                "type": "string"
              }
            },
            "required": [
              "type"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "A local light that shines in every direction from its entity's position,\nfalling off with distance (M17).\n\nUnlike the sun, there may be **many** per scene — up to\n[`MAX_POINT_LIGHTS`], beyond which the scene is invalid\n(`too_many_point_lights`) rather than silently missing the extras. It has no\norientation, so its `Transform.rotation` and `scale` are ignored; only\n`position` is read. Point lights do not cast shadows (the engine has one\nshadow map, and it belongs to the sun) and are not attenuated by fog.\n\nThis is the component that lets a fire light the ground around it: its\n`intensity` and `color` are in the curated script API\n(`world.light_intensity` / `set_light_intensity` / `light_color` /\n`set_light_color`), so the same flicker signal that drives a flame's\nemission rate can drive the light it casts.\n\nPresence counts as \"the scene lit itself\": a scene whose only light is a\n`PointLight` gets no fallback sun, same as with any other light component.",
            "properties": {
              "color": {
                "default": [
                  1.0,
                  1.0,
                  1.0
                ],
                "description": "Linear RGB, each component in `[0, 1]`.",
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "intensity": {
                "default": 1.0,
                "description": "Brightness at one unit of distance. `>= 0`.\n\nFalloff is inverse-square, so this is the value the surface of a sphere\none metre away receives — which makes `intensity` comparable to\n`DirectionalLight.intensity` at exactly that distance and four times\ndimmer at two metres. A campfire is a few units; a candle is a fraction.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "range": {
                "default": 10.0,
                "description": "Distance in world units at which the light reaches exactly zero. `> 0`.\n\nInverse-square falloff never truly reaches zero, so a range is what\nkeeps a light local: the physical curve is multiplied by a window that\nsmoothly closes at `range`. Without it, every light in a scene would\ncontribute a little to every surface, and a lantern in one room would\nlift the black level of the next.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "type": {
                "const": "PointLight",
                "type": "string"
              }
            },
            "required": [
              "type"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "A simulated rigid body (M8). Requires a `Transform`; a **dynamic** body\nalso requires a `Collider` (`missing_collider` — it would fall forever\nthrough everything).\n\nSimulation state is derived, never authoritative: this component is the\ninitial conditions, and `engine simulate --bake` writes the evolved values\nback as ordinary scene text.",
            "properties": {
              "angular_damping": {
                "default": 0.0,
                "description": "`>= 0`.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "angular_velocity": {
                "default": [
                  0.0,
                  0.0,
                  0.0
                ],
                "description": "**Degrees per second**, `[x, y, z]` — the same units and axis order as\n`Transform.rotation`, for the same reason: the agent that writes\n`\"rotation\": [0, 45, 0]` writes `[0, 90, 0]` for a half-turn per\nsecond. Converted to rad/s only at the physics-backend boundary.",
                "items": {
                  "format": "float",
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "body": {
                "$ref": "#/$defs/BodyKind"
              },
              "can_sleep": {
                "default": true,
                "description": "Allow the solver to put this body to sleep once it settles.",
                "type": "boolean"
              },
              "ccd": {
                "default": false,
                "description": "Continuous collision detection, for small fast bodies that would\notherwise tunnel.",
                "type": "boolean"
              },
              "gravity_scale": {
                "default": 1.0,
                "description": "Multiplier on scene gravity. `>= 0`.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "linear_damping": {
                "default": 0.0,
                "description": "`>= 0`.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "linear_velocity": {
                "default": [
                  0.0,
                  0.0,
                  0.0
                ],
                "items": {
                  "format": "float",
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "locked_rotations": {
                "default": [
                  false,
                  false,
                  false
                ],
                "description": "Lock rotation around the `[x, y, z]` world axes. A vehicle locks\n`[true, false, true]`: yaw stays free for steering while contacts can\nno longer pitch or roll it over.",
                "items": {
                  "type": "boolean"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "type": {
                "const": "RigidBody",
                "type": "string"
              }
            },
            "required": [
              "type",
              "body"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "Collision geometry (M8). Requires a `Transform`. With no `RigidBody` on\nthe entity, this is static collision geometry — the common case for\nground planes and walls.\n\nOne flat object discriminated by `shape` (the shape `jq` and an LLM\nhandle best): `cuboid` uses `half_extents`, `sphere` uses `radius`,\n`capsule` (Y-axis) uses `half_height` + `radius`; `trimesh` and\n`convex_hull` take their geometry from `asset`, or from the entity's own\n`Mesh` when `asset` is absent — the collider matches what the screenshot\nshows, by construction. Validation enforces which fields each shape\nrequires and forbids — the file format is the contract, the flat Rust\nstruct is how it stays walkable by the schema-driven validator and the\neditor's generated inspector.\n\n`Transform.scale` scales the shape when the physics world is built — a\ncube scaled 2x collides 2x big, which is what the screenshot shows.\nNonuniform scale on a round shape has no physics representation and is a\nvalidation error, never a silent approximation (mesh shapes scale\nper-vertex, so nonuniform is fine there).\n\nLayers: `layers` names the collision layers this collider belongs to,\n`collides_with` restricts which layers it interacts with. Both absent\nmeans \"collide with everything\" — exactly the pre-layer behavior. Two\ncolliders interact only if each one's `collides_with` (or absence)\nadmits a layer the other belongs to. Layer names are scene-local\nstrings; a scene may use at most 32 distinct names.",
            "properties": {
              "asset": {
                "description": "`trimesh` and `convex_hull` only: the mesh whose geometry to collide\nas (`builtin:` or a `.gltf`/`.glb` path relative to the scene file).\nAbsent, the entity's own `Mesh.asset` is used — the common case; a\nmesh shape with neither is `collider_missing_mesh`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "collides_with": {
                "description": "Only interact with colliders belonging to these layers. Absent =\ninteract with everything. Empty is an error — omit the field\ninstead.",
                "items": {
                  "type": "string"
                },
                "type": [
                  "array",
                  "null"
                ]
              },
              "density": {
                "default": 1.0,
                "description": "Mass comes from `density` x shape volume. `> 0`.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "friction": {
                "default": 0.5,
                "description": "`>= 0`.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "half_extents": {
                "description": "`cuboid` only. Each component `> 0`.",
                "items": {
                  "format": "float",
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": [
                  "array",
                  "null"
                ]
              },
              "half_height": {
                "description": "`capsule` only: half the cylindrical section's height. `> 0`.",
                "format": "float",
                "type": [
                  "number",
                  "null"
                ]
              },
              "layers": {
                "description": "Collision layers this collider is a member of. Absent = member of\nevery layer. Empty is an error — omit the field instead.",
                "items": {
                  "type": "string"
                },
                "type": [
                  "array",
                  "null"
                ]
              },
              "offset": {
                "default": [
                  0.0,
                  0.0,
                  0.0
                ],
                "description": "Local offset of the shape from the entity's transform origin.",
                "items": {
                  "format": "float",
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "radius": {
                "description": "`sphere` and `capsule`. `> 0`.",
                "format": "float",
                "type": [
                  "number",
                  "null"
                ]
              },
              "restitution": {
                "default": 0.0,
                "description": "Bounciness, `[0, 1]`. When two colliders touch, the **larger** of\nthe two restitutions applies (max-combine), so a bouncy ball bounces\non a plain floor exactly as its own value says.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "sensor": {
                "default": false,
                "description": "Sensors detect overlaps (trace events) but exert no forces.",
                "type": "boolean"
              },
              "shape": {
                "$ref": "#/$defs/ColliderShapeKind"
              },
              "type": {
                "const": "Collider",
                "type": "string"
              }
            },
            "required": [
              "type",
              "shape"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "Breaks into pre-authored fragments (M14) — on a hard enough collision,\ninside an explosion, or when a script calls `world.break_entity`.\n\nOn break the entity is replaced, after that step's physics, by one\ndynamic-body entity per fragment (`Parent.frag0`, `Parent.frag1`, …),\neach inheriting the parent's `Material` and motion. Fragments are\nordinary entities afterwards: they render, trace, and bake like anything\nelse.",
            "properties": {
              "dust": {
                "description": "Throw the material's burst of dust, sawdust, glitter or sparks at the\nbreak (M44). Default **on**, and **does nothing without a `material`**\n— there is no generic dust, only stone's and wood's and glass's and\nmetal's.\n\nThe burst is a `ParticleEmitter` entity named `Parent.dust`, spawned at\nthe impact point and sized to the object, which despawns itself once\nits last particle dies. It is an ordinary entity while it lives: it\ntraces, it bakes, and a script can find it by name.",
                "type": "boolean"
              },
              "fragments": {
                "description": "What the entity becomes. At least one.",
                "items": {
                  "$ref": "#/$defs/Fragment"
                },
                "minItems": 1,
                "type": "array"
              },
              "impulse_threshold": {
                "description": "Contact impulse, in kg·m/s (≈ mass x closing speed), at or above\nwhich a collision breaks this entity. **Absent means collisions\nnever break it** — only scripts and explosions do. Impulse rather\nthan force so the number survives a `timestep_hz` change. `> 0`.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": [
                  "number",
                  "null"
                ]
              },
              "material": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/FractureMaterial"
                  },
                  {
                    "type": "null"
                  }
                ],
                "description": "What this is made of (M43), which decides how the pieces behave once\nthey are pieces: how hard they scatter away from the impact, how much\nthey tumble, and what their surfaces are like.\n\n**Absent is M14 unchanged** — fragments take the parent's motion and\nnothing else, on `friction: 0.5, restitution: 0.0`. A material is also\nwhat `engine fracture` generates shard geometry for, but the two halves\nare independent: a wooden crate that breaks into authored boxes still\nscatters like wood."
              },
              "type": {
                "const": "Breakable",
                "type": "string"
              }
            },
            "required": [
              "type",
              "fragments"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "Plays an animation clip against scene time (M9), or against ground\ncovered (M32).\n\n`clip` is a relative path to a property clip (`*.anim.json`), or a\n`path#ClipName` glTF fragment naming a skeletal clip in the entity's own\nmesh file (M30). A player in the file is playing — there is no play/pause\nruntime state.\n\nThe clock is scene time by default, which keeps the pose a pure function\nof (files, time). Setting `stride` swaps that clock for **distance\ntravelled**, and `phase` is where the clip has got to: still a field in\nthe file, so nothing moves into hidden state.",
            "properties": {
              "clip": {
                "type": "string"
              },
              "looping": {
                "default": true,
                "description": "Wrap by clip duration; when false, clamp to the final pose.",
                "type": "boolean"
              },
              "phase": {
                "default": 0.0,
                "description": "How far this player has got, in **cycles** of its clip, when `stride`\ndrives it (M32). Ignored otherwise.\n\nCycles rather than seconds so the locomotion system needs no clip\nduration to advance it: one step covering `d` metres adds `d / stride`,\nand nothing has to open the clip file to know that. The engine writes\nit back every fixed step and the change-based bake splices it, so where\na character is in its stride survives a bake the same way where it is\nstanding does. A looping player's phase is reduced into `[0, 1)` as it\nis stored.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "speed": {
                "default": 1.0,
                "description": "Time multiplier; local time = `t * speed + start_offset`, or\n`phase * speed + start_offset` when `stride` is set.",
                "format": "float",
                "type": "number"
              },
              "start_offset": {
                "default": 0.0,
                "format": "float",
                "type": "number"
              },
              "stride": {
                "default": 0.0,
                "description": "Metres of ground one **cycle** of this clip covers (M32).\n\n`0` — the default — is the M9 behaviour: scene time drives the clip.\nAbove zero the clip is driven by the entity's horizontal displacement\ninstead, advancing `distance / stride` cycles per fixed step, which is\nwhat stops a walk cycle sliding when the character's speed changes.\n`engine list-joints` measures the right value off the clip itself.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "type": {
                "const": "AnimationPlayer",
                "type": "string"
              }
            },
            "required": [
              "type",
              "clip"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "Gameplay logic as data (M10): a Rhai script run once per fixed step.\n\n`source` is a relative `.rhai` path defining `fn step(world, step)`.\nScripts mutate the world through a small registered API and never invent\nstate of their own — baked output after a scripted run is an ordinary\nscene file.",
            "properties": {
              "source": {
                "type": "string"
              },
              "type": {
                "const": "Script",
                "type": "string"
              }
            },
            "required": [
              "type",
              "source"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "One raycast-suspension wheel of a vehicle (M12).\n\nSits on its own entity — the wheel's visual — and names its chassis in\n`vehicle` (an entity with a **dynamic** `RigidBody` + `Collider`). The\nengine groups every wheel naming the same chassis into one raycast\nvehicle: each wheel casts a ray down its suspension, a spring/damper\npushes the chassis, and a tire friction model drives and grips at the\ncontact point. The wheel entity's own `Transform` is **written by\nphysics** every step — suspension compression, steering yaw, and axle\nspin — so screenshots show wheels that steer, roll, and bounce. Author\nits rest pose; simulation owns it afterwards. A wheel entity must not\ncarry its own `RigidBody` or `Collider` (`wheel_with_physics`) — the\nchassis owns all collision.\n\nControl fields (`engine_force`, `brake`, `steering`) are runtime inputs\nthe same way `RigidBody.linear_velocity` is: scripts write them via\n`world.set_engine_force(...)` and friends, and physics reads them each\nstep.",
            "properties": {
              "brake": {
                "default": 0.0,
                "description": "Braking strength, `>= 0`. A runtime input scripts write each step\n(`world.set_brake`).",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "engine_force": {
                "default": 0.0,
                "description": "Drive force along the wheel's rolling direction, in newtons.\nPositive is the chassis's forward (−Z); negative reverses. A runtime\ninput scripts write each step (`world.set_engine_force`).",
                "format": "float",
                "type": "number"
              },
              "friction_slip": {
                "default": 10.5,
                "description": "Tire traction: how much forward/braking impulse the contact patch\ntransmits before it slips, as a multiple of the suspension load.\nLarger = grippier; too large flips the vehicle under hard braking.\n`>= 0`.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "max_suspension_force": {
                "default": 30000.0,
                "description": "Hard cap on the suspension force, in newtons (not mass-scaled).\n`>= 0`.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "offset": {
                "default": [
                  0.0,
                  0.0,
                  0.0
                ],
                "description": "Suspension attachment point in the chassis's local frame, in meters\n(rotated with the chassis, **not** multiplied by its\n`Transform.scale`). The suspension ray starts here and points down\nthe chassis's local −Y.",
                "items": {
                  "format": "float",
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "radius": {
                "default": 0.30000001192092896,
                "description": "Wheel radius in meters. `> 0`.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "side_friction_stiffness": {
                "default": 1.0,
                "description": "Multiplier on the tire's sideways grip. `1.0` = full lateral\nfriction; lower values let the vehicle drift. `>= 0`.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "steering": {
                "default": 0.0,
                "description": "Steering angle in **degrees** about the chassis's up axis; positive\nsteers left. A runtime input scripts write each step\n(`world.set_steering`).",
                "format": "float",
                "type": "number"
              },
              "suspension_compression": {
                "default": 2.5,
                "description": "Damping while the spring compresses, per kilogram of chassis mass.\n`sqrt(stiffness)` is critical damping; ~0.5× that is a comfortable\ncar. `>= 0`.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "suspension_damping": {
                "default": 3.5,
                "description": "Damping while the spring extends (rebound), per kilogram of chassis\nmass. Usually a little higher than `suspension_compression`. `>= 0`.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "suspension_rest_length": {
                "default": 0.30000001192092896,
                "description": "Suspension spring rest length in meters: how far below `offset` the\nwheel center hangs when the spring is neither compressed nor\nstretched. `>= 0`.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "suspension_stiffness": {
                "default": 24.0,
                "description": "Spring stiffness **per kilogram of chassis mass** (the backend\nmultiplies by mass, so the same value suspends a light or heavy\nchassis identically). Higher = stiffer. `> 0`. Static sag per wheel\nis roughly `9.81 / (4 * stiffness)` meters — keep\n`suspension_travel` above that.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "suspension_travel": {
                "default": 0.25,
                "description": "Maximum travel from rest length, in meters, both directions. `>= 0`.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "type": {
                "const": "Wheel",
                "type": "string"
              },
              "vehicle": {
                "description": "Name of the chassis entity this wheel belongs to. Must be a\n*different* entity with a dynamic `RigidBody` and a `Collider`.",
                "type": "string"
              }
            },
            "required": [
              "type",
              "vehicle"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "A screen-space text label (M12): lines of the built-in 8×8 pixel font,\ndrawn over the 3D scene after lighting, independent of any camera.\n\nNeeds no `Transform` — placement is `anchor` + `offset` in framebuffer\npixels, which is what the agent sees in the PNG. Text is always opaque\nand never anti-aliased, so a HUD glyph is bit-exact in baselines. Glyphs\noutside the font's coverage render as a filled box: visibly wrong in the\nscreenshot, never a panic.\n\nM31 adds `parent`, `visible` and `stretch` (shared by every element in the\nfamily), plus `align`, `wrap` and `line_gap`. Every one defaults to the M12\nbehaviour: no parent means a child of the viewport placed by exactly the\nM12 anchor arithmetic, and `wrap: 0` means the single unwrapped line it has\nalways been.",
            "properties": {
              "align": {
                "$ref": "#/$defs/HudTextAlign",
                "default": "left",
                "description": "Alignment of each line within the text's own box, which differs from\nthe box only when the text is stretched or wrapped."
              },
              "anchor": {
                "$ref": "#/$defs/HudAnchor",
                "default": "top_left"
              },
              "color": {
                "default": [
                  1.0,
                  1.0,
                  1.0
                ],
                "description": "Linear RGB in `[0, 1]`, like every color in the engine; encoded to\nsRGB when the overlay is rasterized.",
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "line_gap": {
                "default": 0.0,
                "description": "Extra pixels between lines, on top of the glyph cell.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "offset": {
                "default": [
                  0.0,
                  0.0
                ],
                "description": "Pixels inward from `anchor` (see [`HudAnchor`]). Inside a `row` or\n`column` parent this is a nudge on top of the computed position rather\nthan the position itself.",
                "items": {
                  "format": "float",
                  "type": "number"
                },
                "maxItems": 2,
                "minItems": 2,
                "type": "array"
              },
              "parent": {
                "description": "The name of an entity carrying a [`HudPanel`] to place this inside.\nAbsent (the default) means a child of the viewport.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "size": {
                "default": 16.0,
                "description": "Glyph height in pixels, `>= 4`. The 8×8 font renders at integer\nscale `max(1, round(size / 8))`, so `16` means exactly 2× glyphs.",
                "format": "float",
                "minimum": 4.0,
                "type": "number"
              },
              "stretch": {
                "default": [
                  false,
                  false
                ],
                "description": "Fill the parent's content box on `[x, y]`, ignoring this element's own\nsize on that axis. Two booleans rather than a `\"fill\"` string in a\nnumeric field, which would break the schema-driven walk.",
                "items": {
                  "type": "boolean"
                },
                "maxItems": 2,
                "minItems": 2,
                "type": "array"
              },
              "text": {
                "description": "The label. `\\n` breaks a line explicitly; `wrap` breaks it\nautomatically. Scripts may rewrite it via `world.set_hud_text` — an\nempty string is a legal rest value for a script-driven readout.",
                "type": "string"
              },
              "type": {
                "const": "HudText",
                "type": "string"
              },
              "visible": {
                "default": true,
                "description": "Drawn and hit-testable when true (the default). Hiding a panel hides\nits whole subtree — one boolean is how a menu opens and closes.",
                "type": "boolean"
              },
              "wrap": {
                "default": 0.0,
                "description": "Wrap width in pixels; `0` (the default) is no wrapping. Breaks on\nspaces — a word longer than `wrap` overflows rather than splitting,\nsince a mid-word break in a fixed-width font reads as corruption.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              }
            },
            "required": [
              "type",
              "text"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "A screen-space solid rectangle (M12): the primitive behind health bars,\nspeed bars, and backdrops. Drawn with the panels and images, before all\n`HudText`, file order within the class.\n\nM31 adds only the shared `parent`/`visible`/`stretch`; a rect stays the\nflat script-driven bar it has always been.",
            "properties": {
              "anchor": {
                "$ref": "#/$defs/HudAnchor",
                "default": "top_left"
              },
              "color": {
                "default": [
                  1.0,
                  1.0,
                  1.0
                ],
                "description": "Linear RGB in `[0, 1]`.",
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "offset": {
                "default": [
                  0.0,
                  0.0
                ],
                "description": "Pixels inward from `anchor` (see [`HudAnchor`]). Inside a `row` or\n`column` parent this is a nudge on top of the computed position.",
                "items": {
                  "format": "float",
                  "type": "number"
                },
                "maxItems": 2,
                "minItems": 2,
                "type": "array"
              },
              "opacity": {
                "default": 1.0,
                "description": "`[0, 1]`; `1` (the default) replaces the pixel exactly, fractions\nalpha-blend on the GPU (deterministic per adapter, like every\nbaseline).",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "parent": {
                "description": "The name of an entity carrying a [`HudPanel`] to place this inside.\nAbsent (the default) means a child of the viewport.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "size": {
                "description": "`[width, height]` in pixels, each `>= 0` — zero is legal so a\nscript-driven bar can be empty. Scripts resize via\n`world.set_hud_rect_size` or `world.set_hud_size`. Ignored on an axis\nwhere `stretch` is true.",
                "items": {
                  "format": "float",
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 2,
                "minItems": 2,
                "type": "array"
              },
              "stretch": {
                "default": [
                  false,
                  false
                ],
                "description": "Fill the parent's content box on `[x, y]`, ignoring `size` on that\naxis — the full-screen dim backdrop, and the bar spanning a column.",
                "items": {
                  "type": "boolean"
                },
                "maxItems": 2,
                "minItems": 2,
                "type": "array"
              },
              "type": {
                "const": "HudRect",
                "type": "string"
              },
              "visible": {
                "default": true,
                "description": "Drawn and hit-testable when true (the default).",
                "type": "boolean"
              }
            },
            "required": [
              "type",
              "size"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "A screen-space container that lays its children out (M31).\n\nThis is the component that removes hand-computed pixel offsets. Children\nname it in their `parent`; `layout` decides whether they are stacked in a\n`row`, a `column`, or placed `free` by their own anchors relative to this\npanel's content box.\n\n**Absent `width`/`height` means hug contents** — the panel is exactly its\nchildren's extent plus `padding`. That is the default because it is the\ncase that makes a dialog authorable: the box follows the text instead of\nthe text being fitted to a box someone solved by hand.\n\n`opacity` defaults to **0**, so a bare `HudPanel` is an invisible layout\ngroup; set it and the same component is the dialog's backdrop. One\ncomponent rather than a container plus a rect whose size would have to be\nkept in agreement with it.",
            "properties": {
              "align": {
                "$ref": "#/$defs/HudAlign",
                "default": "start",
                "description": "Cross-axis alignment of children in a `row`/`column`."
              },
              "anchor": {
                "$ref": "#/$defs/HudAnchor",
                "default": "top_left"
              },
              "color": {
                "default": [
                  1.0,
                  1.0,
                  1.0
                ],
                "description": "Background colour, linear RGB in `[0, 1]`. Only visible at\n`opacity > 0`.",
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "gap": {
                "default": 0.0,
                "description": "Pixels between children along the main axis of a `row`/`column`.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "height": {
                "description": "Fixed height in pixels. Absent hugs the children.",
                "format": "float",
                "minimum": 0.0,
                "type": [
                  "number",
                  "null"
                ]
              },
              "layout": {
                "$ref": "#/$defs/HudLayout",
                "default": "free"
              },
              "offset": {
                "default": [
                  0.0,
                  0.0
                ],
                "description": "Pixels inward from `anchor` (see [`HudAnchor`]).",
                "items": {
                  "format": "float",
                  "type": "number"
                },
                "maxItems": 2,
                "minItems": 2,
                "type": "array"
              },
              "opacity": {
                "default": 0.0,
                "description": "`[0, 1]`, defaulting to **0** — an invisible layout group.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "padding": {
                "default": 0.0,
                "description": "Uniform inset in pixels between this panel's edge and its content box.\nPer-side padding is the obvious next field and costs nothing to add\nlater; M12's \"no z field until something needs it\" applies.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "parent": {
                "description": "The name of another [`HudPanel`] entity to nest inside.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "stretch": {
                "default": [
                  false,
                  false
                ],
                "description": "Fill the parent's content box on `[x, y]`, ignoring `width`/`height`\nand hug sizing on that axis.",
                "items": {
                  "type": "boolean"
                },
                "maxItems": 2,
                "minItems": 2,
                "type": "array"
              },
              "type": {
                "const": "HudPanel",
                "type": "string"
              },
              "visible": {
                "default": true,
                "description": "Drawn and hit-testable when true (the default). A hidden panel hides\nits whole subtree.",
                "type": "boolean"
              },
              "width": {
                "description": "Fixed width in pixels. Absent hugs the children.",
                "format": "float",
                "minimum": 0.0,
                "type": [
                  "number",
                  "null"
                ]
              }
            },
            "required": [
              "type"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "A screen-space textured rectangle (M31): icons, logos, framed panels.\n\n`texture` is a PNG relative to the scene file, loaded through the same\n`TextureSource` and `(asset, space)` cache the material system uses, in\nsRGB — so `texture_too_large` fires from `validate`, before a device\nexists. Only the base level is read: the overlay draws at most one\ndestination pixel per texel band and never minifies below it, so a mip\nlevel selection would have no correct answer at this scale.\n\nSampling is **nearest-neighbour**, written out in `engine-core` like every\nother generator here — a render sits under a baseline, so the filter is a\nformat contract, and nearest is exactly reproducible where a bilinear\nfilter is a float-rounding question.",
            "properties": {
              "anchor": {
                "$ref": "#/$defs/HudAnchor",
                "default": "top_left"
              },
              "offset": {
                "default": [
                  0.0,
                  0.0
                ],
                "description": "Pixels inward from `anchor` (see [`HudAnchor`]).",
                "items": {
                  "format": "float",
                  "type": "number"
                },
                "maxItems": 2,
                "minItems": 2,
                "type": "array"
              },
              "opacity": {
                "default": 1.0,
                "description": "`[0, 1]`, multiplied onto the texture's own alpha.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "parent": {
                "description": "The name of an entity carrying a [`HudPanel`] to place this inside.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "size": {
                "description": "`[width, height]` in destination pixels. Ignored on an axis where\n`stretch` is true.",
                "items": {
                  "format": "float",
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 2,
                "minItems": 2,
                "type": "array"
              },
              "slice": {
                "default": [
                  0.0,
                  0.0,
                  0.0,
                  0.0
                ],
                "description": "Nine-slice insets in **source** pixels, `[left, top, right, bottom]`.\nThe default `[0, 0, 0, 0]` is a plain stretch. Corners are copied\n1:1, edges tile along their axis and the centre tiles both ways —\ntiling rather than stretching, because tiling at nearest is exact\nwhere stretching at nearest is a moiré pattern.",
                "items": {
                  "format": "float",
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 4,
                "minItems": 4,
                "type": "array"
              },
              "stretch": {
                "default": [
                  false,
                  false
                ],
                "description": "Fill the parent's content box on `[x, y]`, ignoring `size`.",
                "items": {
                  "type": "boolean"
                },
                "maxItems": 2,
                "minItems": 2,
                "type": "array"
              },
              "texture": {
                "description": "A `.png` path relative to the scene file (invariant 3).",
                "type": "string"
              },
              "tint": {
                "default": [
                  1.0,
                  1.0,
                  1.0
                ],
                "description": "Multiplies the decoded texel in linear space, so one grey frame\ntexture serves a red panel and a blue one — the material system's\nauthoring rule for `albedo_map`, here for the same reason.",
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "type": {
                "const": "HudImage",
                "type": "string"
              },
              "visible": {
                "default": true,
                "description": "Drawn and hit-testable when true (the default).",
                "type": "boolean"
              }
            },
            "required": [
              "type",
              "texture",
              "size"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "Makes the HUD element on its own entity clickable (M31).\n\nCarries no geometry: the hit box is that element's laid-out rectangle. An\nentity with a `HudInteract` and no `HudPanel`/`HudRect`/`HudImage`/\n`HudText` is `hud_interact_without_element`.\n\nA separate component rather than an `interactive: true` flag on each of\nfour components, because the flag would be four fields that must stay in\nagreement, and because the tints belong next to it.\n\nThe tints multiply the element's own colour (clamped to `[0, 1]` after\nmultiplying) and default to `[1, 1, 1]` — no change — so adding a\n`HudInteract` moves no pixel until a cursor arrives. They exist so the\nordinary case, a button that lights up under the pointer, needs no script\nat all; anything richer is a script writing colours.",
            "properties": {
              "disabled": {
                "default": false,
                "description": "Excluded from hit-testing when true, so it never hovers, presses or\nclicks — and never blocks what is under it either.",
                "type": "boolean"
              },
              "hover_tint": {
                "default": [
                  1.0,
                  1.0,
                  1.0
                ],
                "description": "Colour multiplier while the cursor is over this element. Unbounded\nabove — a hover tint brightens.",
                "items": {
                  "format": "float",
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "press_tint": {
                "default": [
                  1.0,
                  1.0,
                  1.0
                ],
                "description": "Colour multiplier while a button is held down on this element.",
                "items": {
                  "format": "float",
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "type": {
                "const": "HudInteract",
                "type": "string"
              }
            },
            "required": [
              "type"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "A deterministic particle emitter (M13): smoke, sparks, dust — and, with the\nM17 fields, fire.\n\nParticles spray from the entity's position into a cone around its local\n**−Z** — the same aiming convention the camera and lights use, so a rising\nsmoke plume is `\"rotation\": [90, 0, 0]`. Each particle is born with\n`start_*` values and reaches its `end_*` values as it dies; the billboard\nrenders as a soft unlit disc, alpha-blended over the scene.\n\nParticles are simulation state on the fixed step clock, advanced by\n`--steps` exactly like physics (`--time` poses animations, it does not\nadvance particles). The state is derived and disposable — never baked,\nnever traced — and the `seed` makes it reproducible: same file, same\nsteps, same particles, so screenshots of smoke diff-render bit-exactly.\n\n# The fire fields (M17)\n\nA cone of identical sprites reads as a sparkler, not a flame. Five fields\nfix that, and **every one of them defaults to the M13 behaviour** — a\npre-M17 emitter is byte-identical, down to which random numbers it draws:\n`radius` spreads emission over a fire bed, the three `*_jitter` fields\nbreak the lockstep of a population born identical, `turbulence` makes\nflames lick, `stretch` turns round puffs into motion-aligned tongues and\nember streaks, and `blend: \"additive\"` makes overlapping flame get\n*brighter* instead of merely more opaque, which is the single biggest\ndifference between fire and orange smoke.\n\n**Random draws are skipped, not defaulted, when a field is off.** Every\njitter field consumes the emitter's RNG only when it is non-zero, in the\ndocumented order (direction → disc → speed → size → lifetime →\nturbulence). That is what keeps every baseline blessed before M17 exact,\nand it is the same discipline as not consuming the RNG on a capped spawn.",
            "properties": {
              "acceleration": {
                "default": [
                  0.0,
                  0.0,
                  0.0
                ],
                "description": "World-space acceleration applied to every live particle, units/s².\nParticles ignore physics; this is where buoyancy (`[0, 1, 0]` for\nsmoke) or gravity (`[0, -9.81, 0]` for sparks) comes from.",
                "items": {
                  "format": "float",
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "blend": {
                "$ref": "#/$defs/ParticleBlend",
                "default": "alpha",
                "description": "How the billboard combines with the scene behind it. `\"alpha\"` (default)\noccludes what it covers; `\"additive\"` adds light to it, so overlapping\nsprites brighten toward white and a thin one is nearly invisible.\n\nFire, sparks, and magic are additive; smoke, dust, and spray are not.\nAdditive particles draw after every alpha-blended one, so a flame glows\n*through* the smoke above it rather than being occluded by it — the\napproximation is deliberate and it is what a real fire looks like,\nsince that smoke is genuinely lit from below."
              },
              "despawn_when_done": {
                "default": false,
                "description": "Despawn this emitter's entity once its `duration` is up **and** its\nlast particle has died (M44). Default `false`.\n\nThis is what keeps a scene that breaks twenty crates from accumulating\ntwenty spent emitters. Requires `duration` — without one the emitter\nnever finishes and the flag could never fire, which is\n`emitter_never_finishes` rather than a field that quietly does nothing.\n\nThe despawn is an ordinary one: it traces as `despawned` like any\nother, and the entity is simply gone from a bake taken afterwards.",
                "type": "boolean"
              },
              "drag": {
                "default": 0.0,
                "description": "Velocity damping per second. `>= 0`; 0 means none.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "duration": {
                "description": "Seconds of **emission** after this emitter's first step (M44). `> 0`.\n\n**Absent is M13: it emits forever.** Present, it is a burst — the puff\nof dust a break throws, the spark shower off an impact — and when the\ntime is up emission stops exactly as `rate: 0` does: no new particles,\nand the live ones finish their own `lifetime`.\n\nIt is measured from when the *system* first sees the emitter, not from\nscene time, so an emitter that arrives mid-run (a break's dust) burns\nits burst from the moment it exists rather than being born already\nspent. That start is derived state, like particle positions: it is not\nbaked, so a scene baked mid-puff reloads and puffs again — the same\nthing the tour's fire already does, for the same reason.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": [
                  "number",
                  "null"
                ]
              },
              "end_alpha": {
                "default": 0.0,
                "description": "Opacity at death, `[0, 1]`. The default 0 fades particles out.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "end_color": {
                "default": [
                  0.699999988079071,
                  0.699999988079071,
                  0.699999988079071
                ],
                "description": "Linear RGB at death, each component in `[0, 1]`.",
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "end_size": {
                "default": 0.20000000298023224,
                "description": "Billboard half-size at death. `> 0`; larger than `start_size` grows\n(smoke), smaller shrinks (sparks).",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "lifetime": {
                "default": 2.0,
                "description": "Seconds each particle lives. `> 0`.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "lifetime_jitter": {
                "default": 0.0,
                "description": "Fraction by which each particle's lifespan varies from `lifetime`,\n`[0, 1)`. The most valuable of the three for fire: identical lifespans\nput the whole population's fade at one height, which draws a flat top\non the flame, and varying them is what makes it ragged.\n\nA particle's lifespan is fixed **at birth**, so animating `lifetime`\nmid-run retimes new particles and leaves live ones alone.",
                "format": "float",
                "maximum": 0.99,
                "minimum": 0.0,
                "type": "number"
              },
              "max_particles": {
                "default": 1024,
                "description": "Cap on live particles; spawns beyond it are dropped (deterministically).\n`[1, 65536]`.",
                "format": "uint32",
                "maximum": 65536,
                "minimum": 1,
                "type": "integer"
              },
              "radius": {
                "default": 0.0,
                "description": "Radius of the disc particles are born on, in world units, centred on the\nentity and lying in the plane **perpendicular to the aim** (local XY).\n`>= 0`; 0 (default) emits from the single point.\n\nA campfire is a bed of coals, not a nozzle: emitting a flame from one\npoint gives a cone with a visible apex, and no amount of `spread` hides\nit. Like `Wheel.offset`, this is rotated by the entity's rotation but\n**not** scaled by its `Transform.scale`.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "rate": {
                "default": 10.0,
                "description": "Particles spawned per second. `>= 0`; 0 stops emission (existing\nparticles live out their lifetime).",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "seed": {
                "default": 0,
                "description": "Seed for the emitter's random spray directions. Same seed, same steps →\nidentical particles; give two otherwise-identical emitters different\nseeds so they don't emit in lockstep.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "size_jitter": {
                "default": 0.0,
                "description": "Fraction by which each particle's size varies from `start_size` /\n`end_size`, `[0, 1]`. One multiplier scales both, so a particle keeps\nits authored growth curve and only its scale changes.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "speed": {
                "default": 1.0,
                "description": "Initial speed in units/second along the sampled cone direction. `>= 0`.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "speed_jitter": {
                "default": 0.0,
                "description": "Fraction by which each particle's launch speed varies from `speed`,\n`[0, 1]`: 0.3 means every particle draws uniformly from ±30%.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "spread": {
                "default": 15.0,
                "description": "Cone half-angle in degrees around local −Z. `[0, 180]`: 0 is a beam,\n90 a hemisphere, 180 a full sphere.",
                "format": "float",
                "maximum": 180.0,
                "minimum": 0.0,
                "type": "number"
              },
              "start_alpha": {
                "default": 1.0,
                "description": "Opacity at birth, `[0, 1]`.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "start_color": {
                "default": [
                  0.699999988079071,
                  0.699999988079071,
                  0.699999988079071
                ],
                "description": "Linear RGB at birth, each component in `[0, 1]`. Unlit.",
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "start_size": {
                "default": 0.20000000298023224,
                "description": "Billboard half-size in world units at birth. `> 0`.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "stretch": {
                "default": 0.0,
                "description": "Seconds of travel to stretch the billboard along its own velocity.\n`>= 0`; 0 (default) keeps sprites round.\n\nThe sprite grows along its direction of motion by the distance the\nparticle covers in this many seconds, so the same value stretches a\nfast ember into a streak and leaves slow smoke nearly circular — which\nis what a camera does. Flame tongues want a little (~0.05), sparks want\na lot (~0.2).",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "turbulence": {
                "default": 0.0,
                "description": "Strength of a swirling world-space acceleration field, units/s².\n`>= 0`; 0 (default) means none.\n\nThis is what separates a flame from a jet of orange dots: hot gas is\nunstable and curls. The field is smooth value noise sampled along each\nparticle's own path (see `turbulence_scale`), so a particle wanders\ncoherently rather than vibrating — and it is generated by an integer\nhash specified in this repo, so it is exactly as reproducible as the\nspray directions.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "turbulence_scale": {
                "default": 1.0,
                "description": "Size in world units of one cell of the turbulence field. `> 0`.\n\nSmall values curl the flame tightly (candle), large ones sway the whole\nplume (bonfire in wind). Roughly: a particle's direction changes over\nthis distance travelled.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "type": {
                "const": "ParticleEmitter",
                "type": "string"
              }
            },
            "required": [
              "type"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "A procedurally generated tree (M19): trunk, recursive branches, and leaves,\ngrown from a `seed` into geometry the renderer draws like any other mesh.\n\nThe tree is built in entity-local space with its **base at the origin,\ngrowing along +Y**, so `\"position\": [x, 0, z]` plants it on flat ground and\n`Transform.scale` sizes the whole thing. It replaces the entity's `Mesh`\nrather than accompanying one (`tree_with_mesh`): the component *is* the\ngeometry.\n\n# Two draws, two materials\n\nA tree needs bark and foliage, and one `Material` cannot be both. The\nentity's own `Material` is the **bark**; the leaves get [`Tree::leaf_color`]\nand [`Tree::leaf_roughness`], and are always opaque and non-metallic. That\nlast part is a constraint, not an oversight: every leaf of one tree is a\nsingle mesh, and the blended pass sorts whole draws, so translucent leaves\ncould not be sorted against each other and would visibly z-fight.\n\n# Randomness\n\nEverything jittered is drawn from a private xorshift seeded by `seed`,\nconsumed in a fixed order (a branch draws its own wander, then recurses into\nits children in index order, then scatters its leaves), so two trees that\ndiffer only in `seed` are different trees and the *same* tree is the same\nmesh forever — which is what lets a forest sit under a pinned baseline.\n`jitter` is the master dial for how much variation there is at all; `0`\ngrows a rigid diagram of a tree.\n\n# Cost\n\nVertices scale as `(branches · whorl)^levels · segments · sides`, and a\ntree that would blow past the budget is a validation error\n(`tree_too_complex`) rather than a hang. `levels: 2` with the default\nbranching is ~1k vertices; `levels: 3` is ~6k. Generation happens once per\ndistinct parameter set and is cached, so a forest of nine trees is nine\nmeshes no matter how many frames render.",
            "properties": {
              "branch_angle": {
                "default": 48.0,
                "description": "Angle in degrees between a child branch and its parent at the\nattachment. Small angles sweep up (poplar), large ones reach out\n(oak) or hang down (spruce, with negative `tropism`). `[0, 180]`.",
                "format": "float",
                "maximum": 180.0,
                "minimum": 0.0,
                "type": "number"
              },
              "branch_start": {
                "default": 0.3499999940395355,
                "description": "Fraction of a branch's length that carries no children — the bare\ntrunk under the crown. `[0, 1)`.",
                "format": "float",
                "maximum": 0.99,
                "minimum": 0.0,
                "type": "number"
              },
              "branch_twist": {
                "default": 137.5,
                "description": "Degrees of rotation around the parent between successive attachment\npoints. The default is the golden angle, which is what real phyllotaxis\nconverges on and what stops branches from stacking into visible rows.",
                "format": "float",
                "type": "number"
              },
              "branches": {
                "default": 5,
                "description": "Attachment points spaced along each parent branch. `[0, 16]`.",
                "format": "uint32",
                "maximum": 16,
                "minimum": 0,
                "type": "integer"
              },
              "crook": {
                "default": 8.0,
                "description": "Degrees of random wander per meter of branch. This is the gnarl: `0`\ngrows perfectly straight poles, `8` is a healthy tree, `25` is an old\nolive. `>= 0`.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "flare": {
                "default": 0.4000000059604645,
                "description": "Extra radius at the very foot of the trunk, as a fraction: `0.4` makes\nthe base 40% wider than the trunk above it. Root flare is most of what\nsays \"grown\" rather than \"placed\", and it is at eye level. `>= 0`.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "height": {
                "default": 6.0,
                "description": "Trunk length in meters, before `Transform.scale`. `> 0`.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "jitter": {
                "default": 0.25,
                "description": "How much every jittered quantity — branch length, radius, angle, leaf\nplacement — varies, as a fraction. `[0, 1)`; `0` is a diagram.",
                "format": "float",
                "maximum": 0.99,
                "minimum": 0.0,
                "type": "number"
              },
              "leaf": {
                "$ref": "#/$defs/TreeLeaf",
                "default": "blade",
                "description": "What hangs on the outermost branches: `\"blade\"` (a folded leaf, the\ndefault), `\"cluster\"` (a foliage blob — cheaper per unit of cover, and\nwhat conifer sprays and distant trees want), or `\"none\"` for a bare\nwinter or dead tree."
              },
              "leaf_color": {
                "default": [
                  0.09000000357627869,
                  0.25999999046325684,
                  0.07999999821186066
                ],
                "description": "Foliage albedo, linear RGB in `[0, 1]` — the leaves' half of the tree's\nappearance, the entity's `Material` being the bark's.",
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "leaf_roughness": {
                "default": 0.75,
                "description": "Foliage roughness, `[0, 1]`. Leaves are waxy, not matte: a little\nspecular is what separates them from felt.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "leaf_size": {
                "default": 0.30000001192092896,
                "description": "Length of one leaf, or diameter of one cluster, in meters. `> 0`.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "leaves_per_branch": {
                "default": 6,
                "description": "Leaves scattered along each outermost branch. `[0, 64]`; `0` is\nequivalent to `\"leaf\": \"none\"`.",
                "format": "uint32",
                "maximum": 64,
                "minimum": 0,
                "type": "integer"
              },
              "length_falloff": {
                "default": 0.3499999940395355,
                "description": "How much shorter children get toward the parent's tip, `[0, 1]`: `0`\nmakes every child the same length (a round crown), `0.8` makes the top\nones stubs (the cone of a conifer).",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "length_ratio": {
                "default": 0.6200000047683716,
                "description": "Child length as a fraction of its parent's. `(0, 2]`.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "maximum": 2.0,
                "type": "number"
              },
              "levels": {
                "default": 2,
                "description": "How many generations of branches hang off the trunk. `0` is a bare\npole; `2` reads as a tree; `3` is a good hero tree; `4` is expensive.\n`[0, 4]`.",
                "format": "uint32",
                "maximum": 4,
                "minimum": 0,
                "type": "integer"
              },
              "radius_ratio": {
                "default": 0.6000000238418579,
                "description": "Child radius as a fraction of the parent's radius where it attaches.\n`(0, 1]`.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "maximum": 1.0,
                "type": "number"
              },
              "seed": {
                "default": 0,
                "description": "Seeds every random draw. Two trees with the same parameters and\ndifferent seeds are different trees; the same seed always regrows the\nsame tree.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "segments": {
                "default": 5,
                "description": "Lengthwise segments per branch — how finely a branch can curve. `[1, 16]`.",
                "format": "uint32",
                "maximum": 16,
                "minimum": 1,
                "type": "integer"
              },
              "sides": {
                "default": 6,
                "description": "Radial sides of a branch tube. `[3, 16]`; `5`–`6` is plenty, since bark\nsilhouettes read from the branching, not the cross-section.",
                "format": "uint32",
                "maximum": 16,
                "minimum": 3,
                "type": "integer"
              },
              "taper": {
                "default": 0.11999999731779099,
                "description": "Radius at a branch's tip as a fraction of its base. `[0, 1]`; small\nvalues give the sharp taper of a young shoot.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "tropism": {
                "default": 8.0,
                "description": "Degrees per meter that a branch curves toward the sky as it grows.\nPositive lifts tips toward the light (most trees), negative lets\ngravity droop them (spruce, willow). This is what makes branches curve\nrather than point.\n\nIt does not apply to the trunk, whose line is [`Tree::crook`] alone: a\nnegative tropism on the trunk is unstable — a degree of crook tips it\noff vertical and the bend then compounds until the tree grows sideways.",
                "format": "float",
                "type": "number"
              },
              "trunk_radius": {
                "default": 0.1599999964237213,
                "description": "Trunk radius at the ground, in meters, before `Transform.scale`. `> 0`.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "type": {
                "const": "Tree",
                "type": "string"
              },
              "whorl": {
                "default": 1,
                "description": "Branches emitted at each attachment point **on the trunk**, spread\nevenly around it. `1` (default) gives the alternate, spiralling\narrangement of most broadleaf trees; `4`–`5` gives the whorls of a\nconifer, where a ring of limbs leaves the trunk at one height.\n\nDeeper levels are always alternate, which is both what a real conifer's\nlimbs carry and what keeps the geometry finite — a whorl compounding at\nevery level multiplies the tree by itself. `[1, 8]`.",
                "format": "uint32",
                "maximum": 8,
                "minimum": 1,
                "type": "integer"
              }
            },
            "required": [
              "type"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "A body of water: an ocean, a lake, a pond, a canal.\n\nThe entity owns its own surface geometry — a tessellated unit grid sized by\n`Transform.scale`, exactly like a scaled `builtin:plane` — so a `Water`\nentity carries **no** `Mesh` and no `Material` (`water_with_mesh`). One\nsurface is one entity: sixteen tiles pretending to be a pond is what this\ncomponent exists to delete, and their seams are visible in any screenshot.\n\nWaves are evaluated in **world space** in the vertex stage, which has two\nconsequences worth knowing: scaling a surface never stretches its waves, and\ntwo adjacent water entities at the same height share one continuous surface\nfor free.\n\nShading is water-specific rather than a `Material`: sky reflection with a\nFresnel-weighted view term, absorption of what is behind the surface with\ndepth (`shallow_color` → `deep_color`), and foam where the water meets\ngeometry or folds at a crest. What it does *not* do is refract — the bed of\na pond is not displaced by the ripples above it.",
            "properties": {
              "crest_foam": {
                "default": 0.0,
                "description": "Foam on the crests, `[0, 1]`; 0 (the default) is off.\n\nDriven by the Gerstner Jacobian — where the surface pinches toward\nfolding, which is exactly where a real wave breaks — so it appears only\non steep waves and needs no second noise field to place it.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "deep_color": {
                "default": [
                  0.009999999776482582,
                  0.05000000074505806,
                  0.07999999821186066
                ],
                "description": "Linear RGB of water far deeper than `depth_fade`. Each component\n`[0, 1]`.",
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "density": {
                "default": 1000.0,
                "description": "Density of the fluid in kg/m³, `> 0`. Fresh water is 1000 (the default),\nsea water about 1025.\n\nThe **only** field here that nothing renders. It is what a [`Buoyancy`]\nbody weighs the water it displaces against, and it lives on the lake\nrather than on the boat because it is a property of the fluid: two hulls\nin one pond disagreeing about how dense the water is would not be a knob,\nit would be a bug. The authoring knob for \"this floats higher\" already\nexists and is [`Collider::density`], in the same unit.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "depth_fade": {
                "default": 2.5,
                "description": "Metres of water the view has to pass through to reach `deep_color` and\nfull `opacity`. `> 0`.\n\nBeer-Lambert absorption against the depth of whatever is behind the\nsurface, so the same water is clear at the edge of a pond and opaque in\nthe middle — which is most of how a surface reads as *deep* rather than\nas a coloured pane. A clear alpine lake is 6 or more; a silty pond is\nunder 1.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "detail": {
                "default": 0.5,
                "description": "Strength of the per-pixel ripple normals, `[0, 1]`.\n\nSmall-scale roughness the grid is far too coarse to carry as geometry,\nperturbing the normal and nothing else. Per line of code this is the\nbiggest single difference between \"blue glass\" and \"water\", because it\nis what breaks the sun and the sky into glitter between the vertices.\nNothing physical may depend on it — no buoyancy, no collision.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "detail_scale": {
                "default": 0.6000000238418579,
                "description": "Size of one ripple cell in metres. `> 0`. Around 0.5 reads as wind\ntexture on a lake; 3 or more as a swell the grid did not resolve.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "foam_color": {
                "default": [
                  0.8600000143051147,
                  0.8999999761581421,
                  0.9200000166893005
                ],
                "description": "Linear RGB of both foam kinds, each component `[0, 1]`. Foam is\nscattered light, so it is opaque where it appears.",
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "ior": {
                "default": 1.0,
                "description": "Index of refraction, `1.0` (the default) being no bending at all — the\npre-M27 surface, which absorbs and tints what is behind it but never\nmoves it. Range `[1, 3]`; water is 1.33.\n\nUnlike [`Material::ior`] this needs no companion `thickness`: the shader\nalready measures how far the view ray travels through the body to reach\nthe bed, so the bend scales with the water's own depth and a pond bends\nits bed most where it is deepest. It also cannot change how *deep* the\nwater looks — absorption stays [`Water::depth_fade`]'s job — so it can\nbe turned on in a tuned scene without re-tuning it.",
                "format": "float",
                "maximum": 3.0,
                "minimum": 1.0,
                "type": "number"
              },
              "opacity": {
                "default": 0.9399999976158142,
                "description": "How opaque deep water becomes, `[0, 1]`. 1 hides its bed completely\nhowever clear the shallows are.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "roughness": {
                "default": 0.05999999865889549,
                "description": "Surface roughness, `[0, 1]`, meaning what `Material.roughness` means.\n\nWater is smooth, but not 0: a mirror-tight sun highlight is a single\nblown-out pixel that aliases as the camera moves, and the reflected sky\ncarries most of the look anyway.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "segments": {
                "default": 64,
                "description": "Quads per axis across the surface. `[1, 512]`.\n\nThis is the resolution the *waves* are drawn at, and it is the one field\nworth thinking about: a wave needs roughly eight quads per wavelength to\nlook like a wave rather than a fold, so a 14 m pond carrying 2 m chop\nwants ~64, and a 200 m ocean carrying 3 m chop cannot be drawn by any\ngrid this component will generate. Detail *normals* are per pixel and\ncost nothing, which is why the glitter survives a coarse grid even\nthough the silhouette does not.",
                "format": "uint32",
                "maximum": 512,
                "minimum": 1,
                "type": "integer"
              },
              "shallow_color": {
                "default": [
                  0.09000000357627869,
                  0.20000000298023224,
                  0.20999999344348907
                ],
                "description": "Linear RGB of water one `depth_fade` deep or less — the colour at the\nshoreline. Each component `[0, 1]`.",
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "shore_foam": {
                "default": 0.0,
                "description": "Width in metres of the foam line where the surface meets geometry.\n`>= 0`; 0 (the default) is off.\n\nThis is the shoreline, and it is also the waterline on anything standing\nin the water: it comes from the depth behind the surface, so a boat, an\nice block and the bank all get one without being marked up.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "type": {
                "const": "Water",
                "type": "string"
              },
              "waves": {
                "default": [],
                "description": "The waves summed to shape the surface, at most\n[`MAX_WAVES`](crate::water::MAX_WAVES) of them. Empty (the default)\nleaves the surface flat: a mirror, which is what a sheltered pond at\ndawn actually looks like.",
                "items": {
                  "$ref": "#/$defs/Wave"
                },
                "maxItems": 8,
                "type": "array"
              }
            },
            "required": [
              "type"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "A cloud: a cumulus, a raft of stratocumulus, a storm anvil, a torn wisp.\n\nA recipe rather than a mesh reference, like [`Tree`] — the engine grows it\ninto a cluster of lobes, each of which grows smaller lobes on itself, seeded\nso two clouds with the same parameters and different seeds are different\nclouds. The entity owns that geometry, sized by `Transform.scale` like a\nwater surface, so a `Cloud` entity carries **no** `Mesh` and **no**\n`Material` (`cloud_with_mesh`). A cloud is not a GGX surface: what a\n`Material` describes, `color` / `shade_color` / `density` / `feather`\ndescribe instead.\n\nNon-uniform scale is the normal case, not an edge case — `scale: [24, 12,\n24]` is what makes a cumulus wider than it is tall, and it oblates the lobes\nwith it.\n\nShading is three cheap stand-ins for multiple scattering, none of which is\nvolumetric: wrapped diffuse between `shade_color` and `color`, a forward-\nscattering silver lining when the camera looks toward the sun, and an alpha\nthat fades toward each lobe's own silhouette. Clouds do not cast shadows\n(the engine has one shadow cascade and it is fitted to the camera, not to a\ncloud at altitude) and are not lit by `PointLight`s.",
            "properties": {
              "children": {
                "default": 3,
                "description": "Lobes grown on each lobe of the previous generation. `[0, 8]`.",
                "format": "uint32",
                "maximum": 8,
                "minimum": 0,
                "type": "integer"
              },
              "color": {
                "default": [
                  1.0,
                  0.9800000190734863,
                  0.949999988079071
                ],
                "description": "Linear RGB of the sunlit side. Each component `[0, 1]`.",
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "density": {
                "default": 0.8999999761581421,
                "description": "How opaque the cloud is where it is thickest, `[0, 1]`. Lobes do not\nwrite depth, so overlapping ones accumulate — which is a cheap stand-in\nfor optical depth, and why a wisp wants a much lower value than a storm.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "detail": {
                "default": 2,
                "description": "Icosphere subdivisions per lobe: 12, 42, 162 or 642 vertices. `[0, 3]`.\nThis is the quality dial, and `2` is plenty for anything at cloud\ndistance.",
                "format": "uint32",
                "maximum": 3,
                "minimum": 0,
                "type": "integer"
              },
              "drift": {
                "default": [
                  0.0,
                  0.0,
                  0.0
                ],
                "description": "World-space metres per second the cloud travels, evaluated against the\nscene clock (`--time`, else `steps / timestep_hz`) — so a drifting sky\nis as reproducible as a wave, and a script is not needed to move it.\n\nThe cloud's *shape* never changes with time. Regenerating lobes per\nframe would mint a new mesh every frame and defeat the renderer's upload\ncache; in this engine, generated geometry is made once.",
                "items": {
                  "format": "float",
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "drift_wrap": {
                "default": 0.0,
                "description": "Metres after which a drifting cloud recycles to where it started. `>= 0`;\n`0` (the default) lets it drift away for good.\n\nWrapping *teleports* the cloud, so it wants to be wider than the view or\nfar enough out that fog has already eaten it before it jumps.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "feather": {
                "default": 3.0,
                "description": "How crisp the cloud's edges are, `[0, 8]`. Alpha follows\n`1 - (1 - facing)^feather` as the surface turns away from the camera, so\n**higher is crisper** and low values are wispy: 1 fades the whole\nsurface proportionally, 3 keeps the body opaque and thins only the last\nfew degrees before the silhouette.\n\nIt is doing two jobs. A real cloud's silhouette is where it thins out,\nnot where its geometry stops — and the same fade is what hides the\nboundaries *between* two interpenetrating lobes, since each of them\nvanishes exactly where its surface turns away.",
                "format": "float",
                "maximum": 8.0,
                "minimum": 0.0,
                "type": "number"
              },
              "flatten": {
                "default": 0.0,
                "description": "How much the cloud sits on a flat base, `[0, 1]`. `0` is a puffball with\nlobes scattered through its whole box; `1` seats every lobe on the base\nplane and folds what hangs below onto it.\n\nA cumulus has a flat bottom because condensation begins at one altitude,\nwhich is why every fair-weather cloud in a field shares a base — it is\nthe cheapest of this component's cues and one of the most legible.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "jitter": {
                "default": 0.30000001192092896,
                "description": "How much every jittered quantity — lobe radius, placement, child size —\nvaries, as a fraction. `[0, 1)`; `0` is a diagram.",
                "format": "float",
                "maximum": 0.99,
                "minimum": 0.0,
                "type": "number"
              },
              "levels": {
                "default": 2,
                "description": "Generations of smaller lobes piled on the base ones. `[0, 3]`: `0` is a\ncluster of plain spheres, `2` reads as cauliflower, `3` is expensive.",
                "format": "uint32",
                "maximum": 3,
                "minimum": 0,
                "type": "integer"
              },
              "lobe_ratio": {
                "default": 0.550000011920929,
                "description": "Child lobe radius as a fraction of its parent's, `(0, 1]`. This is the\ndial that makes the silhouette detailed at more than one scale — at 1\nevery lobe is the same size and the cloud reads as popcorn.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "maximum": 1.0,
                "type": "number"
              },
              "lobe_size": {
                "default": 0.41999998688697815,
                "description": "Diameter of a base lobe as a fraction of the cloud's own size, `(0, 1]`.\nLarge values give a few fat billows, small ones a curdled texture.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "maximum": 1.0,
                "type": "number"
              },
              "lobes": {
                "default": 6,
                "description": "Lobes in the base cluster, spread over the footprint on a golden-angle\nspiral. `[1, 32]`; a handful reads as one cumulus, a dozen or more as a\nraft.",
                "format": "uint32",
                "maximum": 32,
                "minimum": 1,
                "type": "integer"
              },
              "rise": {
                "default": 0.3499999940395355,
                "description": "How strongly child lobes are biased toward the sky, `[0, 1]`. A cumulus\nis a convection cell: its detail is on top, where the air is still\nrising, and its underside is smooth. At `0` children scatter in every\ndirection and the cloud reads as a sea urchin.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "seed": {
                "default": 0,
                "description": "Seeds every random draw. Two clouds with the same parameters and\ndifferent seeds are different clouds; the same seed always regrows the\nsame cloud.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "shade_color": {
                "default": [
                  0.41999998688697815,
                  0.46000000834465027,
                  0.5799999833106995
                ],
                "description": "Linear RGB of the self-shadowed side, each component `[0, 1]`.\n\nBlue-grey rather than grey by default, and that is the point: the\nunderside of a cloud is lit by the sky above it, not by the sun it is\nhiding from. Darkening this toward slate is most of what makes a storm.",
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "type": {
                "const": "Cloud",
                "type": "string"
              },
              "wobble": {
                "default": 0.11999999731779099,
                "description": "Smooth radial distortion of each lobe, as a fraction of its radius.\n`[0, 1)`; a little is what stops a lobe from reading as a ball.",
                "format": "float",
                "maximum": 0.99,
                "minimum": 0.0,
                "type": "number"
              }
            },
            "required": [
              "type"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "A patch of ground: displaced terrain with a procedurally shaded surface\n(M22).\n\nThe entity carries **no** [`Mesh`] and **no** [`Material`] — `Terrain` owns\nboth, like [`Water`] — and having either is `terrain_with_mesh`. Geometry is\na tessellated unit grid sized by `Transform.scale`, displaced by an fBm\nheight field; `Transform.scale.y` multiplies that displacement, so [`height`]\nis what you get at scale 1.\n\nHeights are sampled in **world** XZ, so two patches with the same fields meet\nseamlessly and moving one moves it *through* the field rather than dragging\nits hills along.\n\nUnlike water's waves, the height field is evaluated on the **CPU**: terrain\ndoes not animate, so the surface is generated once and cached, and there is\nexactly one implementation for the renderer, the collider (a `trimesh`\n`Collider` with no asset uses this surface) and\n`world.terrain_height(name, x, z)` to share. Appearance is the opposite —\nper-pixel, in the shader, mirrored by nothing, which is what licenses detail\nfar finer than the grid.\n\n[`height`]: Terrain::height",
            "properties": {
              "basins": {
                "default": [],
                "description": "Circular depressions cut into the height field, in **world** XZ (M42).\n\nEmpty (the default) is M22's landscape exactly — the noise is the whole\nanswer. This is the only way to say \"the ground dips *here*\": everything\nelse about a patch describes a texture of relief rather than a place, and\na body of water needs a place.\n\nCut inside [`height_at`](crate::terrain::height_at), so the drawn\nsurface, its normals, the `trimesh` collider standing on it,\n`world.terrain_height`, a `Road` that follows this patch and a `Meadow`\ngrowing on it all follow from the one implementation.",
                "items": {
                  "$ref": "#/$defs/TerrainBasin"
                },
                "type": "array"
              },
              "bump": {
                "default": 0.30000001192092896,
                "description": "Per-pixel normal perturbation from the detail noise, `[0, 1]`.\n\nBumpiness with no displacement behind it — nothing physical may depend on\nit, which is what allows detail far finer than the grid or the collider.\nIt fades with view distance, because sub-pixel normal variation aliases\ninto sparkle that reads as broken rather than as low quality (the lesson\nwater's detail ripples already paid for).",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "color_variation": {
                "default": 0.25,
                "description": "How strongly the detail noise modulates the blended albedo, `[0, 1]`.\n\nThe cure for the one-flat-colour look: even a single-layer terrain stops\nbeing a sheet of paint. Past ~0.5 the ground reads as camouflage.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "feature_scale": {
                "default": 40.0,
                "description": "Metres across one cell of the largest noise octave. `> 0`.\n\nThe size of the big rolling features, and the field that decides whether\na patch reads as dunes, as pasture or as foothills. Well under\n`segments`-worth of quads and the grid cannot resolve it; far larger than\nthe patch and the ground reads as a tilted plane.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "height": {
                "default": 2.0,
                "description": "Metres of displacement at full amplitude, `>= 0`.\n\nThe field is normalised to `[-1, 1]` before scaling, so this is the peak\nabove (and below) the entity's own Y, and **adding octaves adds detail\nrather than altitude**. 0 is a flat patch, which is a legitimate thing to\nask for and is still shaded by the layer system.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "layers": {
                "default": [],
                "description": "The materials the surface is painted with, at most\n[`MAX_TERRAIN_LAYERS`](crate::terrain::MAX_TERRAIN_LAYERS), blended by\nheight and slope.\n\nEmpty (the default) paints the whole surface with\n[`TerrainLayer`]'s own defaults, so a bare `{\"type\": \"Terrain\"}` is a\nplausible grassy patch rather than an error or a blank.",
                "items": {
                  "$ref": "#/$defs/TerrainLayer"
                },
                "maxItems": 4,
                "type": "array"
              },
              "octaves": {
                "default": 4,
                "description": "How many octaves of noise are summed. `[1, 8]`.\n\nEach octave halves the feature size and scales its amplitude by\n[`persistence`](Terrain::persistence). Past about 5 the added detail is\nfiner than the grid can carry and only costs generation time.",
                "format": "uint32",
                "maximum": 8,
                "minimum": 1,
                "type": "integer"
              },
              "persistence": {
                "default": 0.5,
                "description": "Amplitude multiplier per octave, `[0, 1]`.\n\nLow values give smooth swells; near 1 gives a rough, noisy surface with\nno clear large-scale shape. 0.5 is the usual landscape.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "seed": {
                "default": 0,
                "description": "Chooses the landscape. Any change reshapes every hill.\n\nThe noise hash is written out in this crate rather than pulled from a\ndependency, so a given seed means the same terrain across upgrades — a\nterrain render sits under a `diff-render` baseline, which makes this a\nformat contract.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "segments": {
                "default": 128,
                "description": "Quads per axis across the patch. `[1, 512]`.\n\nThe resolution the *relief* is drawn and collided at. What matters is\nmetres per quad against [`feature_scale`](Terrain::feature_scale): a\n200 m patch at 192 has one vertex per metre, which resolves a 40 m hill\ncomfortably and a 3 m hummock not at all. Surface *detail* is per pixel\nand does not care.",
                "format": "uint32",
                "maximum": 512,
                "minimum": 1,
                "type": "integer"
              },
              "texture_scale": {
                "default": 4.0,
                "description": "Metres across one cell of the surface-detail noise. `> 0`.\n\nThe scale of the mottling within a layer and of the fingers along the\nboundary between two — the *texture*, as opposed to the relief. Around a\nfew metres reads as ground cover seen from standing height.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "type": {
                "const": "Terrain",
                "type": "string"
              },
              "warp": {
                "default": 0.0,
                "description": "Domain warp: how far the field is dragged sideways before it is summed,\nas a fraction of [`feature_scale`](Terrain::feature_scale). `[0, 2]`;\n0 (the default) is off.\n\nTwo lines of arithmetic, and the largest single difference between \"fBm\"\nand \"landscape\". Unwarped fBm is isotropic blobs; warping shears them\ninto ridges and valleys that read as though water once ran over them.\nPast ~1 the surface starts to look smeared.",
                "format": "float",
                "maximum": 2.0,
                "minimum": 0.0,
                "type": "number"
              }
            },
            "required": [
              "type"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "A road: a circuit, a street, a mountain pass.\n\nThe entity owns its surface geometry — one continuous ribbon generated from\nthe centerline — so a `Road` entity carries **no** `Mesh` and no `Material`\n(`road_with_mesh`), the same rule [`Water`] follows and for the same reason.\n\nAsphalt, shoulders and the embankment skirt are all the **same** triangles.\nThat is not a saving, it is the point: road and shoulder as two surfaces at\nslightly different heights build a ledge along the asphalt edge, and a wheel\nthat drops off it wedges against the step and stops the car dead. There is\nno seam between segments either, because consecutive cross-sections share\ntheir vertices.\n\nPhysics reads the same mesh: a `Collider` with `\"shape\": \"trimesh\"` on a\nroad entity needs no `asset` and no `Mesh`, because the road is the\ngeometry. Friction and collision layers stay on the `Collider`, where every\nother surface in the engine keeps them.",
            "properties": {
              "auto_bank": {
                "default": 0.0,
                "description": "Bank every corner by this many degrees at a radius of\n[`auto_bank_radius`](Self::auto_bank_radius), scaling as `1 / radius`\nfor anything wider and capped here for anything tighter (M40). `0` —\nthe default — leaves the road flat. `>= 0`.\n\n**The engine picks the sign**, raising the outside of each turn, and\nthat is the whole reason this field exists next to\n[`RoadPoint::bank`]. Which way a corner banks is a fact about the\nwinding of the polygon it belongs to; deriving it per corner by hand is\nhow a circuit ends up with one corner that throws the car off.\n\nIt is a shape knob, not a physical one. Banking a corner for a *speed*\nis `atan(v² / gR)` and wants a velocity a road cannot know — the answer\nfor a kart and an F1 car differ by 20°.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "auto_bank_radius": {
                "default": 20.0,
                "description": "The corner radius [`auto_bank`](Self::auto_bank) is quoted at, in\nmetres. `> 0`.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "bank_color": {
                "default": [
                  0.20000000298023224,
                  0.17000000178813934,
                  0.12999999523162842
                ],
                "description": "Linear RGB of the embankment below the shoulder. Each component\n`[0, 1]`.",
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "closed": {
                "default": false,
                "description": "Join the last point back to the first. A closed road is a circuit: the\npolygon's exterior angles sum to one turn, so it shuts without a solver.",
                "type": "boolean"
              },
              "color": {
                "default": [
                  0.09000000357627869,
                  0.09000000357627869,
                  0.10000000149011612
                ],
                "description": "Linear RGB of the asphalt. Each component `[0, 1]`.",
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "follow_blend": {
                "default": 30.0,
                "description": "How far either side of a [`pinned`](RoadPoint::pin_height) point its\nheight correction fades out, in metres. `>= 0`.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "follow_smoothing": {
                "default": 12.0,
                "description": "How far along the road the sampled ground is averaged, in metres.\n`>= 0`, and `0` reproduces the terrain exactly, bumps and all.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "follow_terrain": {
                "default": null,
                "description": "Name of a `Terrain` entity this road rides on (M40). Absent — the\ndefault — is a road whose heights are absolute, which is every road\nbefore M40.\n\nWith it set, each point's `y` becomes a **clearance above the ground**\nand the terrain is sampled at every centerline sample rather than only\nat the authored points, then smoothed over\n[`follow_smoothing`](Self::follow_smoothing) metres — terrain is noise,\nand a road that reproduces it is undrivable.\n\nThe road does **not** carve the terrain: a height field stays the one\nsource of truth for where the ground is. Where the smoothed road passes\nbelow the real ground the ground pokes through it, and the answers are\nmore clearance, a [`pinned`](RoadPoint::pin_height) point, or more\nsmoothing.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "grain": {
                "default": 0.0,
                "description": "How strongly the asphalt is grained, `[0, 1]`. `0` — the default — is\nthe flat colour M23 shipped (M40).\n\nA value-noise field in the road's own `(u, v)`, so it follows every\ncurve and grade the way the markings do, perturbing albedo and\nroughness. Deliberately not a normal perturbation: grain that tilts the\nshading normal sparkles under a moving camera at exactly the frequencies\na deterministic renderer should not be producing.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "grain_scale": {
                "default": 0.3499999940395355,
                "description": "Size of one grain cell, in metres. `> 0`.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "markings": {
                "$ref": "#/$defs/RoadMarkings",
                "default": {
                  "center_dash": 3.0,
                  "center_gap": 6.0,
                  "center_width": 0.0,
                  "color": [
                    0.8799999952316284,
                    0.8799999952316284,
                    0.8799999952316284
                  ],
                  "edge_inset": 0.10000000149011612,
                  "edge_width": 0.14000000059604645,
                  "kerb_color": [
                    0.800000011920929,
                    0.10000000149011612,
                    0.07999999821186066
                  ],
                  "kerb_max_radius": 0.0,
                  "kerb_stripe": 1.399999976158142,
                  "kerb_width": 0.8999999761581421,
                  "start_line": false,
                  "start_line_at": 0.0,
                  "start_line_width": 0.699999988079071
                },
                "description": "What is painted on it."
              },
              "points": {
                "default": [
                  {
                    "bank": null,
                    "pin_height": false,
                    "position": [
                      0.0,
                      0.0,
                      0.0
                    ],
                    "radius": 0.0,
                    "width": null
                  },
                  {
                    "bank": null,
                    "pin_height": false,
                    "position": [
                      0.0,
                      0.0,
                      -20.0
                    ],
                    "radius": 0.0,
                    "width": null
                  }
                ],
                "description": "The centerline, corner by corner, in the order they are driven. At\nleast two points; a closed road needs at least three.",
                "items": {
                  "$ref": "#/$defs/RoadPoint"
                },
                "maxItems": 256,
                "type": "array"
              },
              "roughness": {
                "default": 0.9200000166893005,
                "description": "Surface roughness, `[0, 1]`, meaning what `Material.roughness` means.\nAsphalt is nearly matte; wet asphalt is not.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "segment_angle": {
                "default": 5.0,
                "description": "Most degrees of arc one segment may cover through a corner. `>= 0.5`.\n\nThis is the resolution knob that matters: a corner cut every 5° is\nsmooth to drive and to look at, and the cost is linear in the road's\nlength rather than quadratic like a grid's.",
                "format": "float",
                "minimum": 0.5,
                "type": "number"
              },
              "segment_length": {
                "default": 2.0,
                "description": "Longest a straight segment may be before the road is cut again, in\nmetres. `>= 0.25`.",
                "format": "float",
                "minimum": 0.25,
                "type": "number"
              },
              "shoulder": {
                "default": 1.5,
                "description": "Drivable shoulder each side of the asphalt, in metres. `>= 0`.\n\nPart of the same surface, not a second one — see the note above about\nledges.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "shoulder_color": {
                "default": [
                  0.17000000178813934,
                  0.20000000298023224,
                  0.14000000059604645
                ],
                "description": "Linear RGB of the shoulder each side of the asphalt. Each component\n`[0, 1]`.",
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "skirt": {
                "default": 0.6000000238418579,
                "description": "How far the embankment drops below the road's outer edge, in metres.\n`>= 0`.\n\nThis is what stops an elevated road from floating. Set it deeper than\nthe road ever climbs and it simply disappears under the ground plane\nwherever the road is low.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "type": {
                "const": "Road",
                "type": "string"
              },
              "width": {
                "default": 7.0,
                "description": "Width of the asphalt, edge to edge, in metres. `> 0`.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              }
            },
            "required": [
              "type"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "Where roads meet: the patch of asphalt a ribbon cannot be (M40).\n\nA [`Road`] is swept along a curve, which is the wrong primitive for a\ncrossroads — two ribbons crossing leave a hole. A junction is instead the\narea **bounded by the mouths of the roads that reach it**: each arm names a\nroad and which end of it arrives, and the patch stretches to whatever those\nmouths turn out to be.\n\nThe mouths are read off each road's finished surface — the same\n[`RoadSurface`](crate::road::RoadSurface) the renderer draws and physics\nbuilds its trimesh from — so a junction cannot disagree with the road it\njoins about where that road ended, how wide it was there, what height it\nreached, or how far it was banked. That is the `engine road-centerline` rule\napplied inside the engine: nothing re-derives a curve someone else built.\n\n**The junction does not trim its roads.** It would be the tidier result and\nit inverts the ownership every recipe here follows: a road's geometry would\nbecome a function of which junctions happen to name it, and `engine inspect`\non the road would stop predicting the road. The author ends each road at the\njunction's mouth, and because the patch stretches, \"roughly there\" is enough\n— an arm stopping 2 m short simply makes the patch 2 m longer on that side.\n\nLike every other recipe, the entity owns its geometry and so carries **no**\n`Mesh` and no `Material` (`junction_with_mesh`), and a `Collider` with\n`\"shape\": \"trimesh\"` on it takes the patch.",
            "properties": {
              "arms": {
                "default": [],
                "description": "The roads that meet here, in any order — the patch is built in\nrotational order about the mouths whatever order they are listed in. At\nleast two.",
                "items": {
                  "$ref": "#/$defs/JunctionArm"
                },
                "maxItems": 16,
                "type": "array"
              },
              "bank_color": {
                "default": [
                  0.20000000298023224,
                  0.17000000178813934,
                  0.12999999523162842
                ],
                "description": "Linear RGB of the embankment. Each component `[0, 1]`.",
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "color": {
                "default": [
                  0.09000000357627869,
                  0.09000000357627869,
                  0.10000000149011612
                ],
                "description": "Linear RGB of the asphalt. Each component `[0, 1]`.",
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "corner_segments": {
                "default": 6,
                "description": "How finely each of those corners is cut. `>= 1`.",
                "format": "uint32",
                "maximum": 64,
                "minimum": 1,
                "type": "integer"
              },
              "flare": {
                "default": 1.0,
                "description": "How far the corner between two arms reaches out toward where their edges\nwould have crossed, `[0, 1]`.\n\n`1` — the default — is the full flare, a quadratic Bézier through that\nintersection, which is the shape a real corner has. `0` is the straight\nchord from one mouth's corner to the next.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "grain": {
                "default": 0.0,
                "description": "How strongly the asphalt is grained, `[0, 1]` — [`Road::grain`] on a\npatch, so a junction and the roads reaching it can wear the same\nsurface.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "grain_scale": {
                "default": 0.3499999940395355,
                "description": "Size of one grain cell, in metres. `> 0`.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "roughness": {
                "default": 0.9200000166893005,
                "description": "Surface roughness, `[0, 1]`.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "shoulder": {
                "default": 1.5,
                "description": "Shoulder around the patch, in metres — the same surface, for the same\nreason a road's is. `>= 0`.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "shoulder_color": {
                "default": [
                  0.17000000178813934,
                  0.20000000298023224,
                  0.14000000059604645
                ],
                "description": "Linear RGB of the shoulder. Each component `[0, 1]`.",
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "skirt": {
                "default": 0.6000000238418579,
                "description": "How far the embankment drops below the shoulder's outer edge, in metres.\n`>= 0`.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "type": {
                "const": "Junction",
                "type": "string"
              }
            },
            "required": [
              "type"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "Ground cover that grows, seeds and dies on a loop: grass, weeds, wildflowers\nand the dry stand they turn into.\n\nA recipe rather than a mesh reference, like [`Tree`] and [`Cloud`] — the\nengine grows one plant and scatters copies of it over the footprint\n`Transform.scale` gives, so a `Meadow` entity carries **no** `Mesh` and\n**no** `Material` (`meadow_with_mesh`).\n\n**It is the first recipe in this engine whose subject changes shape over\ntime.** A sprout is not a small blade of grass. The resolution is that the\ngeometry is static and the *life cycle lives in the vertex stage*: the plant\nis built once carrying every organ any stage will need, and each organ scales\nto nothing outside the phase window it belongs to. See\n`designs/meadow-design.md`.\n\nThe cycle runs on the scene clock, so it can be sped up:\n[`cycle_length`](Meadow::cycle_length) `: 3.0` runs a whole generation in\nthree seconds. **`0` — the default — freezes the field** at\n[`phase`](Meadow::phase), the way `daylight.day_length: 0` freezes the day:\nmost scenes want a dial, not motion, and a frozen field is reproducible with\nno `--time` at all.\n\nEvery generation **reseeds** rather than regrowing: a plant's position within\nits own cell, its height and its lean all shift a little each time round, so\nthe dead stalk and the sprout that replaces it are not collinear. That costs\none integer hash in the shader and no state anywhere.\n\nA meadow is scenery: no `Collider`, no shadow cast (a 2048² map cannot\nresolve a blade of grass, and what it would record is noise that crawls),\nand no `PointLight` contribution.",
            "properties": {
              "blade_width": {
                "default": 0.007000000216066837,
                "description": "Width of a blade at its base, in metres, `> 0`.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "blades": {
                "default": 5,
                "description": "Blades in one plant. `[1, 12]`; `3`–`5` reads as a tuft.",
                "format": "uint32",
                "maximum": 12,
                "minimum": 1,
                "type": "integer"
              },
              "cycle_length": {
                "default": 0.0,
                "description": "Seconds one full life cycle takes, `>= 0`.\n\n**`0` freezes the field** at [`phase`](Meadow::phase) — the default, and\n`daylight.day_length: 0`'s reasoning exactly.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "density": {
                "default": 24.0,
                "description": "Plants per square metre of footprint, `>= 0`.\n\nThe footprint is `Transform.scale` in XZ, so the plant count is\n`density × area`, rounded up to a square grid. `0` is an empty field,\nwhich is a legitimate thing to animate toward.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "flower_color": {
                "default": [
                  0.4000000059604645,
                  0.3700000047683716,
                  0.17000000178813934
                ],
                "description": "Linear RGB of the flower head, each component `[0, 1]`. What stops the\nweed stage from being nothing but taller grass.",
                "items": {
                  "format": "float",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "type": "number"
                },
                "maxItems": 3,
                "minItems": 3,
                "type": "array"
              },
              "head_size": {
                "default": 0.017999999225139618,
                "description": "Size of the flower and seed heads, in metres, `>= 0`. `0` grows a plant\nthat never flowers.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "height": {
                "default": 0.44999998807907104,
                "description": "Height of a fully grown plant in metres, `> 0`. `Transform.scale.y`\nmultiplies it, the way it multiplies a [`Terrain`]'s relief.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "max_slope": {
                "default": 38.0,
                "description": "Steepest ground grass will grow on, in degrees, `[0, 90]`. Plants on\nsteeper ground are dropped; `90` keeps everything.",
                "format": "float",
                "maximum": 90.0,
                "minimum": 0.0,
                "type": "number"
              },
              "phase": {
                "default": 0.41999998688697815,
                "description": "Where the cycle starts, `[0, 1)` — and where a frozen field sits. The\ndefault is mature green, so `{\"type\": \"Meadow\"}` alone puts a working\nfield of grass in a scene.",
                "format": "float",
                "maximum": 0.999,
                "minimum": 0.0,
                "type": "number"
              },
              "seed": {
                "default": 0,
                "description": "Seeds placement and the template's blades. Two meadows with the same\nparameters and different seeds are different fields.\n\nThe generator's xorshift and the shader's reseed hash are both written\nout in this repo, so a given seed means the same field across dependency\nupgrades — a meadow render sits under a `diff-render` baseline, which\nmakes both a format contract.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "segments": {
                "default": 4,
                "description": "Lengthwise segments per blade — how finely a blade can curve as it leans\nand bends in the wind. `[1, 8]`.\n\nTogether with [`blades`](Meadow::blades) this sets the per-plant\ntriangle count, and the product with the plant count is what\n`meadow_too_complex` bounds.",
                "format": "uint32",
                "maximum": 8,
                "minimum": 1,
                "type": "integer"
              },
              "size_jitter": {
                "default": 0.3499999940395355,
                "description": "How much plant height varies between plants, as a fraction, `[0, 1)`.\n`0` is a lawn.",
                "format": "float",
                "maximum": 0.99,
                "minimum": 0.0,
                "type": "number"
              },
              "splay": {
                "default": 54.0,
                "description": "Degrees the outermost blades splay from vertical, `[0, 90]`. Blade 0\nstays near upright, which is what gives a tuft a centre rather than a\nhole.",
                "format": "float",
                "maximum": 90.0,
                "minimum": 0.0,
                "type": "number"
              },
              "stages": {
                "default": [
                  {
                    "at": 0.0,
                    "color": [
                      0.12999999523162842,
                      0.10000000149011612,
                      0.05000000074505806
                    ],
                    "height": 0.0,
                    "lean": 0.0,
                    "sway": 0.0,
                    "tip_color": [
                      0.1599999964237213,
                      0.12999999523162842,
                      0.07000000029802322
                    ],
                    "width": 0.5
                  },
                  {
                    "at": 0.09000000357627869,
                    "color": [
                      0.1599999964237213,
                      0.3400000035762787,
                      0.07000000029802322
                    ],
                    "height": 0.1599999964237213,
                    "lean": 10.0,
                    "sway": 0.5,
                    "tip_color": [
                      0.3100000023841858,
                      0.550000011920929,
                      0.12999999523162842
                    ],
                    "width": 0.699999988079071
                  },
                  {
                    "at": 0.3100000023841858,
                    "color": [
                      0.05999999865889549,
                      0.1899999976158142,
                      0.03999999910593033
                    ],
                    "height": 0.8600000143051147,
                    "lean": 17.0,
                    "sway": 1.0,
                    "tip_color": [
                      0.17000000178813934,
                      0.4000000059604645,
                      0.09000000357627869
                    ],
                    "width": 1.0
                  },
                  {
                    "at": 0.5299999713897705,
                    "color": [
                      0.09000000357627869,
                      0.20000000298023224,
                      0.05000000074505806
                    ],
                    "height": 1.0,
                    "lean": 21.0,
                    "sway": 0.8999999761581421,
                    "tip_color": [
                      0.30000001192092896,
                      0.36000001430511475,
                      0.10000000149011612
                    ],
                    "width": 1.0499999523162842
                  },
                  {
                    "at": 0.7599999904632568,
                    "color": [
                      0.2199999988079071,
                      0.18000000715255737,
                      0.07000000029802322
                    ],
                    "height": 0.949999988079071,
                    "lean": 27.0,
                    "sway": 0.4000000059604645,
                    "tip_color": [
                      0.6200000047683716,
                      0.5099999904632568,
                      0.1899999976158142
                    ],
                    "width": 0.9200000166893005
                  },
                  {
                    "at": 0.9100000262260437,
                    "color": [
                      0.14000000059604645,
                      0.10999999940395355,
                      0.05999999865889549
                    ],
                    "height": 0.6200000047683716,
                    "lean": 71.0,
                    "sway": 0.25,
                    "tip_color": [
                      0.25999999046325684,
                      0.20999999344348907,
                      0.10999999940395355
                    ],
                    "width": 0.800000011920929
                  }
                ],
                "description": "The life cycle, as keyframes over `phase`. At least two, at most\n[`MAX_GROWTH_STAGES`](crate::meadow::MAX_GROWTH_STAGES), strictly\nincreasing in `at`.\n\nThe default is the full seed → sprout → grass → weeds → dry → collapse\ncycle, so a meadow is worth looking at before anything is authored.",
                "items": {
                  "$ref": "#/$defs/GrowthStage"
                },
                "type": "array"
              },
              "stagger": {
                "default": 0.25,
                "description": "How far plants desync from each other, `[0, 1]`.\n\n`0` marches the whole field in lockstep; `1` spreads offsets across the\nwhole cycle, so every stage is present at every moment and the field\nnever appears to change. A real meadow browns together with variation,\nwhich is why the default is near the low end.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "terrain": {
                "description": "The [`Terrain`] entity this meadow stands on, by name.\n\nEach plant's altitude is sampled from that patch through the same\nfunction `world.terrain_height` and `engine terrain-height` call, so\nthere is one implementation of \"where is the ground\" and nothing to keep\nin agreement. Absent, the field is flat at the entity's own Y.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "type": {
                "const": "Meadow",
                "type": "string"
              },
              "wind": {
                "default": 9.0,
                "description": "How far the wind bends a plant at full sway, in degrees, `>= 0`. `0` is\nstill air.",
                "format": "float",
                "maximum": 90.0,
                "minimum": 0.0,
                "type": "number"
              },
              "wind_direction": {
                "default": 0.0,
                "description": "Which way the wind blows, in degrees — `0` toward −Z, the engine's\nforward convention, shared with `Water`'s wave directions.",
                "format": "float",
                "type": "number"
              },
              "wind_speed": {
                "default": 3.5,
                "description": "How fast gusts travel across the field, in metres per second, `>= 0`.\n\nGusts are a travelling wave, not a per-plant shimmer: sampling the noise\nagainst a moving coordinate is what makes wind cross a meadow visibly.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              }
            },
            "required": [
              "type"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "Puts a skinned character's feet on the terrain under them (M32).\n\nA post-pass over the posed skeleton: each named ankle is moved to the\nground beneath where the clip put it, the joints above it rotate to follow\n(two-bone IK), the hips drop when a leg cannot reach, and the sole tilts to\nthe slope. It runs wherever the pose does, so `engine list-joints --time`\nreports the planted rig and the render draws it — one answer, not two.\n\nThe ground is a `Terrain` entity named in `ground`, and that is a purity\ndecision rather than a convenience: planting against the *physics* world\nwould make the pose a function of the simulation, and the pose being a pure\nfunction of (files, time) is what lets `list-joints` answer at all. The cost\nis that a character cannot stand on a crate — see\n`designs/locomotion-design.md` §5.",
            "properties": {
              "align": {
                "default": 30.0,
                "description": "Degrees the sole may tilt to meet the ground's normal. `0` leaves the\nfoot's animated orientation alone, which is right for a character that\nonly ever walks on the flat.",
                "format": "float",
                "maximum": 90.0,
                "minimum": 0.0,
                "type": "number"
              },
              "feet": {
                "description": "The feet to plant, at most [`MAX_PLANTED_FEET`].",
                "items": {
                  "$ref": "#/$defs/PlantedFoot"
                },
                "type": "array"
              },
              "ground": {
                "description": "The entity carrying the `Terrain` these feet stand on.",
                "type": "string"
              },
              "hips": {
                "description": "The joint lowered when a leg cannot reach its target — the pelvis, in\na humanoid. Absent, the deficit is simply clamped: one foot plants and\nthe other stretches, which is bounded but reads as wrong.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "max_drop": {
                "default": 0.5,
                "description": "How far below the animated ankle a target may be, in metres. This is\nwhat keeps a foot in mid-swing from being dragged to the floor:\nplanting is a *correction*, and a correction with no ceiling is a\ndifferent animation.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "max_lift": {
                "default": 0.5,
                "description": "And how far above it.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "type": {
                "const": "FootPlant",
                "type": "string"
              }
            },
            "required": [
              "type",
              "feet",
              "ground"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "Collision proxies that follow a skinned character's pose (M33).\n\nM30 said a skinned mesh is visual and physics sees only the entity's own\n`Collider`; this is the one item of that reversed. Each part is re-posed\nevery fixed step from the same joint globals the render and\n`engine list-joints` use, so a hitbox cannot disagree with the picture\nabout where a head is.\n\n**The pose drives the proxies and nothing reads them back** — they are\nkinematic, so they are hit, they push dynamic bodies, and they report\ncontacts, but they never move a joint. That is what keeps M30's claim that\nthe pose is a pure function of (files, time) true, and it is why a proxy\nholds a character up exactly as much as a moving wall holds up the hand\npushing it: not at all. What a character stands on is still its own\n`Collider`.\n\nLayers, friction and restitution sit here rather than on each part: \"bullets\nhit hitboxes\" is a statement about the character, and per-part copies would\nbe four more strings to keep in agreement per part.",
            "properties": {
              "collides_with": {
                "description": "Only interact with colliders belonging to these layers. Absent =\ninteract with everything.",
                "items": {
                  "type": "string"
                },
                "type": [
                  "array",
                  "null"
                ]
              },
              "friction": {
                "default": 0.5,
                "description": "`>= 0`.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "layers": {
                "description": "Collision layers every part belongs to. Absent = every layer, exactly\nas on `Collider`. Empty is an error — omit the field instead.",
                "items": {
                  "type": "string"
                },
                "type": [
                  "array",
                  "null"
                ]
              },
              "parts": {
                "description": "The proxies, at most [`MAX_COLLIDER_PARTS`].",
                "items": {
                  "$ref": "#/$defs/ColliderPart"
                },
                "type": "array"
              },
              "restitution": {
                "default": 0.0,
                "description": "Bounciness, `[0, 1]`, max-combined like `Collider.restitution`.",
                "format": "float",
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              "type": {
                "const": "SkinnedCollider",
                "type": "string"
              }
            },
            "required": [
              "type",
              "parts"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "Physics driving a skinned character's skeleton (M39).\n\nM33 said the pose drives the proxies and nothing reads them back, and called\nthat its whole design. **This reverses that sentence for one entity, once,\npermanently.** When `active` turns true the entity's `SkinnedCollider`\nproxies stop being kinematic followers and become dynamic bodies wired\ntogether with rapier joints; from that step on the skeleton is a report of\nwhere they ended up.\n\n**The pose stays in the file, which is why invariant 2 survives the\nreversal.** `pose` is written back after every step, exactly as M32 writes\n`AnimationPlayer.phase` back, and `locomotion::posed_globals_at` reads it\nbefore it looks at a clip — so the render, `engine list-joints`,\n`engine list-colliders` and `world.joint_position` all see the ragdolled\nskeleton through the seam they already shared. M32's rule is what settled\nit: a ragdoll halfway to the floor, baked and reloaded, has to land in the\nsame heap, and a pose living in the physics world would reload standing up.\n\nThe bodies **are** the proxies, so the hitbox that was shot is the body that\nfalls, and the collider set does not change on handoff — which matters\nbecause that set is an input to rapier's broad phase.\n\nSee `designs/ragdoll-design.md`.",
            "properties": {
              "active": {
                "default": false,
                "description": "Whether physics owns the skeleton. A scene may ship this true and its\ncharacter is a corpse from step 0; `world.ragdoll(name)` sets it, and\nnothing clears it — the handoff is one-way (design §3).",
                "type": "boolean"
              },
              "angular_damping": {
                "default": 0.6000000238418579,
                "description": "The same, for spin — and the one that stops a corpse pinwheeling.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "density": {
                "default": 985.0,
                "description": "kg/m³, `Collider.density`'s unit. Each part's mass is its shape's\nvolume times this. Defaults to a shade under water, which is roughly\nwhat a person is.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "joints": {
                "description": "Joints that want something other than the default cone.",
                "items": {
                  "$ref": "#/$defs/RagdollJoint"
                },
                "type": "array"
              },
              "limit": {
                "default": 45.0,
                "description": "Half-angle in degrees of the cone every joint gets unless `joints`\noverrides it.",
                "format": "float",
                "maximum": 180.0,
                "minimum": 0.0,
                "type": "number"
              },
              "linear_damping": {
                "default": 0.05000000074505806,
                "description": "Velocity damping on every part. Deliberately above a physically honest\nvalue: a real body tumbles for longer than a game wants to watch, and\nthis is the dial that fixes it.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "pose": {
                "description": "The skeleton, once physics owns it: one entry per joint of the rig,\nwritten by the engine after every step. Absent until the handoff.\n\nIt is in the file rather than in the physics world because that is what\nmakes a baked ragdoll reload into the same heap — see the type's docs.",
                "items": {
                  "$ref": "#/$defs/JointPose"
                },
                "type": [
                  "array",
                  "null"
                ]
              },
              "type": {
                "const": "Ragdoll",
                "type": "string"
              }
            },
            "required": [
              "type"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "Makes a dynamic body float on a named [`Water`] surface (M41).\n\nArchimedes, sampled: the body's collider is divided into columns, each column\nis asked how deep it sits under the wave above it, and each pushes up with\nthe weight of the water it displaces. Because the pushes land at their own\ncolumns rather than at the centre of mass, a hull that rolls has more of\nitself submerged on the low side and rights itself — the pitch and roll come\nout of the same sum as the lift, with nothing modelling them separately.\n\n**Absent, nothing floats**, which is the pre-M41 engine exactly. The\ncomponent needs a `RigidBody` that is dynamic and a `Collider` to have a\nshape at all, and validation says so rather than letting a scene author a\ncomponent that silently does nothing.",
            "properties": {
              "angular_drag": {
                "default": 2.0,
                "description": "Angular damping in 1/s, scaled by submersion exactly as [`drag`] is.\n`>= 0`.\n\nUsually wants to be the larger of the two: water stops a hull from\nspinning far more effectively than it stops it from drifting, and a boat\nthat rolls for twenty seconds after a wave reads as weightless.\n\n[`drag`]: Buoyancy::drag",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "drag": {
                "default": 1.0,
                "description": "Linear damping in 1/s applied **in proportion to how submerged the body\nis**, `>= 0`.\n\nAdded on top of [`RigidBody::linear_damping`], not replacing it: that\nfield is the body's drag in air, and this is the water's. Water drag is\nnot a property of the boat, which is exactly why it cannot be authored on\nthe `RigidBody` — a hull thrown clear of the pond has to stop being\ndamped the moment it leaves, and a half-submerged one is dragged half as\nhard.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "samples": {
                "default": 2,
                "description": "Columns per axis across the body's footprint, `[1, 4]`. Default 2, so a\nhull is sampled at four points.\n\nThe fidelity knob, and it decides whether the body can **turn**: at 1\nthere is a single upward push through the middle and a raft cannot right\nitself or ride a slope, because a force through the centre of mass makes\nno torque. At 2 each quarter of the hull feels its own wave, which is\nwhat makes a boat pitch into a swell instead of hovering over it. Past\nthat the returns fall off quickly — 3 and 4 are for a long hull spanning\nseveral wavelengths.",
                "format": "uint32",
                "maximum": 4,
                "minimum": 1,
                "type": "integer"
              },
              "type": {
                "const": "Buoyancy",
                "type": "string"
              },
              "water": {
                "default": "",
                "description": "The [`Water`] entity this body floats on, by name. Required.\n\nNamed rather than found by overlap, for [`Meadow::terrain`]'s reason: one\nimplementation of \"where is the surface\", pointed at explicitly. A scene\nwith two ponds has to say which one, and a scene with one still says it,\nso the file records what the physics did.",
                "type": "string"
              }
            },
            "required": [
              "type"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "A convex piece of a broken thing (M43), as a point set that **owns its\ngeometry** — so an entity with one carries no `Mesh`, the rule `Water`,\n`Terrain`, `Cloud`, `Road`, `Junction` and `Meadow` already follow.\n\nIt is `Tree`'s exception on materials: the entity's own `Material` is the\nshard's surface, because a shard of a painted crate is painted.\n\nThe drawn solid is the convex hull of `points`, flat-shaded, and a\n`convex_hull` `Collider` on the same entity collides with that same hull —\nthe file carries points rather than faces precisely so the two cannot be\ndifferent shapes.\n\nOrdinary authored data, not a runtime-only artifact: rubble on the ground is\na pile of these. `engine fracture` is what usually writes them.",
            "properties": {
              "points": {
                "description": "The convex point set, in entity-local metres, scaled by\n`Transform.scale` like every other length. At least 4, at most 32, and\nthey must bound a volume (`shard_degenerate`).",
                "items": {
                  "items": {
                    "format": "float",
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                },
                "maxItems": 32,
                "minItems": 4,
                "type": "array"
              },
              "type": {
                "const": "Shard",
                "type": "string"
              }
            },
            "required": [
              "type",
              "points"
            ],
            "type": "object"
          },
          {
            "additionalProperties": false,
            "description": "Defines the serialized component union alongside everything that must stay\nin step with it.\n\nThe name list feeds `did_you_mean` suggestions and the spawn arm feeds\nA region of space holding a grid of baked light probes (M35).\n\nThe fill light every other surface gets is a lie: `sky_ambient` hands the\nwhole sky hemisphere to the inside of a forest, the underside of a truck and\na bare patch of open ground alike, because nothing in the engine knows that\ngeometry stands between a surface and the sky. A `LightProbeVolume` replaces\nthat constant with a function of *where you are standing and which way you\nare facing* — which is what puts contact darkening under an object and lets\ncolour travel off a coloured wall.\n\nLike [`Water`], [`Terrain`], [`Road`], [`Cloud`] and [`Meadow`], the entity\ncarries a `Transform` and this component and **no `Mesh` and no `Material`**\n(`light_probe_volume_with_mesh`). Unlike those, it grows no geometry at all:\nthe `Transform` is read purely as *bounds* — a unit box scaled and\npositioned, non-uniform scale being the normal case.\n\n```json\n{ \"type\": \"LightProbeVolume\", \"spacing\": 4.0, \"bake\": \"gi/showcase.gi.json\" }\n```\n\nSeveral volumes may overlap, which is how an interior gets finer spacing than\nthe landscape around it; the smallest `spacing` wins, name-sorted where two\ntie. A point outside every volume falls back to `sky_ambient`, which is\nexactly the pre-M35 path — so [`blend`](LightProbeVolume::blend) exists to\nmake that boundary a fade rather than a step.\n\nThe bake is a file rather than a load-time computation because the loop is\nthe product: a BVH build plus a few hundred thousand rays is seconds, and it\nwould otherwise be paid by every `screenshot`, every `diff-render` and every\neditor reload.",
            "properties": {
              "bake": {
                "default": "",
                "description": "Path to the baked transfer file, relative to the **scene file**\n(invariant 3), conventionally `gi/<name>.gi.json`.\n\nWritten by `engine bake-gi`. A path that is not there is\n`gi_bake_missing`; one taken from a different version of the scene is\n`gi_bake_stale`. Neither is a warning, because a bake that disagrees\nwith its geometry renders light that is quietly wrong — the one failure\nmode this system is built to make impossible.",
                "type": "string"
              },
              "blend": {
                "default": 1.0,
                "description": "Metres of fade at the volume's edge, `>= 0`.\n\nOver this distance the volume's irradiance crosses to whatever lies\noutside it — another volume, or the `sky_ambient` fallback. Without it\nthe boundary is a visible step, since the two models disagree by exactly\nthe occlusion the volume measured.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "bounces": {
                "default": 1,
                "description": "Light bounces the bake gathers, `[1, 2]`.\n\nBounce one holds nearly all of the visible difference; bounce two costs\nanother pass over the volume and mostly lifts the black out of deep\nocclusion.",
                "format": "uint32",
                "maximum": 2,
                "minimum": 1,
                "type": "integer"
              },
              "intensity": {
                "default": 1.0,
                "description": "Scales the whole effect, `>= 0`.\n\nExists so an authoring pass can dial GI back without re-baking, and so\n`0.0` is a one-field A/B against the pre-M35 look. Animatable, which is\nhow a scene fades GI in.",
                "format": "float",
                "minimum": 0.0,
                "type": "number"
              },
              "spacing": {
                "default": 4.0,
                "description": "Metres between probes, `> 0`.\n\nA spacing, not a resolution: resizing a volume keeps its GI detail\ninstead of stretching it, and two volumes at the same spacing agree\nwhere they meet. Probe counts are derived from this and the `Transform`\nscale, reported by `bake-gi`, and bounded by `too_many_gi_probes`.",
                "exclusiveMinimum": 0.0,
                "format": "float",
                "type": "number"
              },
              "type": {
                "const": "LightProbeVolume",
                "type": "string"
              }
            },
            "required": [
              "type"
            ],
            "type": "object"
          }
        ]
      },
      "DaylightKeyframe": {
        "additionalProperties": false,
        "description": "One entry in the day's color table.\n\nThe palette carries the **sun's** color and intensity, not the directional\nlight's in general — the moon has its own two fields on\n[`DaylightSettings`], because it does not meaningfully change color through\na night and would otherwise need repeating in every night keyframe.",
        "properties": {
          "ambient_color": {
            "description": "Linear RGB chromaticity of the ambient fill.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "ambient_intensity": {
            "description": "Brightness of the ambient fill.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "fog_scale": {
            "description": "Multiplier on the scene's authored `environment.fog_density`.\n\nA **scale**, not a density, deliberately (design §5): a scene with\n`fog_density: 0` stays clear all day however misty the palette's dawn\nis, and a scene that wants fog authors it once and gets the dawn\nthickening for free. An absolute density here would mean a daylight\nblock silently switching fog on in a scene that never asked for any.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "hour": {
            "description": "Hours in `[0, 24)`.",
            "format": "float",
            "maximum": 24.0,
            "minimum": 0.0,
            "type": "number"
          },
          "sky_ground": {
            "description": "Color below the horizon.",
            "items": {
              "format": "float",
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "sky_horizon": {
            "description": "Sky color at the horizon — which is also the fog color, since\n`environment` has one field for both.",
            "items": {
              "format": "float",
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "sky_zenith": {
            "description": "Sky color straight overhead. Unclamped above 1, like\n`environment.sky_zenith`: a sky is a light source, and clamping it to\nreflectance range makes noon look like dusk.",
            "items": {
              "format": "float",
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "sun_color": {
            "description": "Linear RGB chromaticity of direct sunlight at this hour.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "sun_intensity": {
            "description": "Brightness of direct sunlight at this hour.\n\nIt lives in the table rather than falling out of `sin(altitude)`\nbecause a sunset's redness and its dimness are one artistic decision,\nand splitting them across a physical falloff and a color curve means\nretuning a dusk in two places that then disagree.\n\n**Keep it near zero at the horizon hours.** The sun/moon handoff\n(§ [`Daylight`]) switches the light's direction where the two bodies\ncontribute equally, and how visible that switch is *is* the sun's\nintensity there. A test pins it.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          }
        },
        "required": [
          "hour",
          "sun_color",
          "sun_intensity",
          "ambient_color",
          "ambient_intensity",
          "sky_zenith",
          "sky_horizon",
          "sky_ground",
          "fog_scale"
        ],
        "type": "object"
      },
      "DaylightSettings": {
        "additionalProperties": false,
        "description": "The scene-level `daylight` block — a sibling of `physics` and\n`environment`, not a field inside either.\n\nIt sits outside `environment` for two reasons (design §2). Conceptually\nthis is a *clock-driven system*, closer to `physics`, and it **produces**\nenvironment values rather than being one. Mechanically, `EnvironmentSettings`\nis `Copy` because lights resolve every frame in the viewer; an optional\nkeyframe palette is a `Vec`, and hanging it there would cost that type its\n`Copy` and put a clone in the per-frame path.\n\n**A scene with no `daylight` block renders exactly as it did before this\nmodule existed, byte for byte.** That is the M16 contract repeated, and it\nis why nineteen existing baselines did not move.",
        "properties": {
          "day_length": {
            "default": 0.0,
            "description": "Seconds of scene time per full 24-hour cycle. `0` (the default) freezes\nthe day, which is what most scenes want: a dial, not motion, and a\nscreenshot reproducible from the file with no `--time` at all.\n\n`24.0` makes an hour a second, so `--time 6.5` is 6:30 in the morning.\nThat is the unit the fixtures use because it reads itself.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "drives_sky": {
            "default": true,
            "description": "Compute `environment.sky_zenith` / `sky_horizon` / `sky_ground` **and\nthe ambient term** from the palette.\n\nAmbient rides with the sky rather than with the sun because it *is*\nthe sky's contribution — which is why M16 gates hemispheric ambient on\n`sky` in the first place. A scene that authored non-default band colors\nor its own `AmbientLight` and leaves this on gets the\n`daylight_overrides_sky` warning.",
            "type": "boolean"
          },
          "drives_sun": {
            "default": true,
            "description": "Synthesize the sun's direction, color, and intensity.\n\nWith this on (the default) a scene needs **no** `DirectionalLight`\nentity, and authoring one anyway is `daylight_and_directional_light`.\nTwo owners of one sun is what invariant 8 exists to prevent: a rotation\nin a text file that is silently ignored, or silently overwritten, is a\nvalue that does not mean what it says.\n\nSet it `false` and daylight paints the sky, ambient, and fog only,\nleaving a hand-aimed `DirectionalLight` its job. That is the artist\nscene, and it is one field.",
            "type": "boolean"
          },
          "moon_color": {
            "default": [
              0.550000011920929,
              0.6600000262260437,
              0.949999988079071
            ],
            "description": "Linear RGB chromaticity of moonlight. Magnitude lives in\n`moon_intensity`, like every other light in the engine.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "moon_elevation": {
            "default": 48.0,
            "description": "Degrees above the horizon the moon reaches at its highest. It rides the\nsame arc as the sun offset by twelve hours; giving it its own elevation\nis what keeps it from being a mechanical anti-sun.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "maximum": 90.0,
            "type": "number"
          },
          "moon_intensity": {
            "default": 0.05999999865889549,
            "description": "Brightness of moonlight. The default is deliberately tiny: a moonlit\nscene should read as *navigable*, not as a dim afternoon.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "palette": {
            "description": "The color table over the day. Absent means [`DaylightSettings::builtin_palette`].\n\nEvery field of a keyframe is required: a half-specified keyframe\nsilently interpolating toward black is a worse failure than a\nvalidation error.",
            "items": {
              "$ref": "#/$defs/DaylightKeyframe"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "sun_azimuth": {
            "default": 0.0,
            "description": "Compass bearing of the noon sun in degrees, rotating the whole arc\nabout Y. `0` puts noon toward −Z, matching the engine's aiming\nconvention. Unbounded — it wraps.",
            "format": "float",
            "type": "number"
          },
          "sun_elevation": {
            "default": 62.0,
            "description": "Degrees above the horizon the sun reaches at noon. `90` is overhead,\n`20` is a winter afternoon that never really gets going.\n\nThis one number replaces latitude, longitude, date, and axial tilt.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "maximum": 90.0,
            "type": "number"
          },
          "time_of_day": {
            "default": 12.0,
            "description": "Hours in `[0, 24)` — `6.5` is half past six in the morning. When the\nday is cycling this is the hour at scene time zero.",
            "format": "float",
            "maximum": 24.0,
            "minimum": 0.0,
            "type": "number"
          }
        },
        "type": "object"
      },
      "EntityDef": {
        "additionalProperties": false,
        "description": "One entity in a scene file.",
        "properties": {
          "components": {
            "default": [],
            "items": {
              "$ref": "#/$defs/ComponentData"
            },
            "type": "array"
          },
          "name": {
            "description": "Unique within the scene; this is what CLI commands target (invariant 4).",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "type": "object"
      },
      "EnvironmentSettings": {
        "additionalProperties": false,
        "description": "The scene-level `environment` block (M16): how this scene is *rendered*,\nas opposed to what is in it.\n\nEvery field defaults to the pre-M16 renderer — no sky, no fog, no shadows,\none sample — so a scene that says nothing about its environment renders\nbyte-identically to before the block existed. That is deliberate and is\nwhat let M16 land without re-blessing baselines it had no business\ntouching: the features are opt-in per scene, in the scene file, because a\nscreenshot has to be reproducible from the file alone (invariant 2).",
        "properties": {
          "fog_density": {
            "default": 0.0,
            "description": "Exponential-squared distance fog: `1 - exp(-(d * density)^2)`. `0`\ndisables it. Around `0.008` is a light haze at 100 m; `0.05` is thick.\n`>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "samples": {
            "default": 1,
            "description": "Multisample count for the scene pass: `1` (off) or `4`. The HUD is\ncomposited after the resolve and is never multisampled, so text stays\npixel-exact either way.",
            "format": "uint32",
            "maximum": 4,
            "minimum": 1,
            "type": "integer"
          },
          "shadow_cascades": {
            "default": 1,
            "description": "How many nested shadow maps the sun renders (M38): `1`–`4`, default\n`1`. Each cascade covers a third the extent of the one outside it and\nis therefore three times sharper, so `3` at a `shadow_distance` of\n240 m gives 1.3 cm texels near the camera where 11.7 cm is all a single\nmap can offer. Costs one caster pass and 16 MB per cascade.",
            "format": "uint32",
            "maximum": 4,
            "minimum": 1,
            "type": "integer"
          },
          "shadow_distance": {
            "default": 60.0,
            "description": "How far from the camera the shadow map covers, in meters. The map is a\nfixed resolution, so this trades area against sharpness: a 40 m box is\ncrisp, a 200 m box is blocky. Strictly positive.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "shadows": {
            "default": false,
            "description": "Cast shadows from the scene's `DirectionalLight`.",
            "type": "boolean"
          },
          "sky": {
            "default": false,
            "description": "Draw a gradient sky behind the scene instead of the flat clear color.",
            "type": "boolean"
          },
          "sky_ground": {
            "default": [
              0.1599999964237213,
              0.1599999964237213,
              0.17000000178813934
            ],
            "description": "Color below the horizon — what a ground plane fades into at distance.\nUsually a darker, less saturated version of the terrain.",
            "items": {
              "format": "float",
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "sky_horizon": {
            "default": [
              0.6200000047683716,
              0.7099999785423279,
              0.8199999928474426
            ],
            "description": "Sky color at the horizon. This is also the fog color: fog that does\nnot match the sky it fades into reads as a gray wall, and having one\nfield means it cannot be set wrong.",
            "items": {
              "format": "float",
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "sky_zenith": {
            "default": [
              0.1899999976158142,
              0.3400000035762787,
              0.6200000047683716
            ],
            "description": "Sky color straight overhead. Linear RGB, like every other color in the\nengine — and unclamped above 1, because a sky is a light source and\nclamping it to reflectance range makes noon look like dusk.",
            "items": {
              "format": "float",
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          }
        },
        "type": "object"
      },
      "FractureMaterial": {
        "description": "What a `Breakable` is made of (M43).\n\n`glass` shatters into radial slivers that fly far and fast; `wood` splits\ninto splinters along the grain and tumbles about its long axis; `stone`\nbreaks into chunky blocks that barely scatter, so a granite block comes\napart and its pieces *drop*; `metal` tears into a few large plates that\nmove together and slide.\n\nFour, because four is what the fracture generator has distinct algorithms\nfor and what the runtime has distinct behaviour for. A fifth would need\nboth, and \"generic\" is spelled by leaving the field out.\n\nNOTE: leave these variants undocumented, for [`ParticleBlend`]'s reason —\na doc comment on an enum *variant* makes schemars emit oneOf/const instead\nof a flat `\"enum\": [...]`, which blinds the validation walk's\nclosed-vocabulary check. Measured here: `\"material\": \"wud\"` reported\nnothing at all until these four comments moved up into this one.",
        "enum": [
          "glass",
          "wood",
          "stone",
          "metal"
        ],
        "type": "string"
      },
      "Fragment": {
        "additionalProperties": false,
        "description": "One piece a `Breakable` entity shatters into (M14, shards since M43).\n\nA fragment is **either** a mesh reference or a shard, and exactly one of the\ntwo (`fragment_geometry`):\n\n- `mesh` follows `Mesh.asset` rules (builtin or relative glTF path), and\n  `half_extents` is its cuboid collider in fragment-local units — M14's\n  shape, where shards are boxes to the solver.\n- `points` is a convex point set (M43) that becomes a [`Shard`]: the\n  fragment's collider *is* its geometry, so `half_extents` alongside it is\n  an error rather than a number physics quietly ignores.\n\n`offset`/`rotation`/`scale` place the fragment relative to the parent\nentity, so the assembled fragments overlay the unbroken model. `scale`\nscales the collider, exactly as `Transform.scale` does.",
        "properties": {
          "density": {
            "default": 1.0,
            "description": "Fragment mass comes from `density` x collider volume. `> 0`.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "half_extents": {
            "description": "Cuboid collider half-extents, for a `mesh` fragment. The default is\n`[0.5, 0.5, 0.5]`, matching `builtin:cube`, so a fragment that is a\nscaled builtin cube needs no collider authoring.\n\nOptional rather than defaulted-in-place since M43: a shard's collider\nis its own hull, so the difference between \"absent\" and \"written at the\ndefault\" is the difference between a valid fragment and a claim about a\ncollider physics never builds.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": [
              "array",
              "null"
            ]
          },
          "mesh": {
            "description": "The piece's geometry as a mesh reference. Exclusive with `points`.",
            "type": [
              "string",
              "null"
            ]
          },
          "offset": {
            "default": [
              0.0,
              0.0,
              0.0
            ],
            "description": "Position relative to the parent entity's origin, in parent-local\nunits (the parent's `Transform.scale` applies).",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "points": {
            "description": "The piece's geometry as a convex point set in parent-local metres\n(M43) — what `engine fracture` writes. Exclusive with `mesh`.",
            "items": {
              "items": {
                "format": "float",
                "type": "number"
              },
              "maxItems": 3,
              "minItems": 3,
              "type": "array"
            },
            "maxItems": 32,
            "minItems": 4,
            "type": [
              "array",
              "null"
            ]
          },
          "rotation": {
            "default": [
              0.0,
              0.0,
              0.0
            ],
            "description": "Euler angles in **degrees**, `[x, y, z]`, XYZ order — the same\nconvention as `Transform.rotation`.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "scale": {
            "default": [
              1.0,
              1.0,
              1.0
            ],
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          }
        },
        "type": "object"
      },
      "GrowthStage": {
        "additionalProperties": false,
        "description": "One keyframe of a [`Meadow`]'s life cycle.\n\nAll seven fields are required. A half-specified keyframe is an error rather\nthan a fade to black — M21's palette wrote that rule down, and a meadow's\ntable is read the same way: linearly interpolated, and **wrapping** from the\nlast keyframe back round to the first, so the cycle closes without anyone\nhaving to author phase 1.0 as a copy of phase 0.0.",
        "properties": {
          "at": {
            "description": "Where in the cycle this keyframe sits, `[0, 1)`. Strictly increasing\ndown the table (`meadow_stages_invalid`).",
            "format": "float",
            "maximum": 0.999,
            "minimum": 0.0,
            "type": "number"
          },
          "color": {
            "description": "Linear RGB at the plant's base, each component `[0, 1]`.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "height": {
            "description": "Plant height as a fraction of [`Meadow::height`], `>= 0`. `0` is a plant\nthat is not there, which is what the seed keyframe says.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "lean": {
            "description": "Degrees the plant leans from vertical at its tip. The bend is a\ncantilever — the tip leans this far and the root not at all — so a large\nvalue at the end of the cycle is the plant collapsing rather than\ntipping over rigidly. `[0, 90]`.",
            "format": "float",
            "maximum": 90.0,
            "minimum": 0.0,
            "type": "number"
          },
          "sway": {
            "description": "How much of [`Meadow::wind`] reaches the plant at this stage, `>= 0`.\n\nThis is what separates green grass that flows from dry stalks that\nstand, and it is nearly free — the wind term is already in the vertex\nstage.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "tip_color": {
            "description": "Linear RGB at the plant's tip, each component `[0, 1]`.\n\nTwo colours rather than one because senescence runs tip-downward in real\ngrass: a stand going over turns straw at the top while its base is still\ngreen, and a single flat colour per stage looks painted on.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "width": {
            "description": "Blade width as a fraction of [`Meadow::blade_width`], `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          }
        },
        "required": [
          "at",
          "height",
          "width",
          "lean",
          "sway",
          "color",
          "tip_color"
        ],
        "type": "object"
      },
      "HudAlign": {
        "description": "Cross-axis alignment of a [`HudPanel`]'s children (M31). See [`HudLayout`]\nfor why the variants are undocumented.",
        "enum": [
          "start",
          "center",
          "end"
        ],
        "type": "string"
      },
      "HudAnchor": {
        "description": "Where a HUD element attaches on screen (M12).\n\n`offset` is measured **inward** from the anchor: from a right anchor,\n`offset[0]` runs leftward; from a bottom anchor, `offset[1]` runs upward;\nfrom `center` it is the usual +x-right / +y-down applied to the element's\ncenter. The anchored point is the element's matching corner (its center\nfor `center`), so `offset: [0, 0]` puts the element flush against its\ncorner at any resolution.",
        "enum": [
          "top_left",
          "top_right",
          "bottom_left",
          "bottom_right",
          "center"
        ],
        "type": "string"
      },
      "HudLayout": {
        "description": "How a [`HudPanel`] arranges its children (M31).\n\nNOTE (schemars): variants carry no doc comments on purpose. A doc comment\non a *variant* turns the generated schema from a flat `\"enum\": [...]` into\noneOf/const, which blinds the validation walk's closed-vocabulary check —\nthe same trap `ColliderShapeKind` documents.",
        "enum": [
          "free",
          "row",
          "column"
        ],
        "type": "string"
      },
      "HudTextAlign": {
        "description": "Horizontal alignment of text within its own box (M31). See [`HudLayout`]\nfor why the variants are undocumented.",
        "enum": [
          "left",
          "center",
          "right"
        ],
        "type": "string"
      },
      "JointPose": {
        "additionalProperties": false,
        "description": "One joint's local placement while a [`Ragdoll`] owns the skeleton (M39).\n\n**`rotation` is a quaternion, `w` last — the one rotation in this format\nthat is not XYZ Euler degrees.** `CLAUDE.md` names the reason under Traps:\nXYZ Euler clamps the middle angle to ±90°, so an orientation the solver\nintegrated past that comes back as the `(±180, θ, ±180)` twin, and a\nragdoll's joints go past it in the first second. M30 drew the same line for\nskeletal clips, and the distinction is *who wrote the numbers*: these are\nthe engine's, like a DCC tool's, not an agent's.",
        "properties": {
          "joint": {
            "description": "The joint this places. Every joint of the rig appears exactly once.",
            "type": "string"
          },
          "rotation": {
            "default": [
              0.0,
              0.0,
              0.0,
              1.0
            ],
            "description": "`[x, y, z, w]`, glTF's order and rapier's.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 4,
            "minItems": 4,
            "type": "array"
          },
          "scale": {
            "description": "Carried only when it is not 1: a ragdoll does not scale bones, so this\nis whatever the clip had at the moment of handoff.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": [
              "array",
              "null"
            ]
          },
          "translation": {
            "default": [
              0.0,
              0.0,
              0.0
            ],
            "description": "Metres, in the parent joint's frame.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          }
        },
        "required": [
          "joint"
        ],
        "type": "object"
      },
      "JunctionArm": {
        "additionalProperties": false,
        "description": "One road arriving at a [`Junction`].",
        "properties": {
          "end": {
            "$ref": "#/$defs/JunctionEnd",
            "default": "end",
            "description": "Which end of it arrives."
          },
          "road": {
            "default": "",
            "description": "Name of the entity carrying the `Road` that arrives here (invariant 4).",
            "type": "string"
          }
        },
        "type": "object"
      },
      "JunctionEnd": {
        "description": "Which end of a road arrives at a [`Junction`].",
        "oneOf": [
          {
            "const": "start",
            "description": "The road's first point.",
            "type": "string"
          },
          {
            "const": "end",
            "description": "The road's last point. A closed road has no free end, so an arm onto one\nis a validation error.",
            "type": "string"
          }
        ]
      },
      "ParticleBlend": {
        "description": "How a particle's color combines with what is already on screen.\n\nNOTE: leave these variants undocumented. A doc comment on an enum *variant*\nmakes schemars emit oneOf/const instead of a flat `\"enum\": [...]`, which\nblinds the validation walk's closed-vocabulary check — the same trap\n[`ColliderShapeKind`] carries a note about.",
        "enum": [
          "alpha",
          "additive"
        ],
        "type": "string"
      },
      "PhysicsSettings": {
        "additionalProperties": false,
        "description": "The scene-level `physics` block (M8).",
        "properties": {
          "gravity": {
            "default": [
              0.0,
              -9.8100004196167,
              0.0
            ],
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "timestep_hz": {
            "default": 60,
            "description": "Fixed steps per simulated second. An **integer** deliberately: `1/60`\nhas no exact JSON representation, and an integer keeps scene files\nfree of float-precision noise. `dt = 1.0 / hz`, computed once,\nidentically everywhere. `>= 1`.",
            "format": "uint32",
            "minimum": 1,
            "type": "integer"
          }
        },
        "type": "object"
      },
      "PlantedFoot": {
        "additionalProperties": false,
        "description": "One foot of a [`FootPlant`]: which joint it is, and how far up the chain\nthe solver may rotate to reach the ground.",
        "properties": {
          "ankle": {
            "description": "The ankle joint — the end of the chain, the thing put on the ground.",
            "type": "string"
          },
          "chain": {
            "default": 2,
            "description": "How many joints **above** the ankle rotate to reach the target. `2` is\nthe ordinary leg (knee and hip) and is what the two-bone solve means;\n`1` bends a single hinge and is what a stubby prop leg wants.",
            "format": "uint32",
            "maximum": 2,
            "minimum": 1,
            "type": "integer"
          },
          "sole": {
            "default": 0.0,
            "description": "Metres from the ankle joint down to the bottom of the foot, so the sole\nmeets the ground rather than the joint being buried in it.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          }
        },
        "required": [
          "ankle"
        ],
        "type": "object"
      },
      "RagdollJoint": {
        "additionalProperties": false,
        "description": "A per-joint override of [`Ragdoll`]'s default cone (M39).\n\nA ragdoll whose every joint is a 45° cone reads as a bag. A knee that only\nbends one way, and only backwards, is what makes it read as a body — and\nboth numbers being in the file is what makes it tunable without touching\nRust.",
        "properties": {
          "hinge": {
            "description": "Turn this joint into a hinge about this axis in the **child** part's\nlocal frame, instead of a cone. An elbow or a knee.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": [
              "array",
              "null"
            ]
          },
          "joint": {
            "description": "The joint to override. Some part of the `SkinnedCollider` must ride it,\nand it must not be the root — a root part has no joint to constrain.",
            "type": "string"
          },
          "limit": {
            "description": "Half-angle of the cone, in degrees. Ignored when `hinge` is set.",
            "format": "float",
            "maximum": 180.0,
            "minimum": 0.0,
            "type": [
              "number",
              "null"
            ]
          },
          "range": {
            "description": "The hinge's travel in degrees, `[min, max]`. `hinge` only.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 2,
            "minItems": 2,
            "type": [
              "array",
              "null"
            ]
          }
        },
        "required": [
          "joint"
        ],
        "type": "object"
      },
      "RoadMarkings": {
        "additionalProperties": false,
        "description": "What is painted on a road, and where.\n\nEvery marking is computed per pixel from the road's surface coordinates —\n`u`, metres from the centerline across the road, and `v`, metres along it —\nrather than built as geometry laid on the asphalt. That is what makes a line\nfollow every curve and grade for free, keeps a dash the same length in\nmetres through a hairpin as on a straight, and means paint can never\nz-fight: it is not a surface on a surface, it is the same pixel shaded\ndifferently.",
        "properties": {
          "center_dash": {
            "default": 3.0,
            "description": "Length of one painted dash in the centre line, in metres. `0` makes it\nsolid. `>= 0`.\n\nOn a **closed** road the dash period is snapped to a whole number of\nrepeats around the lap, so the pattern meets itself exactly at the seam\ninstead of leaving a short dash there. The ratio of dash to gap is what\nis preserved, not their absolute lengths.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "center_gap": {
            "default": 6.0,
            "description": "Unpainted gap between dashes, in metres. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "center_width": {
            "default": 0.0,
            "description": "Width of the centre line, in metres. `0` (the default) is no centre\nline, which is what a race circuit wants. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "color": {
            "default": [
              0.8799999952316284,
              0.8799999952316284,
              0.8799999952316284
            ],
            "description": "Linear RGB of the paint — the edge lines, the centre line, the start\nline, and the white half of a kerb. Each component `[0, 1]`.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "edge_inset": {
            "default": 0.10000000149011612,
            "description": "Gap between the asphalt's edge and the outside of the edge line, in\nmetres. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "edge_width": {
            "default": 0.14000000059604645,
            "description": "Width of the line down each edge of the asphalt, in metres. `0` is no\nedge line. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "kerb_color": {
            "default": [
              0.800000011920929,
              0.10000000149011612,
              0.07999999821186066
            ],
            "description": "Linear RGB of the red half of a kerb; the other half is `color`. Each\ncomponent `[0, 1]`.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "kerb_max_radius": {
            "default": 0.0,
            "description": "Corners with a radius at or under this get a kerb on the **inside** of\nthe turn, in metres. `0` (the default) is no kerbs. `>= 0`.\n\nWhich corners are tight enough, and which side of the road is the inside\nof the turn, are facts about the plan-view geometry that per-pixel code\ncannot know, so they are computed once and handed to the shader as spans\n— at most [`MAX_ROAD_KERBS`](crate::road::MAX_ROAD_KERBS) of them.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "kerb_stripe": {
            "default": 1.399999976158142,
            "description": "Length of one red or white kerb stripe, in metres. `> 0`.\n\nFitted per corner to a whole number of stripes, so a kerb begins and\nends on a stripe boundary rather than on a sliver.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          },
          "kerb_width": {
            "default": 0.8999999761581421,
            "description": "How far a kerb reaches out from the asphalt edge, in metres. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "start_line": {
            "default": false,
            "description": "Paint a line across the road.\n\nPainted rather than built, which matters more here than anywhere else: a\nstart line with any real height is a wall the car is parked against at\nstep 0.",
            "type": "boolean"
          },
          "start_line_at": {
            "default": 0.0,
            "description": "Where that line goes: metres along the centerline from the road's first\npoint. `>= 0`, and past the end of a closed road it wraps.\n\nA position rather than \"wherever the polygon starts\", because the two\nare different jobs: the polygon's first point is a *corner*, chosen by\nthe shape of the circuit, and a start line belongs partway down a\nstraight. Splitting a straight with an extra point to move the line\nwould be geometry surgery in service of paint — and on a short straight\nit fails outright, because the neighbouring corner's arc already covers\nthe place the line wanted to be. `engine road-centerline` is how a\ngenerator turns \"here, in world space\" into this number.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "start_line_width": {
            "default": 0.699999988079071,
            "description": "Width of that line along the road, in metres. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          }
        },
        "type": "object"
      },
      "RoadPoint": {
        "additionalProperties": false,
        "description": "One authored point on a road's centerline.\n\nThe road is a **polygon with corner radii**, not a spline: a closed polygon\nreturns to its own first vertex and its exterior angles sum to exactly one\nturn, so position and heading close without solving anything. Nothing here\ncarries a heading, deliberately — a heading is derived, and a stored one can\nbe edited into disagreeing with the points on either side of it.",
        "properties": {
          "bank": {
            "default": null,
            "description": "Roll of the cross-section here, in degrees, positive raising the\ndriver's **right** edge (M40).\n\nAbsent falls through to [`Road::auto_bank`], which is the way to author\na circuit: auto-banking knows which way the road turns and so raises the\noutside of every corner without the file carrying a sign that has to\nagree with the winding of a polygon being edited beside it. Reach for an\nexplicit value for the cases auto-banking cannot know — a banked\nstraight, a corner deliberately flat.",
            "format": "float",
            "type": [
              "number",
              "null"
            ]
          },
          "pin_height": {
            "default": false,
            "description": "Read `position.y` as an absolute height even though the road follows a\nterrain (M40) — a bridge deck, a level a junction has to meet.\n\nThe pin is a *local* correction to the followed profile, faded out over\n[`Road::follow_blend`] metres either side, so the road reaches this\nheight exactly and goes back to hugging the ground. Two pins closer\ntogether than that blend pull on each other and neither is reached\nexactly; validation warns (`road_pins_overlap`).\n\nWithout `follow_terrain` every height is already absolute and this does\nnothing.",
            "type": "boolean"
          },
          "position": {
            "default": [
              0.0,
              0.0,
              0.0
            ],
            "description": "Where the centerline passes, in the entity's local space. `y` is the\nroad *surface* height there; the profile between points is a monotone\ncubic through these, so the grade turns over smoothly and never\novershoots an authored height.\n\nWith [`Road::follow_terrain`] set, `y` means something different: it is\nthe **clearance above the sampled ground**, so `0` sits the road on the\nterrain. [`pin_height`](Self::pin_height) turns it back into an absolute\nheight for one point.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "radius": {
            "default": 0.0,
            "description": "Radius of the arc rounding this corner, in metres. `>= 0`.\n\n`0` is a sharp vertex — mitred, which is exactly right for a point that\nis not really a turn (a start line partway along a straight) and wrong\nfor one that is: past\n[`MAX_SHARP_TURN_DEGREES`](crate::road::MAX_SHARP_TURN_DEGREES) of turn\nthe mitre folds back through the road and validation refuses it.\n\nThe two arcs meeting on one edge have to fit on it, which is the other\nthing a polygon cannot guarantee (`road_corner_does_not_fit`).",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "width": {
            "default": null,
            "description": "Width of the asphalt here, in metres, overriding the road's `width`\n(M40). Absent — the default — is the road's own width, so a file that\nnever mentions this is the M23 road exactly. `> 0`.\n\nInterpolated along the centerline by the same monotone cubic the heights\nuse, so a road never bulges wider than the widest width authored on it.\n**The whole cross-section scales, shoulder included**: `u` — signed\nmetres across the road — has to mean one thing at every `v` for the\nshader to find the asphalt edge, so the widening is one factor on the\nsection rather than a metre value added to the asphalt alone. Paint\nscales with it: a section at 1.5× width wears a 1.5× wider edge line.",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": [
              "number",
              "null"
            ]
          }
        },
        "type": "object"
      },
      "TemplateDef": {
        "additionalProperties": false,
        "description": "One spawnable template in a scene file (M37).\n\nDeliberately not a variant of [`EntityDef`]: a template is a *declaration*\nrather than a thing, and the difference shows up in every consumer —\n`Scene::instantiate` skips it, the renderer never sees it, `simulate` never\ntraces it, and `validate` runs the per-entity walk on it but none of the\nscene-level budget passes.",
        "properties": {
          "components": {
            "default": [],
            "items": {
              "$ref": "#/$defs/ComponentData"
            },
            "type": "array"
          },
          "limit": {
            "default": 64,
            "description": "The most instances of this template that may be **live** at once —\nspawned minus despawned. Spawning at the limit spawns nothing and\nreturns the empty string, which is a value the script reads rather\nthan a crash: a gun that fires faster than its bullets expire is an\nordinary game, not a bug.\n\nA number in the file rather than a hard-coded ceiling because every\ninstance is a collider and the collider set is an input to the broad\nphase — a scene that wants ten thousand bullets should have to say so\nwhere `engine inspect` can read it. `>= 1`.",
            "format": "uint32",
            "minimum": 1,
            "type": "integer"
          },
          "name": {
            "description": "What `world.spawn_entity` names, and the stem of every instance's name\n(`Bullet` spawns `Bullet#1`, `Bullet#2`, …). Unique among templates\n*and* distinct from every entity name: one address space, because\n`world.spawn_entity(\"Drone\")` and `world.set_position(\"Drone\", …)` must never\nbe able to mean two different things.",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "type": "object"
      },
      "TerrainBasin": {
        "additionalProperties": false,
        "description": "One circular depression a [`Terrain`] cuts into its height field (M42): a\nflat floor, a smoothstepped wall, and how far down the floor sits.\n\nThe shape is `radius` metres of floor surrounded by `falloff` metres of wall,\nso the whole footprint is `radius + falloff` and the shoreline of any water\nput here lands somewhere on the wall. **Two numbers rather than one**: a\nsingle radius with a falloff to nothing gives a dish, and a dish deep enough\nto hold water has walls gentle enough that the waterline creeps far past\nwhere it was authored.\n\n**Overlapping basins take the deepest, not the sum.** Overlapping circles are\nhow anything that is not a circle gets authored — a lake is three of them, a\nchannel a line of them — and under a sum every overlap digs to twice the\ndepth, turning a lake into a ring of pits. The cost is a faint crease where\ntwo basins are equally deep, which is under the water in every use this was\nbuilt for.",
        "properties": {
          "center": {
            "default": [
              0.0,
              0.0
            ],
            "description": "Where the basin is centred, in **world** XZ metres — the same space\neverything else about a terrain is sampled in, and the same numbers the\n`Water` entity's `Transform.position` is written in.\n\nWorld rather than the patch's local space for M22's reason: heights are\na function of world position, which is what lets two patches sharing one\ndescription meet seamlessly. A basin in local space would be dragged\naround by its patch and would break that.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 2,
            "minItems": 2,
            "type": "array"
          },
          "depth": {
            "default": 0.0,
            "description": "Metres the floor drops below the surrounding field, `>= 0`.\n\nIn the same space as [`Terrain::height`] — metres of relief at\n`Transform.scale.y` 1, so the patch's vertical scale multiplies a basin\nexactly as it multiplies the hills. A second convention here would make\na patch at `scale.y: 2` disagree with itself about which of its numbers\nare metres.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "falloff": {
            "default": 4.0,
            "description": "Metres of wall between the floor's edge and the untouched field, `>= 0`.\n\nSmoothstepped, with the interpolant the value noise already uses, so the\nwall meets the surrounding ground with a zero derivative and the rim has\nno crease. 0 is a vertical wall, which reads as a quarry rather than a\npond and is occasionally what is wanted.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "radius": {
            "default": 0.0,
            "description": "Metres of flat floor at the centre, `>= 0`. 0 is legitimate and makes\nthe basin a smooth conical hollow of `falloff` metres.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          }
        },
        "type": "object"
      },
      "TerrainLayer": {
        "additionalProperties": false,
        "description": "One material a [`Terrain`] paints itself with, claiming a band of height and\na band of slope.\n\nA pixel's surface is the weighted average of every layer whose bands it falls\ninside; a layer that leaves both at their defaults covers everything and is\nthe base coat.\n\n**Slope does the heavy lifting.** Height alone gives horizontal stripes — a\ncontour map, and unmistakably so once the camera moves. What separates rock\nfrom grass in the world is that soil cannot cling to a steep face, so\n`slope_range` is the selector that reads as real and `height_range` adds\nbands of climate on top of it.",
        "properties": {
          "albedo": {
            "default": [
              0.12999999523162842,
              0.17000000178813934,
              0.09000000357627869
            ],
            "description": "Linear RGB, each component `[0, 1]` — `Material.albedo` for this band.",
            "items": {
              "format": "float",
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "height_blend": {
            "default": 0.5,
            "description": "Metres over which this layer fades out beyond each end of\n`height_range`. `>= 0`; 0 is a hard edge, which reads as a cut line and\nis almost never what ground does.\n\nAbsolute, not a fraction of the band — that was tried first and is a\ntrap. A fraction means a wide band gets a wide fade, so a layer written\nas \"above 1.9 m\" with a generous top end bleeds ten metres *below* where\nit was aimed and washes out everything under it. In metres, what the\nauthor writes is what the surface does.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "height_range": {
            "default": [
              -1000.0,
              1000.0
            ],
            "description": "World-space Y band this layer covers, in metres, `[min, max]`.\n\nWorld Y, not a fraction of [`Terrain::height`], so a layer's altitude\ndoes not shift when the relief is retuned — and so that two patches at\ndifferent elevations can share one description of what grows where.",
            "items": {
              "format": "float",
              "type": "number"
            },
            "maxItems": 2,
            "minItems": 2,
            "type": "array"
          },
          "noise": {
            "default": 0.5,
            "description": "How much the detail noise jitters this layer's boundary, `[0, 1]`, as a\nfraction of its own fade widths.\n\nDrawn honestly, the boundary between two layers is an iso-line of a\nsmooth function — a clean sweeping curve, and the eye reads clean curves\nas artificial faster than it reads anything else here. Jittering the\nheight and slope the layer *thinks* it is at breaks the boundary into\ninterlocking fingers at two scales, for one multiply-add.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "roughness": {
            "default": 0.949999988079071,
            "description": "Perceptual roughness, `[0, 1]`, meaning what `Material.roughness` means.\nWet rock and packed sand are smoother than grass, and the difference\nshows wherever the sun is low.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "slope_blend": {
            "default": 8.0,
            "description": "Degrees over which this layer fades out beyond each end of\n`slope_range`. `>= 0`.\n\nSeparate from [`height_blend`](TerrainLayer::height_blend) because the\ntwo bands are in different units and one number cannot be honest about\nboth.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "slope_range": {
            "default": [
              0.0,
              90.0
            ],
            "description": "Surface-angle band this layer covers, in degrees from horizontal,\n`[min, max]`. 0 is flat ground, 90 a vertical face.",
            "items": {
              "format": "float",
              "maximum": 90.0,
              "minimum": 0.0,
              "type": "number"
            },
            "maxItems": 2,
            "minItems": 2,
            "type": "array"
          }
        },
        "type": "object"
      },
      "TreeLeaf": {
        "description": "What hangs on a tree's outermost branches.\n\nNOTE: leave these variants undocumented, like [`ParticleBlend`]'s — a doc\ncomment on a variant turns the schema into oneOf/const and blinds the\nvalidation walk's closed-vocabulary check.",
        "enum": [
          "blade",
          "cluster",
          "none"
        ],
        "type": "string"
      },
      "Wave": {
        "additionalProperties": false,
        "description": "One travelling wave in a [`Water`] surface's sum.\n\nGerstner rather than a sine: a Gerstner wave moves each point of the surface\n*toward* the crests as well as up, which sharpens crests and flattens\ntroughs. A sum of sines is a rubber sheet, and no amount of tuning it makes\nit read as water.",
        "properties": {
          "amplitude": {
            "default": 0.05999999865889549,
            "description": "Half the crest-to-trough height, in metres. `>= 0`.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "direction": {
            "default": 0.0,
            "description": "Heading the wave travels, in degrees: a yaw about +Y applied to the\nengine's forward axis, so `0` travels toward **−Z** and `90` toward\n**−X**. The same convention as aiming a camera, a light or a particle\ncone.",
            "format": "float",
            "type": "number"
          },
          "speed": {
            "default": 1.2000000476837158,
            "description": "Crest travel speed in metres/second. `>= 0`; reverse a wave with\n`direction`, not a negative speed.\n\nReal deep-water waves are dispersive (`c = sqrt(g·λ/2π)`, so long waves\noutrun short ones), and authoring speed per wave rather than deriving it\nkeeps a scene free to be wrong about that on purpose — a lake's chop\nlooks better slightly slower than physics says.",
            "format": "float",
            "minimum": 0.0,
            "type": "number"
          },
          "steepness": {
            "default": 0.4000000059604645,
            "description": "How far the surface is pulled toward the crests, `[0, 1]`: 0 is a plain\nsine, 1 is a cusp.\n\nThe **sum** over every wave must not exceed 1\n(`water_waves_self_intersect`). Past that the surface folds through\nitself and the crests curl into loops — a well-known Gerstner failure,\nand cheaper to refuse than to debug from a screenshot.",
            "format": "float",
            "maximum": 1.0,
            "minimum": 0.0,
            "type": "number"
          },
          "wavelength": {
            "default": 4.0,
            "description": "Crest-to-crest distance in metres. `> 0`.\n\nThis is the field that sets the *scale* of the water: 40 m swells read\nas ocean, 2 m chop as a lake, 0.4 m as a puddle. It must also be\ncomfortably larger than one grid quad, or the surface cannot represent\nthe wave — see [`Water::segments`].",
            "exclusiveMinimum": 0.0,
            "format": "float",
            "type": "number"
          }
        },
        "type": "object"
      }
    },
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "description": "A scene file, exactly as it appears on disk.",
    "properties": {
      "daylight": {
        "anyOf": [
          {
            "$ref": "#/$defs/DaylightSettings"
          },
          {
            "type": "null"
          }
        ],
        "description": "The day/night block (M21) — a sibling of `physics` and `environment`,\nnot a field inside either. Absent means the pre-M21 engine exactly:\nnothing computes a sky, and the lights are whatever the entities say."
      },
      "entities": {
        "items": {
          "$ref": "#/$defs/EntityDef"
        },
        "type": "array"
      },
      "environment": {
        "anyOf": [
          {
            "$ref": "#/$defs/EnvironmentSettings"
          },
          {
            "type": "null"
          }
        ],
        "description": "Scene-level rendering settings; absent means the defaults, which are\nthe pre-M16 renderer exactly (M16)."
      },
      "name": {
        "type": "string"
      },
      "physics": {
        "anyOf": [
          {
            "$ref": "#/$defs/PhysicsSettings"
          },
          {
            "type": "null"
          }
        ],
        "description": "Scene-level physics settings; absent means the defaults, so scenes\nwithout physics don't change (M8)."
      },
      "templates": {
        "description": "Shapes a script may spawn at runtime (M37) — declared here, never\ninstantiated when the scene loads. Absent means the pre-M37 engine\nexactly: nothing can be spawned, and no code path differs.\n\nA template is an [`EntityDef`] plus a `limit`, and it exists so a\nspawn can name something *the file already declares* rather than\nconstructing geometry from a script, which would put scene data in a\n`.rhai` and break invariant 2.",
        "items": {
          "$ref": "#/$defs/TemplateDef"
        },
        "type": "array"
      }
    },
    "required": [
      "name",
      "entities"
    ],
    "title": "SceneFile",
    "type": "object"
  }
}
